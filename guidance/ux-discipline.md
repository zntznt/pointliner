# Pointliner — UX Discipline

## The binding interaction standard

This is to UX what `CLAUDE.md` is to architecture: the durable, always-relevant standard that every feature is measured against. It exists because the project has strong engineering discipline and **no UX discipline** — interaction, discoverability, accessibility, and copy decisions are made ad hoc per feature, which is why the same concept behaves differently in different places.

**This document is binding.** `ux.md` is the *vision* (the verbosity dial, the discoverability strategy); this is the *standard* that the AI must satisfy on every change. Where the two ever conflict, this document wins for behavior; `ux.md` wins for the staging/rollout of guidance overlays.

**Normative language:** **MUST** = required to merge · **MUST NOT** = prohibited · **SHOULD** = expected (a deviation needs a one-line written reason) · **MAY** = permitted.

**Relationship to the other docs**

| Doc | Owns | Altitude |
|---|---|---|
| `CLAUDE.md` | Architecture invariants | Always loaded (carries the UX spine in its "UX discipline" section) |
| **`ux-discipline.md`** (this) | **The UX standard — behavior, vocabulary, principles** | **Read before any UI work** |
| `ux-definition-of-done.md` | The conformance gate (checklist) | Run on every UI change |
| `ux.md` | Discoverability *strategy* / verbosity dial | Vision / staging |
| `accessibility.md` | A11y *sequencing* (phased tickets) | Execution tracker |
| `adding-an-artifact.md` | Build recipe for new artifacts | On-demand recipe |

**Hard precedence.** Three engineering invariants are reality, not UX choices, and this standard never overrides them:
1. `node.text` is plain-text source of truth.
2. Keyboard operability is added **alongside** `mousedown`+`preventDefault`, **never** by converting to `click`/`<button>` (the caret invariant).
3. The render model is a **virtualized list** — per-row attributes are set in the `render()` pass, not patched globally.

When a UX rule below would require breaking one of these, the rule is satisfied *within* the invariant (e.g. add a `keydown` listener beside the existing `mousedown`, never replace it).

---

## 0. Prime directive

> **UX is a first-class acceptance criterion, equal to correctness and test-coverage.** A change that passes its tests but violates this standard is **not done**. Every UI-touching change MUST clear `ux-definition-of-done.md` before merge — the same way every new artifact MUST clear the 12-step recipe.

The five principles below are the whole standard in one breath:

| # | Principle | One-line law |
|---|---|---|
| **P1** | Predictable | A key, gesture, or word means the same thing everywhere. |
| **P2** | Discoverable | Every capability has a visible front door — never syntax-only at the floor. |
| **P3** | Reachable | Every interactive element is operable and announced to assistive tech. |
| **P4** | Responsive | No silent success, no silent failure. |
| **P5** | Coherent | One authoring language — reuse the existing syntax, don't mint a new one. |

**P1 and P5 are the twin consistency pillars** — P1 governs *behavior*, P5 governs *syntax* — and both take precedence over P2–P4. When two principles conflict, the consistency pillars win, then the lower number, then the more specific rule.

---

## 1. Canonical vocabulary

A unified UX requires unified words. These terms are binding for **UI copy, the `?` panel, docs, and `aria-label`s**. The code keeps its own identifiers (`node`, `artifact`) — this table governs *what the user sees and what we say*, and draws the one distinction that matters: the **internal** term vs the **user-facing** term.

| Concept | Internal term (code/data — unchanged) | User-facing term (copy/labels — canonical) | Never say |
|---|---|---|---|
| The single thing the user edits (one `.opml`) | `root` / `fileName` / `fileHandle` | **document** | outline, note, file* |
| A collection of documents on disk | `workspaceDir` / `workspace…` | **folder** | workspace, notebook, library |
| A line in the outline | `node` (`node.text`, `nodeById`) | **point** | item, row, bullet, entry |
| The glyph left of a point | bullet | **bullet** (the glyph only) | dot, marker |
| Parent/child relations | children/parent | **child / parent / nested** | sub-item |
| What a point renders as | `node.type` | **block** (bullet, numbered, to-do, paragraph, heading, code, quote, divider, **base**) | format, kind |
| A live inline object | `artifact` / `[[type:key]]` | **pill** | widget, chip, token, badge |
| Inserting one at the caret (`@`) | `insertInlineArtifact` | **insert** | embed, object |
| Changing a point's block (`/`) | — | **turn into** | convert |
| Focusing a subtree | `focusedId` | **zoom in / zoom out** | drill, hoist, focus |
| Hiding/showing children in place | `collapsed` | **collapse / expand** | fold, toggle |
| Typed syntax that becomes a pill | `promoteInlineShorthand` | **shorthand** | macro, inline formula |
| A keyboard-navigable overlay list | — | **menu** (`/`, `@`, link, state) | dropdown, popup, palette |
| The `#TODO`/`#NEXT`/`#WAITING`/`#DONE` marker (any sequence state) | `#KEYWORD` at start of `node.text` (the `#` prefix reuses the hashtag sigil) | **state badge** | tag, label |
| An ordered, user-definable set of state keywords with a done split (the built-in to-do set is the default one) | `[[seq:key]]` + `node.seq` sidecar · `collectSequences` | **sequence** | workflow, status set, pipeline |
| A static pipe table written in any point's text — a display/export form, not a view of a base | — | **table** | markdown table, grid |
| A structured data object with its own dedicated point; its table view is the current (and default) view | `node.type === 'base'` (code) · `/base` verb | **base** | dynamic table, widget, database |
| The whole-base top bar | — | **base header** | toolbar |
| The base's bullet menu (whole-base ops + node ops) | base bullet (grid icon) → `showBulletPopup` | **base menu** | table menu, ⋯ menu |
| The per-column operations menu | — | **Column menu** | column panel |
| The editable column-name chip in a base header cell | — | **name pill** | header chip |
| One-click column aggregates | `mtApplyAggregate` | **Calculate** | summary |
| The dated record of every generated result | `logRoll` / `root.rollLog` / the `Random results` home point | **random results** (the log) | roll log, rolls, dice log |

> **Rule V-1 (MUST):** New user-facing strings use the user-facing term. **Rule V-2 (MUST NOT):** Rename the internal `node`/`artifact` identifiers — that is churn against load-bearing code; the split above is the entire point.
>
> **The containing hierarchy is folder > document > point.** \* "file" is not banned: a saved/downloaded `.opml` (or a self-contained `.html`) genuinely *is* a file, so "save to a file", "saved to a file", "Save to this file" are correct — **file = the on-disk storage, document = the thing you edit.** What is banned is calling the document itself an "outline" / "note" (use **document**). Note the two other legitimate senses of "outline" that stay: **"outliner"** (the app's nature) and **"the outline"** meaning the navigable tree/view you're looking at (not the file). And **per-point notes** (`node.note`, the text under a point), footnotes, and `is:note` keep "note" — that is a different concept from the document.

The canonical split — **"node" in code, "point" in copy** — also resolves the terminology drift the existing docs already show (they use node / bullet / item / artifact / pill / widget interchangeably).

---

## 2. The five principles

Each carries: the **law**, **normative rules**, and an **acceptance test** the AI can self-check against (the testable criteria the current UX docs lack).

### P1 — Predictable

> **Law:** A user who learns one interaction MUST be able to predict the rest. A key, gesture, or word resolves to the same conceptual action in every context.

- **P1-1 (MUST):** No key *silently* inverts meaning by context. `Enter` = new point and `Shift+Enter` = line break in every block — **one advertised exception: Paragraph (prose mode)** inverts these (`Enter` = line break, `Shift+Enter` = new point). This exception is conformant because it is explicit: the `/` menu description and the empty-state hint both advertise the inversion. Any undocumented per-block inversion is a violation. **`Enter` = new point splits at the caret** (UXP-60): the trailing half becomes the new sibling, the leading half and any children stay on the source, and the continuation marker (to-do/quote/numbered) leads the new half — caret at end is the empty-continuation append. This is peer-standard (Workflowy/Logseq/Roam) and not a context inversion: it is the *same* "new point" action, made caret-aware.
- **P1-2 (MUST):** A new shortcut MUST fit the keyboard grammar in §3. Introducing a new modifier meaning requires editing §3 first.
- **P1-3 (MUST):** `Esc` always resolves *outward* in one fixed order: close menu → clear cell selection → clear point selection → zoom out → blur. No feature gives `Esc` a different direction.
- **P1-4 (MUST):** Destructive keys are guarded (`Backspace` deletes a point only when empty **and** childless — codify the current behavior so it can't regress).
- **P1-5 (SHOULD):** A shortcut a browser can intercept (`Ctrl+1…6`) MUST NOT be the *only* path to a capability and MUST be documented as best-effort; the visible control is primary.

**Acceptance test:** hand a user §3 and they correctly predict a shortcut they've never pressed.

### P2 — Discoverable

> **Law:** No capability is reachable *only* by typed syntax or *only* by a memorized key. Every capability offers a **visible affordance**, a **typed path**, and a **menu path** — and the menu teaches the syntax.

This is fully compatible with `ux.md`'s lean-first model: the *capability* must never be syntax-only at the **floor** (`ux.md`'s own rule: "a complete reference is reachable from every mode"), but the *visible affordance* MAY be a verbosity-gated overlay that Lean mode quiets. Discoverable ≠ loud; it means *a way in exists without prior knowledge*.

- **P2-1 (MUST):** Every capability satisfies all three doors. A capability that is built but reachable only by syntax (or gated entirely off with no visible path at any verbosity) is **non-conformant**. *(The `[[` link picker was the canonical example — built but gated off; it is now un-gated (UXP-4). The rule stands for the next case: "no front door at any level" is never a conformant end state.)*
- **P2-2 (MUST):** Menu items show **label + one-line description + the typed syntax** (the `/`/`@` menus already do this — it is now the standard for every menu).
- **P2-3 (MUST):** A power feature entered only through raw markdown MUST also have an affordance. *(Org `#+TBLFM:` formulas are entered today by typing a raw line with no UI — non-conformant; a formula affordance is required, syntax retained as the power path.)*
- **P2-4 (SHOULD):** Stateful generative state is inspectable — variables and tags get an overview surface. *(Both shipped: the variables panel + `{` picker — UXP-9; the `#` tag picker sourced from the document-wide tag index — UXP-10.)*
- **P2-5 (SHOULD):** Shorthand previews before it commits (pairs with P4-2).

**Acceptance test:** a user who has read no docs can find and use the feature through visible UI alone (in Guided mode).

### P3 — Reachable (accessibility)

> **Law:** The outline is navigable to assistive technology, and every interactive element is keyboard-operable, programmatically named, and focus-visible — added **additively**, never by a visual redesign and never by replacing `mousedown`.

This principle **defers to `accessibility.md` for sequencing.** §5 states the targets and the interim behavior; `accessibility.md` owns the phase order. The standard's job is to make sure a11y is a *requirement on every feature*, not a separate project that touches the same widgets in a disconnected pass.

- **P3-1 (MUST):** Every interactive element has a programmatic accessible name (`aria-label` or visible label) — `title` alone is insufficient.
- **P3-2 (MUST):** Every interactive element is keyboard-operable, via a `keydown` listener **beside** the existing `mousedown` (the caret invariant).
- **P3-2b (MUST — #1470):** A focusable control rendered **inside a point's contenteditable** also carries `contenteditable="false"`. `tabindex` alone is not enough and *looks* like enough: with the control still editable, the browser retargets focus to the editing host on the first keystroke, **before `keydown` is dispatched**, so the twin from P3-2 reads `e.target` as the point, never runs, and the key falls through to the outline (Enter splits the point). It only misbehaves while the host has no selection yet — a point never focused this session, i.e. every point on a freshly loaded document — which is why a source pin on the twin stays green and only a driven check on a *cold* point sees it. The generative pills had this from the start; ten other controls shipped without it. `tabindex` value stays each control's own business (`-1` for inline chips, `0` for a consent prompt, native for a checkbox); the `contenteditable="false"` is the invariant. Enforced by the census in `tests/test.mjs` (`#1470`, enumerated from the twins in `onKeyDown`) and driven in `tests/browser.mjs`.
- **P3-3 (MUST):** Visible `:focus-visible` on every focus stop; honor `prefers-reduced-motion`.
- **P3-4 (MUST):** Color is never the sole carrier of meaning (state badge, `#ERR`, highlight pair with text/icon).
- **P3-5 (MUST):** State changes not tied to focus (reroll, autosave warning, `#ERR`) are announced via the `aria-live` region.
- **P3-6 (landed — UXP-19):** Full `role="tree"` on the outline (flat treeitems + `aria-level`/`posinset`/`setsize`, the virtualized-tree pattern), `role="grid"` on interactive bases, and `tabindex="-1"` + Enter/Space activation on pills shipped in the dedicated UXP-19 pass — additively, beside the existing `mousedown` paths (the caret invariant held). The interim labels (pill `aria-label` updated on reroll, per-row labels) remain in force as the ongoing P3-5 obligation.

**Acceptance test:** the feature is operable by keyboard only, and a screen reader announces its name, state, and any result.

### P4 — Responsive (feedback & errors)

> **Law:** Every action confirms; every failure explains. No action produces an unexplained result.

The project already has the **reference pattern**: the storage-quota warning (proactive, plain-language, actionable). Generalize it; don't reinvent it.

- **P4-1 (MUST):** A failure is never silent. *(Inline `{…}` shorthand that fails to promote MUST signal "not recognized," not just stay plain text.)*
- **P4-2 (SHOULD):** Errors explain the cause. *(`#ERR` becomes `#ERR (cycle)` / `(bad ref)` / `(non-numeric)` in the cell and its `aria-label`.)*
- **P4-3 (MUST):** Structural/destructive actions confirm via the existing toast (delete subtree, paste points, cut, bulk indent).
- **P4-4 (MUST):** Feedback reuses the §7.3 pattern — toast for transient success, inline marker for validation, `aria-live` for non-focused change, banner for approaching-limit. No feature invents its own feedback UI.

**Acceptance test:** no action — success or failure — leaves the user with "nothing happened and no reason why."

### P5 — Coherent (one authoring language)

> **Law:** Pointliner has **one** authoring language, not a pile of per-feature mini-languages. A new capability MUST express itself within an existing syntax family. Inventing a new syntax or grammar is a last resort that requires sign-off and, by default, **retires or subsumes** the syntax it overlaps. Net syntax count does not grow casually.

**Why this is its own principle.** The clearest symptom of scattered direction is **syntax sprawl** — each feature shipping its own delimiters and notation, until users (and the AI) face a flood of new ways to write things. The architecture *invites* this: `CLAUDE.md` tells contributors "a new token type / new expression primitive fits very well." That is true for the **engine** and false for the **user** — cheap to add is not free to learn. P5 is the UX counterweight.

- **P5-1 (MUST):** A new feature reuses a syntax from the inventory below. No new top-level delimiter, sigil, or notation without explicit sign-off.
- **P5-2 (MUST):** The `{…}` grammar engine is **the** composition layer for generative/computed inline content. New generative or computed features plug into `{…}` (a new `resolveBrace` branch or an `evalMath` primitive) — **not** a new delimiter. (Already the architecture: "every custom artifact is under grammar." P5 makes it a UX rule too.)
- **P5-3 (MUST NOT):** Ship two syntaxes for the same outcome. A proposed parallel notation — e.g. a render-only `{= expr}` *second* syntax alongside the promoting one, or `B3`-style table refs beside `@row$col` — is rejected unless it **replaces** the old one. (`@row$col` is already declared "the one true form" for tables; extend that discipline everywhere.)
- **P5-4 (MUST):** Every authoring syntax lives in the inventory **and** the `?` panel. A typeable, meaningful syntax that isn't documented in one place is a defect.
- **P5-5 (SHOULD):** Prefer **subsuming over adding** — extend an existing syntax's grammar rather than mint a sibling.

**Syntax inventory — the closed set.** This is the entire authoring language. Additions require sign-off and an inventory update; new work maps onto one of these rows.

| Family | Syntax | Owns |
|---|---|---|
| Markdown | `#`–`######`, `>`, **`>!`** (spoiler), ```` ``` ````/`~~~`, `---`/`***`, `-`/`1.`/`- [ ]`, `**`/`__`, `*`/`_`, `++`, `` ` ``, `~~`, `==`, `[text](url)`, `![alt](url)`, bare-URL autolink, GFM pipe table (header + `---` delimiter row), definition list (`term` then `: def`) | block + inline text formatting, links, images, autolinks, static pipe tables, definition lists, **spoiler blocks** |
| Emoji | `:shortcode:` | emoji |
| Hashtag | `#tag`, **nested** `#tag/sub`, hyphens ok (`#plain-tag`) | tags. Nesting is an extension of the existing `#` form (segments `[\w-]+` joined by `/`), **no new sigil or delimiter**; tag search is hierarchical (`#thread` matches `#thread/torn-letter`) and **inherits down the subtree** (2026-07-26 — the tag gained *reach*, not notation, so still no new sigil: a tag covers every point filed under it; ancestor STATE keywords are stripped and `has:tag` stays literal). Grammar mirrored in `mdInline` / `collectTags` / search-parser / the chain builders |
| Footnote | `[^key]` | footnotes |
| Node link | `[[#id\|label]]`; **cross-doc** `[[docId#id\|label]]` (CF-2) | links / mirror — same-doc, plus the roadmap-locked cross-file form (a docId before the `#`, no new delimiter) |
| Artifact token | `[[type:key]]` | stored pill reference (internal — users never type it) |
| **Grammar engine** | `{…}` → `{= expr}` (incl. subtree rollups `{= sum\|avg\|count\|min\|max(prop)}`, the query-reducer overload `{= sum\|avg\|min\|max("query", prop)}` — SR-8, and `{= convert(x, from, to)}` declarable units — SR-6), `{NdM}`, `{a\|b 2\|c}` (a weight is a number **or** a `{= expr}` — A5 dynamic odds), `{cond: then \| else}`, `{shuffle\|cycle\|once\|stopping: a \| b \| c}`, **`{markov: a→b, b→c}`** (typed markov chain — `markov:` keyword then comma-separated `from→to weight` transitions, an anonymous twin of the `@`/Markov dialog), **`{rule Name: a \| b}`** (typed NAMED grammar declaration — a `rule` keyword + a name + an ordinary rule right-hand side; a registered, document-wide callable `[[grammar:key]]` pill, the typed twin of the `@`/Grammar dialog and the last of the three declaration forms to get one), **`{seq Name: active \| done}`** (typed sequence declaration — `seq` keyword + a name + the `parseSequence` states tail; a NAMED, atomic `[[seq:key]]` pill, the typed twin of the `@`/Sequence dialog), **`{Nx: template}`** (repeat — emit template N times; N a literal 1–99 or, via #545, a dice roll `{2d4x: …}`), `{rule}`/`{table}`/`{chain}`/`{var}`, **`{name := expr}`** (typed variable declaration, formula or random pick — the typed twin of the `@`/Variable dialog; `:=` sniffed before the `:`-splitting paths so it never collides with rule lines or `{= }`. Stage A of guidance/typed-var-declaration-proposal.md), **`{"literal"}`** (a quoted string literal — the text-value escape so a bare word stays a rule lookup; nested `{…}` still interpolate inside), **`{ref.mod}`** (A1 text modifier — `cap`/`title`/`upper`/`lower`/`a`/`s`/`ed`/`ing`/`poss`/`ord`), **`{row.field}` / `{base.row.field}` dotted variable reads** (variable-base projections — the same `.` form as A1/A6, resolved rule-first then longest-dotted-name, optional trailing modifier chain; see the variable-bases note below), **`{query: expr}`** (QP-1 — an embedded live search over the outline, the typed twin of the saved-search door), **`{roll: search-query}`** / **`{roll folder: search-query}`** (DECISION-191b — pick a random point matching the query, the generative twin of `{query:}`; the optional `folder` MODIFIER before the colon widens the draw to the whole reading set, #899. Recorded 2026-07-27: the scope word sits in the reserved-keyword slot, the same place `{prop KEY:}` / `{date KEY:}` / `{rule Name:}` read a modifier, NOT in the query, so `{roll: folder}` still searches for the word — the §4c rule that `folder` must stay searchable text. It cannot ride a record field the way the scoped `{query:}` pill does, because a roll has no sidecar: it freezes into an anonymous grammar whose `def` IS this text, and a re-roll re-expands that text. Consequence, in its favour: a scoped roll unfolds in edit mode, where a scoped query pill must stay atomic), **`{count: search-query}`** (#541 — the query family's third verb: a live inline number of matching points, the counted twin of `{query:}` which lists them and `{roll:}` which picks one; `count:` keyword-sniffed like its siblings), **`{oracle: band}`** / **`{oracle: band + swing}`** (#543 — the typed twin of the `@`/Oracle door: an `oracle:` keyword + a likelihood band (`certain`/`likely`/`even`/`unlikely`/`impossible`), optionally `+ swing` for the six-way answer; promotes to the same anonymous `Yes N | No M` weighted-alt pill the dialog builds, over **original** odds bands per the IP fence), **`{prop key: value}`** (LF-1 — typed property write; consumes the brace on exit, the value renders as a chip), **`{date due: value}`** / **`{date start: value}`** (LF-1d — typed schedule write; consumes the brace, validated by `parseDueDate`, invalid stays literal + flashes, #407), **`{meter: value/max}`** (#648 — a segmented bar of a numeric property; a COMPUTED render-time display like the `[/]` cookie, resolved live against the render node, no sidecar, not a control) | **ALL** generative, computed, and **declared-config** inline content. The declared-config cluster (`{prop}`, `{date}`) is a RECORDED widening (2026-07-09, closing #393): a side-effectful sidecar write that consumes the brace is neither generative nor computed, but overloading the ONE brace beats minting a sigil (P5-1); the widening itself is now part of the charter, not an unrecorded creep. |
| Dice notation | `NdM[!][rK][kh\|kl\|dl\|dh N][>=…]` | dice (inside `@dice` and `{…}`) — `rK` = reroll any die ≤K once (an additive extension of this row, 2026-06-14; not a new family) |
| Grammar rules | `name: a \| b 2 \| c`; **dotted sub-rules** `key.field: …` | named grammar rules — **a roll table IS one of these** (one rule = a weighted table; the `entry weight`-per-line roll-table syntax was **retired** by the June 2026 collapse, legacy records migrate on load). A6: a rule name may be **dotted** (`sword.damage: 1d8`), read via `{item.field}` — same `.` form, not a new delimiter |
| Markov | `State -> Target weight, …` | markov chains |
| **Uncertain value (estimate)** | `lo to hi` (a 90% CI → lognormal), `normal(m,s)`, `uniform(lo,hi)`, scalars, `+ − × ÷`, and `sum(prop)`/`avg(prop)` over children — inside `@estimate` and the `{lo to hi}` shorthand | **distributions** (B2) — the ONE recorded exception that adds a new computational sub-language, because a distribution **cannot be a number** so it cannot ride `evalMath`; has its own Monte-Carlo sampler. The `to` operator is the new lexical element |
| Table formula | `#+TBLFM:` with `@row$col` + ranges | table calc (the one true form) |
| Status headline | `#TODO [#A] body` (any sequence keyword; `#` reuses the hashtag sigil) | task state + priority — the keyword may be any state of any **sequence** (default or user-declared); bare `TODO` without `#` is plain text |
| Search query | `term term` (AND), **OR via a standalone spaced `\|`** (QX-5 — AND binds tighter; glued/negated/quoted pipes stay literal), `"a b"`, `-term`, `#tag` (hierarchical: `#thread` matches `#thread/torn-letter`; **inherited**: a tagged point's subtree matches too), `is:` (whitelist: `done`/`todo`/`note`/`failing`/`passing`/`scheduled`/`unscheduled`/`overdue`/`held`/`pill`/`random`/`leaf`/`parent`/`collapsed`/`expanded`/`broken`/`empty`/`orphan`/`duplicate-title`/`recently-edited`), `state:value` (seq-aware, UXP-113), `priority:A` + `priority:none`/`any` (UXP-109/183), `var:NAME` (QX-3 — the declaring point), `key:value` property filter (+ quoted spaced values, UXP-131) and **numeric compares** `key:>N`/`key:<N` (QX-4), `due:`/`start:` `today`/`overdue`/`<date`/`>date`/`week`/`month` (QX-2 windows), `has:` (reserved words `tag`/`dice`/`math`/`est`/`grammar`/`markov`/`var`/`seq`/`children`/`footnote`/`link`/`backlink` (QX-1/6), else `has:<propkey>` presence) | search-box filtering only — never node text. Recorded UXP-20 decision (2026-06-13): operators over the **existing** vocabulary; `is:` is the one field-prefix pattern. OR + `state:` SHIPPED (QX-5 / UXP-113, sanctioned — this row previously said the opposite; corrected 2026-07-09, #408). Malformed tokens stay literal text (the `{…}` escape-hatch rule). `due:`/`start:` are date-aware specialisations of `key:value` — not a new operator family. Front doors: the focus-shown legend under the box + the `?` panel. The `is:` whitelist here is drift-guard-pinned against `parseSearchQuery` |
| Progress cookie / clock | `[/]` (fraction), `[%]` (percent), **`[o N/M]`** (manual clock), **`[o /M]`** (counting clock) | a live tally of a point's checkboxes + child tasks (`[/]`/`[%]`), or a hand-advanced segmented clock (`[o N/M]`). Recorded UXP-20 decision (2026-06-13): the **Org-canonical** cookie, reusing the `[…]` bracket authoring family (`[ ]`/`[x]`, `[#A]`, `[^key]`, `[[…]]`) — no new sigil. A computed *display* value (edit shows the `[/]` recipe, render shows `[2/5]`), the same edit-raw/render-pretty model as `#+TBLFM:`; the token is plain text in `node.text`, no sidecar. Counts each checkbox marker individually + each keyword/sequenced child once (done-ness sequence-aware); scope = own text + direct children. Front door: `@progress`. A non-cookie literal `[/]`/`[%]` only renders when the point owns tasks; otherwise it stays text. The **clock** (`[o N/M]`, #646) reuses the same `[…]` family and text-is-truth model but its `N` is authored/clicked, not computed (front door `@clock`) |
| Dates (start + due) | `start: YYYY-MM-DD` and/or `due: YYYY-MM-DD` (also `today`, `today+N`, `tomorrow`) as **property** values | per-point scheduling as a start→due **range** (project-management style). Recorded UXP-20 decision (2026-06-13, extended 2026-06-13): dates live as `start`/`due` properties in `node.props` — zero new authoring syntax (the properties system already exists); the `due:`/`start:` search operators are date-aware extensions of the existing `key:value` search row. Displayed as date-smart chips (Today / Tomorrow / Mon / 3d overdue; the start chip leads with `▸`). Front doors: `/due` slash verb (labelled **"Schedule"**), bullet menu "Set / Edit dates", Agenda strip (toolbar button). The Schedule dialog's date fields carry a full-width **inline `role=grid` calendar** — type in the field or `ArrowDown` into the grid, arrows + Enter/click to pick (day cells fire on `mousedown`+`preventDefault`, the caret invariant; the caret returns to the field on pick). **A day-pick COMMITS-AND-CLOSES when the other date field is empty** (#418, owner-adjudicated trim 2026-07-09): the common single-date bump is chip → day, two interactions; a point with both dates in play keeps the explicit Save (range edits stay deliberate). Clicking a `due`/`start` chip opens the Schedule dialog; date keys don't appear in the generic Properties editor. The Agenda is a **vertical stack of bars** inside the toolbar (not a sidebar; the stack clamps to the viewport with the agenda pane as the scroll region — the #389 ceiling, design-language §4): a top bar carries the one-at-a-time view switcher (**Week · Month · Timeline**) plus the **Done / Running / Overdue / Sort** filter chips with the always-present **List** (Due + Running rows); the selected view — **Week**, **Month** (a month grid with `‹ ›` nav + arrow-key cell navigation) or **Timeline** (a Gantt chart — fixed name column + scrollable day axis, today line) — opens as its own full-width bar below. All views read the same `collectDueDates`; click/Enter any item to zoom in. The Schedule dialog also carries a **Repeat** field (a reserved `repeat` property, #462 — see the recurring-tasks note below); completing a task with a repeat rolls its dates forward and re-opens it. Documented in the `?` panel's **Dates & agenda** section + the focus-shown search legend (UXP-37). |

**Note — cross-document links (CF-2) extend the Node-link row (no new delimiter).** Recorded decision (2026-06-16): `[[docId#nodeId|label]]` — a link to a point in **another workspace document** — is the **roadmap-locked cross-file extension** of the existing `[[#id|label]]` node-link token (`guidance/roadmap.md` Phase 2 step 5), **not a new delimiter or family**. It mints no new sigil: the only new lexical element is an optional **docId before the `#`** (the same `[[ … ]]` brackets, the same `#nodeId`, the same `|label`). Detection is by non-collision — the same-doc form requires `#` immediately after `[[` (zero chars before it), the cross-doc form requires ≥1 char before `#`, so the two regexes are mutually exclusive and `collectLinks`/`LINK_RE`/the same-doc pill are untouched. A token whose docId is the current doc **delegates to the same-doc pill** (mirror included), so a copied `[[A#id|]]` behaves identically in its home doc. Behavior is P1-consistent: a cross-doc link **clicks to navigate** exactly like a same-doc one — it just switches the document first (dirty-guarded), then zooms. A missing target is **visibly broken** (P4), never silent; the pill carries a muted trailing `↗` cue + an `aria-label` naming the target doc (P3). Cross-doc links are **title-only in v1** (no transclusion/mirror across docs). Front doors (P2/P5-4): **Copy link** emits the portable form when a workspace is connected; the `?` panel's **Links** note and `features.md` document the `[[docId#id]]` form, and the `↗` cue teaches it. Chromium-gated (cross-doc needs the workspace); same-doc links are unchanged and ungated. The inventory row gains a form; no new family.

**Note — spoiler blocks extend the Markdown blockquote row (no new family).** Recorded decision (2026-07-13, #645): `>!` at the start of a line — a **spoiler / secret block**, hidden behind a blur in display until clicked or keyed to reveal — is an **extension of the existing `>` blockquote form on the Markdown row**, not a new top-level delimiter or family (the same kind of extension as nested tags `#tag/sub` on the Hashtag row). It mints no new sigil: `>` already means "set-apart block," and the only new lexical element is a single `!` immediately after it (`SPOILER_RE = /^ {0,3}>!\s?(.*)$/`), a character that carried no prior meaning after `>` (images are `![`, never `>!`). **The one correctness constraint is parser order:** because `BQ_RE = /^ {0,3}>\s?(.*)$/` also matches `>!` (with `!` as content), the spoiler branch MUST be tested **before** the blockquote branch in `mdToHtml`, and the blockquote grouping loop excludes `>!` lines — so a spoiler is never swallowed into a `<blockquote>`, in either order (a quote then a spoiler, or the reverse). Reveal is **transient DOM state only** (a `.revealed` class toggled by `toggleSpoiler`, never written to `node.text`), so a spoiler re-conceals on the next render — deliberate for the self-blinding use. Edit mode needs no special handling: `>! text` is plain editable markdown like `>` or `[[#id|label]]`, not an atomic pill. Export can't conceal (a flat file has no reveal), so `flattenSpoilers` labels each spoiler line `(spoiler) …` in Markdown/plain-text export — the reader is warned (P4), never silently shown or silently dropped. Reveal is keyboard-operable (role=button, tabindex, Enter/Space, aria-expanded, mousedown+preventDefault for the caret invariant — the note-indicator pattern reused, P3). Front doors (P2/P5-4): the `/secret` menu verb (writes the `>! ` prefix) and the **Secret and spoiler blocks** concept-guide entry + `guide/`. The inventory row gains a form; no new family.

**Note — progress clocks extend the Progress-cookie row (no new sigil).** Recorded decision (2026-07-13, #646): `[o N/M]` — a hand-advanced **segmented tension clock** (Blades/Ironsworn), rendered as a quarter-fill ring glyph (`○ ◔ ◑ ◕ ●`) plus the exact `N/M` count — reuses the **`[…]` bracket authoring family on the Progress-cookie row**, the same home as `[/]`/`[%]`/`[#A]`/`[^key]`, not a new delimiter or family. The `o` is a mnemonic body inside the existing brackets, not a new sigil. It follows the cookie's **text-is-truth model exactly** (the token is plain text in `node.text`, no sidecar, OPML round-trips for free) with **one deliberate difference**: unlike the cookie's *computed* tally, a clock's `N` is **authored and advanced by click**, so the token carries both literal numbers and a click rewrites `N` in the text (the same edit-raw/render-pretty round trip). The glyph is a **unicode string in the sparkline idiom** (`clockGlyph`), never an SVG arc — so it is export-safe (`flattenArtifacts` freezes it to `◑ 3/6`, the same string the pill shows) and the cores are pure + Node-testable (`parseClock`/`clockGlyph`/`formatClock`/`advanceClock`/`advanceClockInText`). The ring rounds fill to the nearest quarter while the number stays exact, so a 6- or 8-clock loses no information. Interaction is P1-consistent with the generative pill family (a click changes the pill): click advances, Shift+click steps back; keyboard Enter advances, Shift+Enter steps back; `mousedown`+`preventDefault` for the caret invariant, `role="button"` + `aria-label` (P3). An out-of-bounds token (`[o 9/6]`) fails the strict `parseClock` and **stays literal text** (P4, the `[…]` escape-hatch rule), never a miscounted pill. Front doors (P2/P5-4): the `@clock` insert verb (drops `[o 0/6]`) and the **Progress clocks** concept-guide entry + `guide/`. The inventory row gains a form; no new family. **Extended same-day (#646 follow-up): a COMPUTED clock `[o /M]`** — an **empty count slot** before the `/` — makes the clock tally its fill from the point's done sub-points, exactly the way the `[/]` cookie counts (`clockFillFor` = `progressCount(node).done` capped at `M`). This is the cookie's own "empty means compute" instinct inside the same token, not a new form-family: a digit count is manual, an empty count is computed. Because the tally is **stateless** (recomputed every render like the cookie), the "advance on add / regress on delete" behavior is free — no event tracking. A computed clock is a **display value, not a control** (class `clock-computed`, no `role=button`/`tabindex`/advance handler — you cannot click a computed value, exactly as you cannot click a `[/]` cookie); the manual-clock click-ordinal walk skips computed clocks (`.clock:not(.clock-computed)`) so the two coexist in one point without index drift. A **completion cue** (`clockCompletionCue`/`completingChildIndex`) marks the sub-point whose done-ness fills the last segment with a muted `●` (`.clock-done-cue`, a content glyph exempt from the contrast floor, DL §4) — render-only (a parent↔child relationship, never in `node.text`, so not exported). The cue applies only to computed clocks (a manual clock's `N` is clicked, not child-derived, so "the completing child" is undefined for it).

**Note — meter pills are a computed sub-form of the Grammar-engine `{…}` row (no new sigil).** Recorded decision (2026-07-13, #648): `{meter: value/max}` — a **segmented bar of a numeric property** (LegendKeeper's Meter Block: HP / spell slots / stress), rendered as a unicode-block bar (`██████░░░░`) plus the exact count — is a new content-sniffed sub-form of the existing `{…}` brace grammar, the same class as `{= expr}` / `{cond:}` / `{query:}`, not a new delimiter or family. It is **computed, not authored** — so it follows the `[/]` cookie model, NOT the clock's: the `{meter: …}` recipe stays plain text in `node.text` (no sidecar), resolves **live against the render node** (`cookieNode`) every render, and is a **display value, not a control** (no `role=button`, not clickable — you cannot click a computed value). It reads the point's own properties via `childPropNumber`: `hp/hpmax` (prop/prop), `hp/20` (prop/literal max), `hp` (bare → percent of 100), or `8/12` (two literals). The bar is a **unicode string in the sparkline idiom** (`meterBar`), never an SVG — so it is export-safe (`flattenArtifacts` freezes it to the same `██████░░░░ 8/12` string) and the cores are pure + Node-testable (`parseMeter`/`resolveMeter`/`meterBar`/`formatMeter`). The bar rounds fill to 10 cells while the count stays exact (the "glyph is mood, number is truth" rule, as with the clock). An unresolvable meter (missing or non-numeric property, zero max) shows a visible `{meter?}` marker (P4), never a silently wrong bar. Front doors (P2/P5-4): the `@meter` insert verb (drops `{meter: hp/hpmax}`) and the **Meters** concept-guide entry + `guide/`. **Icon-pool styles (same-day follow-up):** a trailing style word — `{meter: hp/5 hearts}` (also `dots`/`boxes`/`skulls`/`stars`/`shields`/`droplets`) — draws one FA glyph per unit, filled at accent, **empty = the same solid glyph dimmed** (`.meter-icon-empty`, opacity; FA Free has no outline for skull/shield/droplet, so a dimmed solid carries the empty read universally, `parseMeter` gains the style word + `meterPool`). This required a **woff2 re-subset** — the embedded weight-900 FA blob was regenerated from the vendored `fa-solid-900.ttf` to add heart/skull/star/shield/droplet **on top of the existing glyph set** (verified: 0 lost, +5 gained; the regular/brands blobs untouched), with `::before` rules + `FA_GLYPHS` entries added per `adding-an-artifact.md`. **The pool is hard-capped at 10 icons** (`METER_POOL_CAP`): a larger max **falls back to the bar** (`meterPool` returns null → the bar renders), so a `{meter: hp/500 hearts}` never draws a runaway row — a P4/UI-safety guard. An icon row **exports as the bar** (a flat file has no FA glyphs). Deferred (recorded): a base **column role** (`Show as → Meter`) reusing the same core at the cell paint site, half-icon ratings, and level-tinted fill. The inventory row gains a form; no new family.

**Note — conditional text reuses the Grammar-engine row.** Recorded decision (2026-06-14): `{cond: then | else}` (Ink-style conditional text — emit `then` when the comparison holds, else `else`; `else` optional) is a new **content-sniffed sub-form of the existing `{…}` brace grammar**, not a new top-level delimiter — the same kind of addition as `{= expr}` / `{NdM}` / `{a|b}` already on that row. It mints no new sigil at the floor: the condition is an **`evalMath` comparison** (the existing expression engine, reused) and the branches are **grammar templates** (the existing engine, reused); the only new lexical element is the `:` separator *inside* a conditional brace, content-sniffed exactly like the brace's existing `=`/`|`/dice forms. Detection is syntactic (a comparison before a top-level `:`, no top-level `|` in the condition) so a plain alternation `{a|b}`, a rule definition `name: …`, or a prose `{note: hi}` never read as a conditional. An unresolvable condition fails **visibly** (`{cond?}` marker, P4), never silently. Front doors (P2/P5-4): the `{cond}` chip + hint in the grammar dialog, and the **Pills & shorthand** row in the `?` panel. The inventory row gains a form; no new family.

**Note — stateful sequences reuse the Grammar-engine row.** Recorded decision (2026-06-14): `{shuffle | cycle | once | stopping: a | b | c}` (a deck that draws without replacement, or an ordered cycle/once/stopping run) is a new **content-sniffed sub-form of the existing `{…}` brace grammar**, the same shape as the conditional above — a reserved *mode keyword* before a top-level `:`, then `|`-separated items (the existing alternation list). It mints no new sigil: the items reuse `|`, each item is a grammar template (may roll dice / call a rule), and the only new lexical element is the `:` after a mode keyword, content-sniffed like the brace's existing forms. Detection (`seqParts`) is syntactic — a word from the closed set `{shuffle,cycle,once,stopping}` before the `:` — so a conditional (a comparison before the `:`), a plain alternation, a rule definition, or a prose `{note: hi}` never read as a sequence. The pill is **stateful as a standalone pill** (the draw position/bag lives on its grammar record and round-trips through `_grammar`); used *inside* a rule it degrades to a stateless pick (no per-instance record there). Body-click **advances** (P1: a generative pill changes on click, here deterministically/without-replacement rather than re-rolling); it **unfolds** to its `{mode: …}` source for inline editing (no pencil), the dice/anonymous-grammar model. This is the home the standing "decks/bags / stateful randomness" open question was waiting for — CLAUDE.md's invariant is updated from "nowhere clean to live yet" to resolved. Front doors (P2/P5-4): the `@` **"Deck"** menu door (its own dialog) and the **Pills & shorthand** row in the `?` panel. The inventory row gains a form; no new family.

**Note — string literals reuse the Grammar-engine row.** Recorded decision (2026-06-30): `{"text"}` (also `'text'`) — a **quoted string literal** — is a new **content-sniffed sub-form of the existing `{…}` brace grammar**, the same class as the conditional / sequence forms above, not a new top-level delimiter. It closes the one hole in "everything is typeable": a **text/pick variable** could only be made from the `@`/Variable dialog, never typed, because the typed `{name := expr}` path sniffs a bare-word RHS as a *generator rule* (`{label := hello}` → `{hello?}`, a rule lookup). The quoted literal is the escape: `{client := "Acme Corp"}` declares a fixed text value, restoring parity with the dialog's Pick mode. It mints no new sigil at the floor — quotes carried no prior meaning in a grammar body, and `resolveBrace` simply strips a matched leading/trailing `"`/`'` and returns the inside (run through `expandTemplate`, so nested `{…}` still interpolate: `{"Hi {name}"}` is a literal frame with live values, not a dead end). **Predictability (P1) was the design driver:** a bare word *always* means a rule reference, quoted text *always* means a literal, so the meaning never depends on whether a rule of that name happens to exist (the rejected alternative — "bare word falls back to literal when no rule matches" — had exactly that context-dependent inversion). Detection is the first check in `resolveBrace` (quotes win over every sniff). The literal flows through the same `rollPickSource` chokepoint, so a typed `{name := "…"}`, a dialog Pick of `"…"`, and a standalone `{"…"}` all behave identically and a typed var round-trips through unfold (`{name := "text"}`) unchanged. Front doors (P2/P5-4): the **Variables** concept-guide entry (`{company := "Acme Corp"}` example + body line) and `guide/computing-numbers.md`'s "a variable can hold text" subsection; the dialog's Pick mode remains the visible form door. The inventory row gains a form; no new family.

**Note — typed markov chains reuse the Grammar-engine row.** Recorded decision (2026-06-30): `{markov: a→b, b→c}` — a **typed inline markov chain** — is a new **content-sniffed sub-form of the existing `{…}` brace grammar**, the same class as the sequence / conditional forms above, not a new top-level delimiter. It closes a typeability gap: a markov chain could only be made from the `@`/Markov dialog, with no inline form (unlike dice/grammar/decks, which all promote when typed). Detection (`markovParts`) is the reserved keyword `markov` before a top-level `:` — exactly the `seqParts` shape — so it never collides with a conditional (needs a comparison before the `:`), a sequence (a `shuffle/cycle/once/stopping` mode), a repeat (`Nx:`), or a plain `{name: rhs}` rule line. It mints no new sigil at the floor: inside the brace it is the **existing `parseMarkov` grammar** (`from→to weight`, arrow `→` or `->`), just comma-separated instead of one-per-line (the inline shape; `markovParts` re-joins commas to the newline `def` the parser already reads, and a comma fragment with no arrow extends the previous transition's target list). The typed pill is **anonymous** (no name), so it **unfolds** to its `{markov: …}` source in edit mode (`artifactToShorthand('markov', …)` for `typed && !name`), the dice/anonymous-grammar/typed-var model. **Naming stays a dialog feature** and a NAMED chain stays an **atomic** pill: a chain name is doc-wide callable config (`collectRules` registers it as `{kind:'markov'}` so `{name}` resolves it) that the unfolded text can't carry — the same CLAUDE.md "atomic in edit mode" rule that governs named grammars. Record shape is identical to the dialog's (`makeTypedMarkovRoll` reuses `makeMarkovRoll` → `{key, def, start, steps, path}` + `typed:true`), so it round-trips through `_markov`, prunes, and re-walks on click like any markov pill. Front doors (P2/P5-4): the `@` **Markov chain** menu desc + example now show the `{markov: …}` form, the **Markov chains** concept-guide entry gains the typed examples, and `guide/generating-text.md` leads with the inline form. The inventory row gains a form; no new family.

**Note: contested rolls reuse the pick-var declaration.** Recorded decision (#1243, 2026-08). A contest `{name := A vs B}` pits two dice expressions against each other and freezes the **margin**. It is a **content-sniffed sub-form of the existing `{name := expr}` pick-var declaration**, not a new top-level form. It mints no new sigil and does not teach math to roll dice: the only new lexical element is the `vs` operator inside a pick source. Each side is the **existing `parseDice` engine**, and `rollParsed` already collapses a side to one total (a pip sum OR a success count), so `rollVersus` reads the same rule at every level: `2d6 vs 2d6`, `2d6+str vs 2d6+def`, `4d6kh3 vs 2d8+2`, and a **success pool against a success pool** `6d10>=7 vs 5d10>=7` (margin = net successes). The contest freezes as a **pick var** (`rollPickRecord` stores `rolled` = the margin, plus a `versus` display record), so the margin reaches `evalMath` through the **existing var channel** the same way a captured die `{r := 1d20}` does: `{= name}` is the margin, so damage and degrees of success compute from it. `classifyBraceBody` is contest-aware so classify and promote stay in lockstep (a contest is valid iff **both** sides parse as dice), and a sum-vs-pool mismatch is a visible **dashed warn** (P4), never a silent wrong number. Front doors (P2/P5-4): the `@`/Variable menu desc + example show the form, the **`{` picker** carries a **contest** row, and the **Contested rolls** concept-guide entry teaches it. The declaration row gains a form; no new family.

**Note — the Guided tier types pills inline; the picker is a door, not an ambush.** Recorded decision
(2026-07-25): in Guided (the DEFAULT tier), typing `{` handed the point to the full-screen builder.
That panel takes focus, so the point left edit mode and every keystroke after the brace was eaten:
`{2d6}` left `"{"` and nothing else, on a blank point and mid-line alike. The app's own placeholder
says *"make a live pill with `{`"* and the welcome tour says *"put `{2d6}` in any point"* — both were
false in the tier the tour ships in. Four independent persona sessions hit it; Standard and Lean were
always fine, so it was invisible to anyone past onboarding.

**`{` now uses the same inline `#brace-menu` in every tier** (Lean keeps its own one-line tip). The
inline menu already does everything correctly: the caret stays in the point, it filters as you type,
Enter applies the highlighted form, arrows navigate. The full picker remains reachable — unchanged on
`@`, the toolbar and `Ctrl+K`, and newly as an explicit **Browse all pills** row offered on a bare
`{`, appended at the call site so `filterBraceForms` stays a straight map over `BRACE_FORMS`.

Two approaches were considered and rejected, both recorded because the reasoning is the useful part.
An **empty-point rule** (picker only when the point is otherwise blank) was rejected on measurement:
a bare `{2d6}` on a blank point is the single most common way to write a pill, and that is exactly
the case the rule preserves the picker for, so it fixed mid-line typing and left the common case
broken. A **non-focus-stealing picker** was rejected on P3: the builder's entire keyboard (Enter to
insert, arrow navigation, pane focus) is bound to elements inside the panel, so a picker that never
takes focus is mouse-only unless a keyboard bridge is also built from the point's keydown handler.
Trading a typing bug for an accessibility regression is not a trade this project makes.

Dismissing the picker now restores the caret (`restoreChromeReturn`, which `openBuilder` already
armed and nothing consumed) **instead of** `closeIo`'s plain focus, not in addition to it: `closeIo`
is synchronous and the restore defers to a frame, so running both lands the caret at 0 and corrects
it a frame later. The inventory is unchanged; no syntax was added or retired.

**Note — typed NAMED grammar rules reuse the Grammar-engine row.** Recorded decision (2026-07-24): `{rule Name: a | b}` — a **typed named grammar declaration** — is a new content-sniffed sub-form of the existing `{…}` brace grammar, the same class as `{seq Name: …}` and `{name := expr}`, not a new top-level delimiter. It closes the last typeability gap in the declaration cluster: a variable and a sequence could both be declared by typing, but a **named, re-rollable generator could only be made from the `@`/Grammar dialog**. Everything typeable was either anonymous (`{a | b}`, frozen to one pill, not callable elsewhere) or frozen (`{x := a | b}`, a pick var rolled once), so "declare a generator once and call it from anywhere" had no keyboard path at all.

**Why the `rule ` keyword is mandatory (the P1 driver).** A bare `name: a | b` cannot be the form, and the failure was not theoretical: `promoteBraceBody`'s alternation branch claims any body with a top-level `|`, so a typed `{loot: sword | shield 2}` promoted to an anonymous grammar whose two alternatives were the literal strings `loot: sword` and `shield` — a pill that displayed "loot: sword" half the time, with no error. Worse, `{tavern: The {adj} {noun}}` (pipes only inside nested braces) matched no branch at all, classified as prose, and had its inner braces shredded into orphan pills. The keyword makes the intent unambiguous before either path is reached, and it is the word the app already uses for the concept in its own failure copy.

Detection (`ruleDeclParts`) is the reserved `rule ` keyword + a name + a top-level `:`, then an ordinary rule right-hand side validated by **`parseRules` itself** (exactly as `seqDeclParts` defers to `parseSequence`), so the name charset — including **dotted sub-rule fields**, `{rule sword.damage: 1d8}` — and the alternation grammar have one owner. It is sniffed in the keyword-declaration cluster, well before the alternation branch. Single-line only: a multi-rule definition stays a dialog job, since it could not round-trip back through one brace.

The record is the dialog's own (`makeGrammarRoll` → `{key, def, origin, result}`), differing by a single field: `typed: true` **instead of** `anon: true`. That one flag is the entire mechanism — `collectRules` and `collectCallables` each skip `anon` records to keep a synthetic `origin` out of the document namespace (UXP-33), and a *declared* name is precisely what must not be skipped. Because the name lives in the text, the pill **unfolds** back to `{rule Name: …}` for editing (the typed-var argument, O1); a dialog-declared grammar carries no `typed` flag and stays atomic, unchanged.

**Keyword-commit has two triggers, and which one a keyword gets is a measurement, not a preference (#1501).** `rule`, `seq` and `prop` commit on a **space**, because those words rarely open ordinary prose inside braces. The eleven generate keywords (`shuffle`, `cycle`, `once`, `stopping`, `deck`, `oracle`, `roll`, `markov`, `query`, `count`, `meter`) and the repeat count (`3x`, `2d4x`) cannot: they are ordinary English words, and a space rule condemns `{once upon a time}`, `{deck the halls}`, `{stopping by woods on a snowy evening}` and `{roll a d20 and add your bonus}` — all measured. A pipe rule is no better, because `{once | twice}` and `{count me in | count me out}` are legitimate alternations. So they commit on a **nested brace**: `{3x {a|b}}` is an attempt, `{3x the fun}` is prose. The trigger is the thing that can actually be damaged — only a nested brace gets promoted out from under the outer form — and prose keeps every shape it has today.

**A cue names the form the engine accepts, not a generic one.** The keyword's own form rides in `GENERATE_FORM_HINT`, and a test asserts every keyword has an entry *and* that each entry's body classifies as a real artifact. An oracle takes a band and a roll takes a query, so a shared `a | b | c` example would send the reader one wrong step further; the test makes teaching an unparseable form impossible.

Classification is **keyword-commit** (the `{oracle:}`/`{meter:}` shape): once a body opens with `rule `, a tail that will not parse is `'invalid'`, never prose. That verdict earns it the `.brace-attempt` cue and engages `promoteInlineShorthand`'s anti-shred guard, so a broken rule fails visibly and whole instead of being torn into orphan pills (P4). Front doors (P2/P5-4): the `{` body-completion picker row (**rule**), the `@` **Grammar** and **Roll table** card copy + examples, and the **Building generators** concept-guide entry. The inventory's Grammar-engine row gains the `{rule Name: …}` form; no new family.

**Note — sequences (TODO state sets): applied via existing rows; declarable via `@` OR a typed `{seq …}` form.** User-definable state sets (sequences) are *applied* with zero new syntax: via `/` (writes `#KEYWORD` into the status-headline position), and the `#keyword`-in-text IS the existing hashtag sigil reused. The `#` prevents bare capitalized words from accidentally becoming badges; `#word` that is not a known state is a normal clickable hashtag. *Declaring* a sequence has two front doors: the `@` Sequence dialog (an artifact-token `[[seq:key]]` pill, like `@var`) **and**, added 2026-06-30, a typed **`{seq Name: active | done}`** form — a content-sniffed `{…}` sub-form (the Tier-3 typeability addition, alongside the typed markov). Detection (`seqDeclParts`) is the reserved `seq ` keyword + a single identifier name before a top-level `:`, then the dialog's exact `parseSequence` states tail, so it never collides with a deck (`shuffle:`…), a markov (`markov:`), a conditional (a comparison before the `:`), or a `{name: rhs}` rule line. Unlike the typed markov, a sequence is **always named** (the name is how its states are applied doc-wide), so there is no anonymous form and the pill is **atomic** — it does NOT unfold (the name is doc-wide config the unfolded text can't carry, the same rule as named markov/grammar); editing is via the dialog. The record shape (`{key, name, states, doneFrom}`) and `collectSequences` registration are identical to the dialog's. Front doors (P2/P5-4): the `@` Sequence menu desc/example, the **Custom workflows** concept-guide entry, and `guide/tasks-and-organizing.md` now show the `{seq …}` form. The inventory's Grammar-engine row gains the `{seq Name: …}` form; no new family. *(Distinct from the generative "stateful sequences / decks" note above — that's the `{shuffle|cycle|once|stopping: …}` deck form; this is the to-do state-set feature. Different concepts, both legitimately named "sequence" in their own domain.)*

**Note — subtree aggregation adds NO new syntax (new math *functions*).** Recorded decision (2026-06-14): `{= sum(cost)}` / `{= avg(score)}` / `{= count(cost)}` — rolling up a **property** over a point's **direct children** — are three new **functions inside the existing `{= …}` math form**, which is exactly the P5-preferred way to extend math ("a new name inside the existing grammar," per the CLAUDE.md evalMath note). No new delimiter, sigil, or form: `sum`/`avg`/`count` are not existing evalMath names, so there is no clash, and the argument is an ordinary identifier (a property key). They are implemented as a pre-substitution to a number before evalMath (`expandAggExpr`, the proven `#+TBLFM:` translation model), keeping evalMath's number-only contract. Front doors (P2/P5-4): the `sum(cost)` hint in the math dialog + the **Pills & shorthand** row in the `?` panel. Render-time + live (the `cookieNode` render global + full `render()` on edits), no sidecar — the `{= …}` recipe is the source of truth, the same edit-raw/render-pretty model as `#+TBLFM:` and the `[/]` cookie. **Extended (2026-06-14): `min`/`max` over children added** — `{= min(cost)}` / `{= max(cost)}`, completing the aggregation family. This is the **same recorded math-function extension**, not a new syntax family: it is **purely additive / zero-regression** because evalMath's numeric `min`/`max` already require **≥2 args** (single-arg `min(ident)` was already an error there), and the aggregation regex matches only a *single bare identifier* — so a comma'd numeric `min(a, b)`/`max(1, 2)` is untouched (the spreadsheet `MIN(col)` vs `MIN(a,b)` split; `min`/`max` are already the app's aggregate vocabulary in the base "Calculate" menu). Empty set → the **identity element** (`min(∅)`=`+∞`, `max(∅)`=`-∞`), so an F2 range/extremal `check` is vacuously true on a point with no qualifying child rather than spuriously false. Numeric **and date-shaped** child props aggregate (`childPropNumber` tries `Number` first, then `parseDueDate` → epoch-days), so a date-property extremal (`max(due)`/`min(start)`) computes; a non-date string still → `null`. **Extended (2026-06-19): word count `words(subtree|self|children)`** — a prose word-count roll-up, the *same* recorded math-function extension (a new function inside `{= …}`, pre-substituted by `expandAggExpr` so it resolves in pills, the math + check dialogs, and export alike; no new delimiter). It differs only in taking an **explicit scope keyword** from a closed set — `subtree` (self + all descendants — so, unlike the property rollups, it recurses), `self`, `children` — instead of a property key; that keyword is an argument value, not a sigil. **Amended (2026-07-17, #827 owner decision):** per-point notes are **excluded** from `words()` by default; the optional second closed-keyword argument `words(scope, notes)` opts them back in — the `sum(prop, scope)` two-arg shape, an argument value again, no new delimiter, and it composes with every scope including numeric depths (`words(2, notes)`), which a parallel keyword set could not spell. Reading time is composition (`{= words(subtree) / 200}`), so there is no separate `readtime`. Front door: the `words(subtree)` example + body line in the **Roll-up totals** guide entry. The inventory entry grows; no new family.

**Note — item-weight expressions (A5) + the yes/no oracle add NO new syntax.** Recorded decision (2026-06-14): the weight in a weighted alternation (`{a | b 2 | c}`) may now be a `{= expr}` instead of a literal number (`{a | b {= str}}`) — **dynamic odds**, resolved against the doc variables at pick time (`pickWeightedAlt(alts, vars)`). This is **not a new form**: it extends the *existing* trailing-weight slot of the weighted-alternation row by letting it hold the *existing* `{= …}` math form (the same "reuse the brace, no new sigil" move). A weight is read only when a non-empty template precedes it (so a bare `{= 2d6}` alt stays content); an unresolved expr → neutral weight 1 (the alt is not dropped); a numeric `≤ 0` → 0, so `{= cond ? 1 : 0}` can conditionally disable an alt. The trailing-`{= }`-is-a-weight rule is the same accepted-ambiguity class as the trailing-number weight it extends, and the dialog/`}`-close **live preview** shows the outcome (P4, not silent). *(Qualified 2026-07-30, #1354: that claim held for the weighted alternation this note was written for, and NOT for a body promoted as `origin: {BODY}` — a conditional or repeat, whose own closing `}` ends the string. `parseAlt`'s weight capture spanned it and truncated the template, so `{cost > budget: OVER by {= cost - budget} | under}` rendered the fragment `{cost > budget: OVER by`, silently. The weight expression is now fenced to `[^{}]`: a weight is a math expression and `evalMath` is number-only, so a legitimate one never contains a brace, and ` {=` inside a branch now means "a computed value here" rather than "a weight". `templateAttempt` mirrors the fence; the pair is pinned. See UXP-287.)* **The yes/no oracle** is purely a *front door*, not syntax: the `@` **"Oracle (yes/no)"** door opens a likelihood picker (Certain / Likely / Even / Unlikely / Impossible — **original, neutral** ratios; the IP fence forbids copying a published oracle's tables) that builds an anonymous `Yes N | No M` weighted-alternation pill — the most-reused solo-gen primitive, shipped as a recipe over the weighted-alt syntax. Because the odds field IS the weighted-alt body, the odds can be A5 `{= expr}` weights (state-modulated odds). Front doors (P2/P5-4): the `@` Oracle door + the `{a|b {= w}}` form on the **Pills & shorthand** `?`-panel row. *(Amended #543/2026-07: a **typed** `{oracle: band}` twin later shipped — a content-sniffed `{…}` sub-form promoting to the same `Yes N | No M` weighted-alt pill. It IS now a recorded form on the Grammar-engine row above; this note's original "the inventory does not grow" applied to the dialog-only door, before the typed form existed.)* *(Amended #1499/2026-08: the band now RIDES THE RECORD, from both doors, so `{oracle: likely}` round-trips its own words like every other generator and the pill's label names it ("Oracle (likely)") instead of reading "Grammar: Yes". The promotion had been recorded here as a deliberate constructor — "the band name is the value, not a stored syntax" — and it was, but the consequence was not recorded: the word `oracle` did not survive, so `{oracle: certain + swing}` came back as a six-way body no author can re-band by eye. The band is STORED rather than derived from the odds, because the odds are also a legal hand-typed source: rebuilding `{oracle: …}` from them would rewrite an authored `{Yes 3 | No 1}`, the same silent rewrite pointed the other way. It is verified against the odds on every read, so editing them in Edit grammar returns the pill to plain odds rather than leaving a band it no longer rolls.)*

**Note — outline constraints / lint (F2) add NO new syntax (a reserved property + an `is:` value).** Recorded decision (2026-06-14): a point may carry a reserved **`check` property** whose value is an `evalMath` boolean assertion over the point and its direct children (`sum(cost) <= budget`, `sum(weight) == 100`, `count(score) >= 3`, own-prop `hours <= 8`). This is **zero new authoring syntax** on the dates precedent exactly: `check` is a reserved property key (like `start`/`due`), the value is the **existing** `evalMath` expression language, and the rollups are the **existing** B1 child aggregations (`sum`/`avg`/`count(prop)` via `expandAggExpr`). Evaluation overlays the point's own numeric props on `globalVarMap` (own props win; evalMath's `today`/constants still win over both) → `pass` (1) / `fail` (0) / `error` (null). The point shows a live pass/fail/error chip (P4 — fail **and** error are visible, pass is a muted `✓`, never silent), and **`is:failing`** — a new value in the **established `is:` operator family** (`is:done`/`is:todo`/`is:note`), not a new operator family — is the doc-wide lint filter (matches a `fail` **or** `error` check). The chip routes through `openPropChip` like the date chips; the value is the `evalMath`/B1 escape-hatch (an invalid assertion renders as the error chip, never silently passing). Persistence is free (`check` is an ordinary `_props` property). The only naming decision is the reserved word — **`check`**. Front doors (P2): the `/check` slash verb (labelled **"Check"**), the bullet-menu "Add / edit check" door, the chip itself, and the `?` panel + focus-shown search-legend rows; the dialog teaches the scope/examples with a live preview that explains *why* an expression can't evaluate. The inventory gains one reserved property key and one `is:` value; no new family. Numeric extremal/range checks (`max(cost) <= cap`, `min(score) >= 1`) work via the B1 `min`/`max` aggregation (shipped 2026-06-14); **date-property** extremals (`max(due) <= deadline`, `min(start) >= kickoff`) **now compute too** (`childPropNumber` aggregates date-shaped props as epoch-days, 2026-06-14). **Deferred (follow-ons):** multiple checks per point (one per point in v1; `evalMath` has no `&&`); upward/cross-parent references; structural/existence checks (that is F5, enforced tree grammars).

**Note — properties-as-variables + ancestor inheritance (#460 / #461) add NO new syntax (a resolution-model change).** Recorded decision (#460 shipped own-node scope; #461, 2026-07-10, the ancestor half): a point's own numeric **properties** are visible to its `{= }` math pills and `check` by key (`STR: 14` → `{= STR + 2}` reads 16), and a pill/check also inherits numeric properties from its **ancestor** chain (a scene under a `STR: 14` character sees `STR`). This is **zero new authoring syntax**: properties already exist (the `key: value` system), variables already resolve by name in `evalMath`, and inheritance is purely a change to *which* `{ name: number }` scope the existing engine is handed. Precedence, high → low: **own props → nearest ancestor → … → farthest ancestor → doc variables** (evalMath's own constants like `today` still win at eval time). The ONE place the two node-scoped evaluators compute this is the pure `resolveNodeScope(node, ancestors, docVars)` (own props win last), fed the chain by `ancestorsOf(node)` at the two DOM-render sites (the math pill and the check chip) — so they can't drift (the parity risk the issue flagged). **Promotion parity:** the `{= }` shorthand validates against the same inherited scope at creation (`makeMathResult(expr, scope)` via `promoteBraceBody`), so a pill that only resolves through an own/ancestor prop actually promotes instead of staying raw text. **Scope boundary (v1):** a query base's `= expr` cell and the `is:passing`/`is:failing` search filter stay own-node — their rows come from a SEARCH, not a subtree, so inheriting the matched point's outline-ancestor props would be a surprising mental model, not the character-sheet one. The inventory does not grow; no new sigil, delimiter, or form.

**Note — point timestamps (#467) add reserved `created`/`edited` properties + an `is:` value; NO new authoring syntax.** Recorded decision (2026-07-10): a point carries `created` (set once, on first content) and `edited` (re-stamped whenever its OWN text/note changes) as reserved properties holding a local ISO datetime to the minute. This is **zero new authoring syntax** — they are the same reserved-property model as `check`/dates (hidden from the generic Properties editor, round-trip via `_props`), and the only new search vocabulary is **`is:recently-edited`**, a value in the **established `is:` family** (last 48h; `duplicate-title` precedent), not a new operator. **The important precedent it sets:** these are the **first app-MAINTAINED (non-authored) props** — the app writes them, the user cannot (a typed `{prop created: …}` is refused, so a timestamp can't be forged). That dents the "`node.text` is the sole truth" stance slightly, and the boundary is deliberate: timestamps are *metadata about* the point, never content, so they live in the sidecar `props` (already non-text) and are stamped at ONE chokepoint (the `exitEdit` text-commit + `commitNote`), gated on the text/note actually changing — **structural moves, child edits, and app-driven prop writes (e.g. a recurring-task roll-forward) do NOT stamp** (`edited` means "I changed what this point says"). **Back-fill is `unknown`, never epoch 0:** a point with no stamp is absent from `is:recently-edited` and (a future sort) sorts last — `parseStamp` returns `null`, callers branch on null. Granularity is minute-precision (enables real recency). Front doors (P2): a muted "Created … · Edited …" line in the **zoom detail view** (not an always-on chip — that would clutter every edited point), the `is:recently-edited` legend row + `?`-panel entry. **Deferred:** a general sort-by-recent UI (there is no cross-surface result-sort seam yet; `is:recently-edited` is the recency capability this ships). The inventory gains two reserved property keys + one `is:` value; no new sigil, delimiter, or form.

**Note — recurring tasks (#462) add a reserved `repeat` property with a small, signed-off phrase vocabulary.** Recorded decision (2026-07-10, an explicit P5 sign-off): a dated to-do may carry a reserved **`repeat` property** whose value is a tiny recurrence phrase, and completing the task rolls its `due`/`start` forward to the next occurrence and re-opens it (so the one point recurs, no piling-up copies). This rides the **dates precedent** — `repeat` is a reserved property key like `start`/`due`/`check` (hidden from the generic Properties editor, merged back on save, round-trips through `_props`, no new sidecar or token) — but unlike F2's `check` (which reused the *existing* `evalMath` language), the repeat **value is a small new authoring vocabulary**, so it is a deliberate, recorded inventory growth, not a side effect. The vocabulary is a **closed set, owner-signed-off, kept intentionally tiny** (`parseRepeat`, a pure `null`-on-miss core pinned like `parseDueDate`): intervals `every [N] day|week|month|year` (+ the bare aliases `daily`/`weekly`/`monthly`/`yearly`), a weekday list `every <weekday>[,<weekday>…]`, and a month-day `every month on the <Nth>` / `monthly on the <Nth>`. Ordinals, "last day of month", "2nd Tuesday", and "every weekday" are **deliberately deferred** (the issue's scope-creep warning) — a bigger grammar needs its own sign-off. The roll-forward is **anchored off the old due date** (fixed cadence — a late completion never drifts the schedule), preserves the start→due span (both dates shift by the same delta), and snaps forward past today if the task was very overdue (never reschedules into the past). It is **visible + announced, never a silent render-time flip** (P1/P4): completion flashes "⟳ Rescheduled to <date>." through `flashHint`, which reaches `#a11y-live` (P3). Front doors (P2): the **Repeat field in the Schedule dialog** (`/due`), with a live preview that describes what `parseRepeat` understood (so the tiny grammar teaches itself) and flags an unrecognized phrase (P4); a `⟳`-marked `repeat` chip on the point that routes back to the Schedule dialog; the **Dates & agenda** concept-guide entry + `guide/dates-and-planning.md`. The inventory gains one reserved property key and its bounded phrase vocabulary; no new sigil, delimiter, or `{…}` form.

**Note — uncertainty fields (B2) are the recorded exception that adds a new sub-language.** Recorded decision (2026-06-14, an explicit P5 sign-off): the **estimate** artifact introduces a small uncertain-expression mini-language — `lo to hi` (a 90% confidence interval → lognormal), `normal(m,s)`, `uniform(lo,hi)`, scalars, `+ − × ÷`, and `sum(prop)`/`avg(prop)` over children — and the **`to` operator is genuinely new syntax**. This is the **rare, justified exception** to the closed inventory: the core invariant is that **`evalMath` always returns a number**, but a distribution **is a sample array, not a number**, so it *cannot* ride the math engine — it needs its own form and its own Monte-Carlo sampler (`sampleUncertain`, seeded via `rngFromSeed`; the cores are pure + Node-testable). Choosing B2 **is** the sign-off; the inventory grows by one row (the uncertain-value family). It is bounded to stay minimal: the value families are a **closed set** (`to`, `normal`, `uniform`, plus the `sum`/`avg` rollups — beta/mixtures/correlation are deferred); the engines stay **separate in v1** (an estimate used inside a `{= …}` math expression fails visibly, like any non-number — no cross-engine bridge); storage is tiny + reproducible (`{key, expr, seed}` — re-sampling from the seed reproduces the exact estimate, so a click just re-seeds and a shared C1 snapshot reproduces it). Detection: the `@estimate` dialog accepts the full language; the typed `{…}` shorthand promotes the **constructors** (`to`/`normal`/`uniform`) plus any expression referencing a declared distribution (#952's `usesDistVar`, so `{cost * 2}` promotes while a bare `{cost}` stays a variable reference — the operator is what promotes it) (`estParts`), so a bare `{sum(cost)}` never silently diverges from the `{= sum(cost)}` deterministic-math form (rollups are dialog-authored). Display is the headline **mean ± [p5,p95] + a pure-string unicode sparkline** (`formatDist`/`sparkline` — export-safe, the same string in the pill and the flattened markdown). The pill freezes + **re-samples on click** like dice (P1, a generative pill changes on click), the pencil edits, and it **unfolds** to its `{expr}` source for inline editing (the dice/anonymous-grammar model). P3: the pill is keyboard-operable (Enter/Space re-samples), the `aria-label` is the mean±CI text and the sparkline is decorative (`aria-hidden`); P4: a malformed expression shows an `#ERR` chip, never a blank. Front doors (P2/P5-4): the `@` **"Estimate / uncertain"** door, the `{lo to hi}` shorthand, and the **Pills & shorthand** `?`-panel row. The inventory gains one family — the only such growth besides A1.

**Note — `{Nx: template}` repeat is a recorded inventory sub-form addition.** Recorded decision (2026-06-14): `{3x: {beast}}` emits a template N times (1–99), re-expanded independently each time, joined by spaces — "ogre wyrm ogre". This is a **new content-sniffed sub-form** of the existing `{…}` brace grammar, the same shape as `{cond:}` and `{mode:}` sequences. It mints no new sigil: the template is a grammar template (may roll dice / call rules), and the only new lexical element is a digit-run + `x/X` before the `:` — content-sniffed like the brace's other forms. Detection (`repeatParts`) is syntactic: `/^\d+[xX]:/` — disjoint from `condParts` (needs a comparison), `seqParts` (needs a reserved mode keyword), and `modParts` (not a bare identifier + dot). Literal N in v1; a **DICE count shipped via the #545 sign-off** (`{2d4x: goblin}` — the head must parse as pure dice, rolled fresh per expansion, clamped 0–99, a 0 roll emits nothing). A **variable** count stays out (a var-modified head fails the pure-dice parse and stays literal). Promotes to an **anonymous grammar pill** (`origin: {Nx: template}`) so unfold/refold/prune/export reuse the grammar machinery. Front doors (P2/P5-4): the `{Nx}` chip in the grammar dialog + the `{3x: {beast}}` row in the **Pills & shorthand** `?` panel. The inventory gains one sub-form on the Grammar-engine row; no new family.

**Note — hierarchical / property items (A6) extend the `.suffix` form + the Grammar-rules row (no new delimiter).** Recorded decision (2026-06-14): a grammar item may carry named **fields** via **dotted sub-rules** (`sword.damage: 1d8`), read with `{item.field}` (`{weapon.damage}` picks a weapon then reads its damage). This reuses the **`.` suffix form already recorded by A1** — the suffix now resolves to a **sub-rule field** as well as a modifier, disambiguated by the modifier set (`fieldParts` matches a 2-segment ref whose suffix is **not** a known modifier, and it is checked **after** `modParts` so a modifier always wins the overlap — "don't name a field after a modifier"). Two extensions of existing rows, **not a new family**: the **Grammar-rules row** gains dotted rule names (`key.field: …`), and the modifier/`.suffix` note gains the field meaning. The hard part — **cross-reference consistency** (so `{item.name}` and `{item.damage}` are the SAME item) — is solved by riding the **locked random-pick variable** (declare `w = {weapon}`, then `{w.name}`/`{w.damage}` reference the one frozen item), **NOT** by a per-expansion bind: the reverted per-expansion `ctx.binds` **scope** (PR #51, `generation-direction.md` §2) **must not return** (note this is the scope model, not the `:=` operator, which later shipped as the persistent `{name := expr}` declaration). Standalone `{rule.field}` works one-shot (pick + read, independently). Resolution: a directly-named sub-rule (`{sword.damage}`) → that rule; else resolve the base to an item key then read `key.field`; else a `{base.field?}` marker (P4, visible). v1 = a single field; **field-then-modifier chaining (`{w.damage.cap}`) SHIPPED via the #545 sign-off** (`fieldModParts`: ≥3 segments, a non-modifier field, an all-modifier tail — resolves like the plain read then folds `applyMods`; a field named after a modifier stays shadowed). Multi-level nesting (`{planet.country.town}`) remains deferred. Promotes to an anonymous grammar pill (`origin: {item.field}`) — the A1 path. Front doors (P2/P5-4): the grammar-dialog hint (define `key.field:` sub-rules; the pick-var consistency recipe) + the `{weapon.damage}` row in the `?` panel. No new `@` door (it's grammar authoring).

**Note — text modifiers (A1) are a recorded inventory ADDITION** (one of two — the other is B2's uncertain-value family, above). Recorded decision (2026-06-14, the explicit P5 sign-off this feature carries): a `.mod` suffix on a **rule or variable reference** shapes its output — `{beast.a}` → "an ogre", `{noun.s}` → "foxes", `{name.cap}` → "Name", chainable `{beast.a.cap}` → "A dragon". Unlike every other recent generative change, this **does add a genuinely new lexical element** — the `.mod` suffix inside `{…}` — so it is an explicit, recorded syntax-inventory growth (the `{ref.mod}` form on the Grammar-engine row), not a side effect. It is bounded to stay minimal: a **closed set of canonical tokens, one per function, NO aliases** (`cap` · `title` · `upper` · `lower` · `a` · `s`, plus the additive follow-ons `ed` (regular past tense), `ord` (ordinal), and — via the #545 sign-off — `ing` (present participle) and `poss` (possessive) — adding a modifier *name* is additive within the existing `.mod` form, NOT a new inventory decision); it applies to **bare references only** (a rule or variable — never alternation/dice/math directly; for a modified random pick, name a rule then `{thatRule.cap}`); and the base resolution + result are the **existing** rule/var machinery — only the suffix is new. Detection (`modParts`) is syntactic: a base identifier followed by `.`-separated suffixes, **every** suffix a member of the closed set (so `{file.txt}` → literal, `{cap}` → a bare name). The base may also be a **literal integer** (`{3.ord}` → "3rd") — a small additive extension *within* the existing `{ref.mod}` form (a base that resolves to itself, no rule/var lookup; `resolveBrace`/`classifyBraceBody`/`promoteBraceBody` branch on `/^\d+$/`), **not** a new inventory row; `2d6` still matches neither base arm, so `{2d6.cap}` stays a dice body. A modified reference routes through the **grammar** pill (promoted `origin: {ref.mod}`) for rule, var, AND literal-integer bases — never the var-pill path — so unfold/refold/prune/export reuse the grammar machinery unchanged. Front doors (P2/P5-4): the `{.cap}` chip + modifier hint in the grammar dialog, and the `{name.cap}`/`{noun.s}` row in the `?` panel. The `a/an` heuristic is vowel-**letter** based and plurals are regular-only (documented limitations — see `guidance/features.md`).


**Note — variable bases project dotted variable NAMES (recorded P5 addition on the A1/A6 `.` form).** Recorded decision (2026-07-16, owner-approved plan): an authored base marked as a **variable base** (`node.varbase = {name?}`; never a query base) projects each data row as **dotted document variables** — row "Orc" + column "HP" → `orc.hp`, readable in grammar (`{Orc.HP}`) and math (`{= Orc.HP + 5}`), chaining through the existing lazy resolver like any formula variable. Column 0 projects under its own header (`orc.name` = "Orc", the display string — so `{Orc.Name}` renders the name and `{Orc.Name.s}` pluralizes); an optional base name prefixes every projection (`monsters.orc.hp`), and the prefix REPLACES the bare form. **What grew:** dots inside variable *names* (a namespace, not a new sigil — the `.` form is the one A1/A6 already recorded), the ≥3-segment brace read `{base.row.field[.mod…]}` (resolved longest-dotted-name-first with an all-modifier tail, unresolvable bodies stay literal — the escape hatch preserved), and dotted identifiers in `{= }` (evalMath's ident reader consumes `.` only before an identifier char, so decimals are untouched). **Precedence ladder (P1):** exact dotted RULE > exact dotted VARIABLE > the A6 base-then-`<value>.field` indirection > marker; modifiers keep winning the 2-segment overlap (a column named `cap`/`a`/`s`… is unreachable in grammar text — read it in math, or rename; the PR-B dialog warns). **Cell classification** is deliberately NOT the typed-declaration rule (`varDeclIsPick` reads a single bare word as an alias formula — wrong for data like `Type = undead`): a cell starting with `=` is a formula (chains allowed), a bare number is a number, anything else is frozen text verbatim — so a `2d6` cell is TEXT in v1 (re-rollable pick cells are a recorded follow-up needing a per-base rolls store; there is no per-cell record to freeze a roll on today). Bare row names (`{Orc}` alone) deliberately do NOT project — bare identifiers share the grammar-rule namespace and rules win, so a row silently shadowed by a same-named rule would be a P1 trap; adding it later is compatible, removing it later would break documents. Projection is positional (the two gather hooks ride `collectVars`/`collectNodeDecls`, so a variable base is a declaration site like any other — nearest-above wins); the projected defs carry no `[[var:]]` pill, so the shadow marker cannot mark them (cosmetic, accepted). Deferred, recorded: pick/dice cells, `sum(dotted)` rollups (`expandAggExpr` stays single-bare-identifier), the `var:` search operator over projections, per-prefix var-panel grouping. Front doors ship in PR B (the base-menu door, the `$` badge, the dialog with its addressable-names preview, the concept-guide entry + `guide/`). The inventory's Grammar-engine row gains the dotted read forms; no new sigil or delimiter family. **Extended (2026-07-16, the follow-up round): three of those deferrals closed, zero new syntax.** (1) **Pick/dice cells resolved via pill-in-cell** — no rolls store: a cell that IS a single dice/grammar pill projects its frozen `total`/`result` through the pick channel (`{2d6}` typed in a cell promotes to the pill; click it to re-roll; a re-roll/edit on a projecting base triggers a full `render()` — the `rerollPickVar` idiom — so every referencing point repaints; the base bullet menu's Pills section is the keyboard re-roll door). Plain-text `2d6` stays text, unchanged. (2) **Column totals** — `{= sum(base.col)}` (also `avg`/`count`/`min`/`max`): a 2-segment dotted prop in the existing rollup forms aggregates the numeric values of every row's `col` across a NAMED variable base, resolved from the vars map (`aggregateVarBaseColumn`); bare props keep the child-prop meaning byte-for-byte, an unmatched/3-segment/scoped dotted call stays literal → visible `#ERR`. Recorded under the same math-function-extension family as the subtree-aggregation note. (3) **Var-panel grouping** — each projecting base's names collapse under one header (default collapsed, session-only state, `aria-expanded`). Still deferred: the `var:` search operator over projections.

**Note — the Markdown row is the full CommonMark/GFM surface the renderer ships (UXP-20 audit, 2026-06-16).** A P5-4 sweep of the authoring entry points (inline `mdInline`, block `mdToHtml`, the `/` + `@` menus, the `{…}` grammar forms) found the inventory listed the inline *formatting* sigils but omitted the rest of the Markdown family the parser already renders: inline **links** `[text](url)`, **images** `![alt](url)`, bare-URL **autolinks**, **GFM pipe tables** (a header row + a matching `---` delimiter row — rendered **static / read-only** anywhere a point's text contains one, UXP-22; an optional trailing `#+TBLFM:` recipe rides the separate Table-formula row), and **definition lists** (`term` then `: def`). These are **standard markdown, not app-specific syntax** — no sign-off was needed; the audit's job was to make the closed set *match what ships*. The `?` panel's **Format (markdown)** section was completed in the same pass (P5-4 needs both homes; it also gained the `++` underline the inventory already listed). No new authoring language — the inventory now documents reality.

**Note — declarable units add NO new syntax (a new math *function* + a declarative data store, SR-6/#875).** Recorded decision (2026-07-17): `{= convert(x, from, to)}` reads unit names against a built-in ratio table (length/mass/volume/time) plus a doc-declared `root.units` (File → Custom units, `parseUnitDecls`). It is the **same math-function extension** as the subtree-aggregation note — `convert` is a new name inside the existing `{= …}` form, substituted to a number in the `expandAggExpr` pre-pass (`replaceConvert`) so evalMath stays number-only. **The rejected form is a unit-suffix literal (`3mi`)** — that WOULD be a new lexical element at the floor (P5), so it is deliberately out; the function form is the in-bounds generalization. The declared units are pure data (the calendars/sequences substrate pattern), round-tripping the `<_units>` head element. Front door (P2): File → Custom units + the `custom-units` concept-guide entry. The inventory's math-function set grows; no new family.

**Note — query reducers add NO new syntax (the `count("query")` generalization, SR-8/#877).** Recorded decision (2026-07-17): `{= sum|avg|min|max("query", prop)}` reduces a property over a live query set — the whole reducer family over the **quoted-query convention that `count("query")` already established**. No new lexical element: the quoted first arg is the same disambiguator the count overload used, and `queryReduce`/`reduceAgg` substitute to a number via the same `expandAggExpr` pre-pass. Works in checks for free (`sum("#task", cost) <= budget`). Front door (P2): the `rollups`/`check` concept-guide entries gain the reducer examples. The inventory's math-function set grows; no new family.

**Note — estimates read declared variables (a resolution-model change, SR-7/#876).** Recorded decision (2026-07-17): the B2 uncertain-value sampler now threads the doc `vars` map (`parseUncertain(expr, vars)`/`sampleUncertain(…, vars)`), so a bound may be a declared variable — `{cost_low to cost_high}`, `{units * (5 to 10)}`. This is **zero new authoring syntax**: no new operator or delimiter, the record stays `{key, expr, seed}`, and it is purely a change to which names resolve inside the *existing* uncertain-expression language (the estimate sub-language row is unchanged). A bare word resolves only when the vars map holds it (else it fails → literal, the promotion gate), and a text variable fails visibly — the same type-safety contract math variables follow. The inventory does not grow; no new sigil, delimiter, or form.

**Acceptance test:** the change added **zero** new top-level syntaxes — or, if it added one, it retired/subsumed an overlapping one and updated both the inventory and the `?` panel.

---

## 3. Keyboard grammar (authoritative)

The standard the corpus is missing entirely. Every block type conforms; there are no per-block exceptions. New keyboard work edits this table first.

| Input | Reserved meaning | Notes |
|---|---|---|
| Plain keys / arrows | Act **within** text (type, move caret, select) | MUST NOT be hijacked for structure while editing |
| `Enter` | New point | **All** blocks — *Paragraph is the sanctioned exception — see P1-1* |
| `Shift + Enter` | Line break in the point | **All** blocks — *Paragraph is the sanctioned exception — see P1-1* |
| `Backspace` (point start) | Merge the point up into the previous visible point | Collapsed caret at offset 0: appends this point's marker-stripped text to the row above, carries its children + sidecars, lands the caret at the join (UXP-68 — the inverse of the Enter-split). An empty childless point deletes instead (editing flow). Declines at the first point / into a base or code block |
| `Tab` / `Shift+Tab` | Indent / outdent | Points **and** table cells |
| `↑/↓` (non-editing) | Move the row cursor between points | Fires in the global handler when `activeContentId == null` and focus is not in an input — moves `selFocusId` one step via `flatRowStep`, clears any selection, scrolls the row into view, shows a `.node-cursor` highlight. Prerequisite to Shift+Arrow. Every move is **announced** with its position ("Two, 2 of 3") — there is no focused element for AT to read, so the cursor has to speak for itself (#1490) |
| `Enter` (non-editing, row cursor) | Enter the point under the row cursor, caret at the end | The way back **in** from the blur rung, and the counterpart to `↑/↓` above: without it the row cursor was a read-only state you could move and never act on, and the only key that returned you to the document was `Tab`, which lands on a *different* point. Gated on `<body>` holding focus **and on the key not already having been handled** (`!e.defaultPrevented`). `<body>` alone is **not** the stranded state, which #1496 measured: an in-content control that repaints on activate (a pill, a clock, a task checkbox) destroys the focused element and puts focus on `<body>` *mid-dispatch*, so one Enter both activated the control and opened the point behind it, and the next split that point. An `Enter` another handler has acted on is not an `Enter` into a stranded outline. A dialog, menu or field keeps its own `Enter` by holding focus (#1490, #1496) |
| `Shift + ↑/↓` (non-editing) | Extend / contract the multi-point selection | Fixes `selAnchorId` at the cursor position (inherited from the last-edited point), moves `selFocusId` one step, calls `rangeSelectTo` — identical model to Shift+click (UXP-57). `Esc` clears |
| `Alt + ↑/↓` | Move the point (reorder) | The movement modifier — reserved for movement |
| `Ctrl/⌘ + . / ,` | Collapse / expand | Replaces the `Ctrl+↑/↓` binding (which collides with caret-to-edges) |
| `Ctrl/⌘ + Enter` | Zoom into point | *Sanctioned carve-out (UXP-67): in a **base** (`role=grid`) the chord **commits the cell/header edit** instead — the grid-cell convention, cf. Table `Enter`. A normal point zooms.* |
| `Shift + F10` / `ContextMenu` (Menu key) | Context menu of the **focused element** — a focused **point/bullet** → that point's bullet popup (`#bpop`); a focused **base cell** → the **Column menu** for that cell's column | The OS-standard "context menu of the focused element". Opens the menu and moves focus into it: `↑↓`/`Home`/`End` navigate, `Enter`/`Space` activate, `Esc` closes. The bullet popup carries every per-point action (type switch, zoom, copy link, move, delete, convert-to-base — and, on a base, the whole-base ops); the Column menu carries every per-column op. So it is the single keyboard door to both, with no dedicated chord (the colliding `⌘+M`/`⌘+Shift+M` were dropped — see below) |
| `F6` / `Shift + F6` | Move focus into the open **docked panel** — Footnotes, Linked from, Variables — and step between them; a step past the last one goes back to the point | The OS/browser-standard "cycle panes" key, chosen on the same argument that chose `F1` below: the outline is a text field, so every printable key must keep typing, and `Tab` already means indent — a function key is collision-free by construction. The ring is `outline → each open panel in page order → outline`, so the key is its own way home; `Esc` from a panel is the other, restoring the caret exactly where it stood (see the `Esc` row). With no docked panel open the key is **not** claimed, so the browser's own `F6` still works. Arriving announces the panel by the same name its region carries. Fixes #1465 C2: these three panels are the ones that appear *without* taking focus, so before this nothing could ever move focus into them and reaching for them with `Tab` silently re-indented the document instead |
| `Esc` | Back out one layer | menu → cell sel → point sel → zoom → blur (P1-3). **The blur rung leaves the row cursor on the point you left**, painted and announced — focus genuinely goes to `<body>`, because `.node-content`'s focus listener enters edit and landing there would undo the Escape, so the cursor is what carries "where you are" instead; `↑/↓` move it and `Enter` goes back in (#1490). **Backing out of chrome restores the interrupted edit**: leaving the search field, the file menu, the help panel, or a docked panel (`F6`, above) via `Esc` puts the caret back exactly where it stood — same point, same offset — unless another point was focused meanwhile (`armChromeReturn`/`restoreChromeReturn`) |
| `F1` (picker menu open) | Guide entry for the **highlighted suggestion** in the `{` picker | The keyboard twin of the `.bm-help` mark (mouse). Chosen because a picker is open INSIDE a text field, so every printable key — `?` included — must keep typing; F1 is the platform help key and collision-free by construction. `preventDefault` suppresses the browser's own F1 page. Generalizes as "help for the thing under focus" if help marks appear elsewhere (#736) |
| Search `↓` | Step from the search field into the outline | First **matching** point while a filter is active, else the first displayed point. The filter stays applied; `Esc` from the landed point follows the normal back-out order |
| Builder search `Tab` | Commit ghost-text autocomplete suggestion, then focus the detail pane. `Shift+Tab` returns focus to the command list | Transient dialog-internal meaning — does not redefine global `Tab` (sanctioned transient Tab-group, UXP-65) |
| `Ctrl/⌘ + S / O / F` | Save / open / find | — |
| `Ctrl/⌘ + /` · `?` | Open the keyboard shortcuts panel | `?` only when not typing; the panel is the keyboard cheat-sheet |
| `Ctrl/⌘ + Shift + /` | Open the Concept guide | The "bigger help": the full feature reference (vs the shortcuts panel). Works mid-edit. The File menu's Concept guide row and the panel's "Concept guide ›" footer are the pointer doors (UXP-125) |
| `Ctrl/⌘ + Z` / `Y` / `Shift+Z` | Undo / redo | — |
| `Ctrl/⌘ + B / I / U` | Bold / italic / underline the selection (edit mode) | The standard editor chords: `FORMAT_SHORTCUTS` → `applyInlineFormat`, which wraps the selection in `**…**` / `*…*` / `++…++` (#459). `preventDefault` always (so native `⌘B` never injects a raw `<b>`); a no-op on a collapsed selection. Unshifted, so no collision with the shifted capture/link chords |
| `Ctrl/⌘ + Shift + L` | Copy link to point | — |
| `Ctrl/⌘ + Shift + I` | Toggle the Capture strip | Global; opening focuses the strip input, the same chord closes it |
| `Ctrl/⌘ + Shift + 1–0` | Capture to inbox N (0 = inbox 10) | Opens the strip targeting slot N; while open the same keys switch the destination in place; an empty slot adopts the selected point |
| `Ctrl/⌘ + Alt + 1–0` | Set the current point as inbox N | The inverse door; the bullet menu's "Set as inbox" mirrors it |
| `Ctrl/⌘ + Shift + Enter` | Follow link under caret (edit mode) | Caret inside a `[[#id\|label]]` or `[[docId#id\|label]]` token → navigate to the linked point. No-op when the caret is not inside a link token. Pure core: `tokenUnderCaret(text, offset)`. |
| `Ctrl/⌘ + Shift + X` | Add or tick a to-do on the current line | Editing point, or the point under the cursor outside edit mode; both routes go through the pure core `taskShortcutAt(text, offset)`. A plain line **gains** `- [ ] ` (#1471: it used to no-op here, silently, which is the line most people try it on first). An existing box flips `- [ ]` ↔ `- [x]`. It **never removes** a marker: that is `toggleTaskLine` and the touch edit bar's **Toggle to-do** button, which is the door that adds *and* removes. A `#KEYWORD` line is already a to-do in the form the state cycle owns, so this declines and names `Ctrl/⌘ + Shift + S`. Announces every outcome, including the refusal, via `#a11y-live`. |
| `Ctrl/⌘ + Shift + S` | Cycle to-do state forward | Editing point, or **every selected point** outside edit mode (LF-1f). Advances `#TODO → #NEXT → #WAITING → #DONE → (cleared) → #TODO …`. No-op on a point with no state keyword. Announces the new state via `#a11y-live`. |
| `Ctrl/⌘ + Shift + P` | Cycle to-do priority forward | Editing point, or **every selected point** outside edit mode (LF-1f). Advances `#A → #B → #C → (none) → #A …`. A priority hangs off a state keyword, so a point with none is a no-op that **says so** and names `Ctrl/⌘ + Shift + S` (#1471). Announces via `#a11y-live`. |
| `Ctrl/⌘ + C / X / V` | Copy / cut / paste points | Multi-select context |
| Table `Tab` / `Shift+Tab` / `Enter` | Next / previous cell (wraps across rows; `Tab` at the last cell adds a row) · `Enter` = cell below (stops at last row), `Shift+Enter` = cell above (stops at top row) · computed (formula-driven) cells are read-only (`Σ`-tagged, Tab-navigable) | **P2-3** — lands selecting the cell's contents (type-to-overwrite). Cells are single-line, so `Shift+Enter` navigates (Excel/Sheets convention), never inserts a break. Column ▾ menu: `↑↓` navigate items, `Enter`/`Space` select, `Esc` closes |
| Table `↑/↓/←/→` | `↑/↓` cell above / below — at the top/bottom edge, **exit** the base to the adjacent outline point. `←/→` cell left / right — stop at the left/right edge (no wrap, no exit; horizontal has no outline analog) | Plain-arrows rule: the caret moves within the cell's text first; navigation fires at the text edge — the first/last line for `↑/↓`, the first/last character for `←/→` — same convention as `↑/↓` between points (which likewise **enter** a base's first/last cell when arrowing past it). A navigation-placed (type-to-overwrite) selection acts as a spreadsheet cursor: arrows move cells directly. That nav-placed selection never opens the selection toolbar; an intentional one (mouse drag, `Shift+arrows`, `Ctrl/⌘+A`) does. `@` in a cell opens the insert menu (sans Table — no table-in-base — and Footnote, which is point-level) |
| Manual clock: click / `Enter` · `Shift`+click / `Shift+Enter` · tap / long-press (touch) | Advance / step back one segment | The advance-reverse pair on a manual clock pill (`[o N/M]`, #646): `Shift` reverses the step on desktop, and on touch — which has no Shift — a 450ms press-and-hold is the recorded step-back twin of the tap (#702, the bullet long-press gesture reused; a fired hold swallows its trailing click so it never also advances). The Shift+F10 Pills rows (Advance clock / Step clock back) are the modifier-free keyboard door |
| Overlay group `↑↓←→` · `Home`/`End` · `PageUp`/`PageDown` | Move within a **roving group** inside an open overlay — one Tab stop for the whole group, arrows inside it | The graph and the timeline (UXP-240), joining the calendar grid / table grid / tablist / search legend. **Timeline** (a vertical list): `↑/↓` adjacent row, `Home`/`End` the ends, `PageUp`/`PageDown` the adjacent **month** — the same "page a month" meaning this table already gives those keys in the agenda calendar grid and the Schedule date picker, reused on a third surface rather than minted. `←/→` unbound (no horizontal axis). **Graph** (one force-directed web, navigated in **document order** — measured as already tracking the layout): all four arrows step one node, `Home`/`End` the ends; `PageUp`/`PageDown` deliberately unclaimed so they scroll the panel. Ends **clamp**, never wrap. Shared pure core `roveIndex` |
| `Ctrl/⌘ + 1…6` | Collapse to level (best-effort) | Toolbar is primary (P1-5) |
| `Ctrl/⌘ + Shift + J` | Toggle journal mode (in-doc ↔ file-per-day) | Doc-level config that fits no `/`,`@`,`{…}` door; flashes the new mode (P4). Listed in the `?` panel (File). `cycleJournalMode` |
| `Ctrl/⌘ + Shift + ] / [` | Next / previous **document tab** | Cycles the open-tab strip (workspace only). No-op with <2 tabs. Dirty-guarded by `switchWorkspaceDoc`. `cycleDocTab(±1)` — listed in the `?` panel (File) |
| `Alt + 1–9` | Jump to the Nth **document tab** | Direct jump within the open-tab strip (workspace only; `gotoDocTab`). Listed in the `?` panel (File). NOTE: `Alt` is elsewhere the point-movement modifier (`Alt + ↑/↓`), so this overloads it onto tab navigation — an accepted convenience, flagged as an owner grammar decision if the overload ever bites (UXP-144) |
| `Ctrl/⌘ + D` | Duplicate the point (subtree) | Works editing or from the row cursor (`duplicateNode`). Listed in the `?` panel (Edit) |
| `Ctrl/⌘ + Shift + Backspace` | Delete the point (subtree) | The explicit whole-point remove; plain `Backspace` edit-flow (merge-up / delete-empty) untouched. Listed in the `?` panel (Edit) |
| `Ctrl/⌘ + Shift + V` | Toggle the Variables panel | Global; re-renders while open. Listed in the `?` panel (File) |
| `Ctrl/⌘ + Shift + . / ,` | Verbosity dial: forward (quieter) / reverse (more guidance) | The LF-2 3-position dial (`guided → standard → lean`); reverse escapes the one-way ratchet. Flashes the new tier (P4). Listed in the `?` panel (File) + the File-menu row keycap |
| `/verb` then `Enter` | Apply a point command from the `/` menu | The keyboard door to every block/organize verb. A bare verb opens its dialog where it has one (`/due` → Schedule) |
| `/verb:value` (opted-in verbs only) | Apply a command **with an inline argument**, skipping its dialog | Opted-in set = the `SLASH_ARG_VERBS` membership in index.html (authoritative; today: `due`, `start`, `check`, `alias`, `template`, `prop`, `base`, `savetemplate`, `refile`) — e.g. `/due:tomorrow`, `/start:2026-07-01` (also `today`, `today±N`), `/check:sum(cost) <= budget`, `/alias:wyrm, drake`, `/template:Weekly`. **Only arg-verbs consume the `:value`**; on any other verb the colon is plain text (`/quote:x` → quote, `:x` stays). Reuses the `/` door — **not** a new sigil, does not grow the §2 inventory (P5). See §7.1a for the build contract (narrow gate, trigger-pinned strip, P4 on a bad value) — **UXP-69** |

*(Retired: `Ctrl/⌘ + M` (Column menu) and `Ctrl/⌘ + Shift + M` (Base menu). `⌘+M` collides with the macOS "minimize window" system shortcut, so both chords were dropped. Column **and row** ops → `Shift+F10` on a focused base cell (the cell's context menu covers both axes; row sections appear on data rows); base menu → the base's bullet popup (`Shift+F10` on the focused point), where whole-base ops live. No dedicated chord remains for any of them.)*

**Modifier semantics (memorize these, not the table):** plain = text · `Tab` = depth · `Alt` = move · `Ctrl/⌘` = app command · `Esc` = back out · `Shift` = extend.

*(Sanctioned transient Tab-group (UXP-65) — **narrowed 2026-07-25**: in-content chips/pills are normally `tabindex="-1"` (roving, not Tab stops), and the **focus-shown search panel** (`#search-hint`) was the recorded exception on the grounds that a transient, self-dismissing panel may host a full Tab-group. The **example chips inside it no longer take that exception**: UXP-238 measured 48 of them and converted the group to roving, so "small and bounded" turned out not to describe them. The saved-search chips and workspace-result rows remain Tab stops. The rule that survives is the narrower one: **a transient panel may host a Tab-group only while the group stays small; measure it, do not assume.** Size, not transience, is what earns the exception.)*

*(Roving groups, the standing pattern — seven sites as of 2026-07-25: the agenda calendar grid, the table grid, the `role="grid"` helper, the tablist, the search legend (UXP-238), and the **graph + timeline overlays** (UXP-240). All share the pure `roveIndex` core: ends **clamp**, never wrap. The per-surface arrow mapping follows the **visual layout** rather than a fixed key-to-axis rule — the legend's chips run horizontally, so `←/→` step and `↑/↓` change row; the timeline is a vertical list, so `↑/↓` step and `PageUp`/`PageDown` change month. That is not a context inversion (P1-1): in both, the arrows move along the axis the eye reads, and the secondary key jumps a group.)*

---

## 4. Discoverability — the three-door rule

For any capability, build the doors in this order. A feature is not done until all three exist and agree.

| Door | Requirement |
|---|---|
| **Visible affordance** | A control a user can see without knowing the feature exists (toolbar button, `+`, `=`, formula entry, chevron, badge). MAY be verbosity-gated, but MUST exist at the Guided floor. |
| **Typed path** | The keyboard / markdown / shorthand way. |
| **Menu path** | An entry in `/`, `@`, bullet, or a contextual menu that **prints its typed syntax** (P2-2), so the menu *is* the tutorial (`ux.md`'s highest-leverage win). |

**Build discipline (from `ux.md`, now binding):** ship the bare interaction first, then add its helpers (hints, chips, descriptions) as a **separate, verbosity-gated overlay** — so Lean and Guided never diverge.

---

## 5. Accessibility baseline

Restated as a per-feature requirement, deferring to `accessibility.md` for phase order. The non-negotiable framing:

- **Additive only.** Attributes + CSS. No sizing/color/layout change as part of a11y work (matches `accessibility.md`'s guardrail).
- **Alongside, never replacing.** Keyboard handlers are added next to `mousedown`+`preventDefault`. Converting a control to `click`/`<button>` to "make it accessible" is prohibited — it breaks the caret invariant.
- **Per-row at render time.** ARIA goes on in the `render()` pass; there is no global list to patch later.

**Required now (every feature):** accessible names (P3-1), keyboard operability beside mousedown (P3-2), focus-visible + reduced-motion (P3-3), non-color signals (P3-4), `aria-live` for off-focus change (P3-5), and the **interim pill/row labels** of P3-6.

**Sequenced in `accessibility.md` — all landed:** full `role="tree"`, `role="grid"` on tables, and pill `tabindex` (UXP-19); dialog focus-trap (UXP-16); contrast retune (UXP-18). The standard's contribution is that these stopped being a *separate track*: the conformance matrix (§9) and the DoD (`ux-definition-of-done.md`) tie each a11y requirement to the feature that introduces it, so discoverability and accessibility are satisfied **in the same pass on the same widget** — closing the "two disconnected passes" gap.

---

## 6. Feedback & error pattern

One pattern, four channels. Reuse — do not invent.

| Situation | Channel | Reference |
|---|---|---|
| Transient success (link copied, points pasted) | **Toast** | the existing "Link copied" toast |
| Input validation (shorthand preview/failure) | **Inline marker** at the point of input | P4-1/P4-2 |
| State change while focus is elsewhere (reroll, `#ERR`, autosave) | **`aria-live` region** | P3-5 |
| Approaching a limit (storage, depth, size) | **Proactive banner** with an action | the storage-quota warning — the gold standard |

**Toast priority (#391):** the toast channel is ONE element, so it carries a priority rule: an error owns the element for its full dwell; a hint that lands mid-dwell is deferred (it still reaches assistive tech immediately, and the last deferred hint replays after the dwell); a new error may always preempt. One pattern does not mean last-writer-wins: a failure is never silently displaced by a success (P4-1). The rule lives in the pure `toastGate`.

**Live-region priority (#1498):** the polite region is ONE element too, and it carries the same kind of rule for the same reason. `announce()` clears it synchronously and re-sets it on the next frame, so two messages produced in the same tick both land in one frame and the first is never a settled value: it is not superseded, it is never spoken at all. Measured: a check flipping to FAILING and a first-encounter teaching tip landed 0ms apart, and the failing constraint was the one lost. So a coaching aid is the lowest-priority thing on this channel and waits (`NUDGE_YIELD_MS`) for the functional message to settle. What the app is saying about the edit a person just made outranks a tip about what they could do next.

**A sink that names a point names it the way the screen does (#1498).** Any message or label that identifies a point resolves its title through `displayText`, never `textForDisplay` and never raw `node.text` — those stop at block prefixes and leave `[[math:key]]` in place, so the point a sighted reader sees as "Kitchen job 2,700" reaches a screen reader as its internal token. Same rule as the `[[` picker and the graph (#821, UXP-264): the spoken name and the shown name are the same name.

Two census results qualify it. **One member it does not reach:** a base-view card title is a table CELL, which has no plain-text shown value (`unfoldTokensIn` gives the source, not the result), so `bvMoveCard` announces `stripMd`'s glyph placeholder — "there is a dice pill here" rather than a token or a stale number. Leave it that way until cells grow a resolver of their own. **One member that had to be fixed to make the rule true:** the live-title link sink and the workspace title index (#1526). #1402 fixed that sink's nested-link half and left the artifact half, so a `[[#id]]` reference to a point holding a pill printed the raw token on screen, and one index map fed six foreign-document captions the same way. Both now go through `displayText`. **And one that had to be fixed twice over:** #1528, the other half of the same sink. A caption built from a FOREIGN title resolved that title's own `[[#id]]`s against the live document, printing a bare node id. The rule is the graph's (#1110): a bare `[[#id]]` is same-document to the document the STRING came from, so the resolver is anchored on that document, not on the open one. `linkText` and `displayText` now take that document as an argument (omitted = the open one), and all four foreign readers pass it — the cross-doc link caption, the cross-doc backlink rows, the `[[` picker's other-doc rows, and the cross-doc unlinked-mention collector, which had already anchored its variable scope and not its links. A test enumerates those readers, because this family has now been left half-fixed four times (#1110, #1402, #1526, #1528).

**One failure, one sentence (#1500).** When a feature can fail in several ways, the message names the one that happened. `convert()` failed in four ways and reported the fourth for all of them, because the information was destroyed before the message was chosen: `convertUnits` collapses its causes into one `null` and the substitution replaces the call with a bare marker, so by the time the copy is picked there is nothing left to distinguish `convert(10, blorp, kg)` from `convert(10, km, kg)`. Both read "a conversion between two different kinds of units (like a length and a mass)", and the first has no dimensions to mismatch. **Where a message is chosen downstream of the failure, the marker carries the cause** — that is the shape, not a special case for units. A sympathetic instance is worth stating: `convert(12, m2, ft2)` looks like a dimension mismatch and is not, because there is no area dimension at all, so it fails by the identical unknown-unit path as a typo.

**A remedy is cited only after it has been driven (#1500).** The unknown-unit message points at `/units`, and that door was driven end to end before the copy named it: `cp` then `gp = 100 cp`, submitted with the dialog's own **Define units** button, makes `convert(2, gp, cp)` answer 200. Two things that would have made the citation wrong were found by driving it rather than reading it: the button is not called Save, and a unit declared before its base is skipped with a warning, so the message says "the base first". Copy that names a door the author has not opened is a guess.

**A state change means the same thing through every door (#1502).** Completing a repeating point rolls its schedule forward and re-opens it — and that lived in the two edit-mode paths only, so the state-badge picker, the bulk cycle chord and the row-cursor state command each flipped the point to done and left its schedule where it was. The recurrence was gone permanently, with no cue. The line was display-mode vs edit-mode, which is not a distinction a user can see or predict: `Ctrl+Shift+S` meant "reschedule" while editing and "end this forever" from the row cursor. **The remedy is one writer, not four call sites**: every door commits its done-flip through `commitTodoDone`, and a test enumerates the doors and asserts none of them re-derives `checked` itself. A fifth door that forgets is the bug returning, and there is now only one thing for it to call.

**Tone:** plain language, names the thing, offers the next step. The storage warning ("…save it to a file to be safe") is the template for all of it.

**Drafts:** a transient input surface (the capture strip, the search box) MUST NOT discard a non-empty draft on dismiss; the draft is kept for the next open, or its loss is confirmed (P4).

---

## 7. Pattern catalog (canonical implementations)

To stop reinvention, every feature reuses these rather than building its own.

### 7.1 Menu pattern (`/`, `@`, `{`, `#`, link, state pickers)
One behavior contract: open on trigger → filter as you type → `↑/↓` move, `Enter`/`Tab` apply, `Esc` close (P1-3) → each row = icon + label + description + **typed syntax** (P2-2) → `role="menu"`/`menuitem` per `accessibility.md` Phase 1 → reduced-motion respected. The **file menu is not a menu** — it is a settings `dialog` (per `accessibility.md`); don't force `role="menu"` on it. The **`{` grammar picker** (UXP-9 + UXP-192) is this pattern applied to the grammar engine: it opens on a bare identifier prefix (or empty) inside an unclosed `{` and offers two kinds of row — a **Grammar forms** group (every `{…}` sub-form — `{= expr}`, `{cond: a|b}`, `{shuffle: a|b}`, `{ref.mod}`, `{5 to 10}`, …; applying inserts the scaffold with its placeholder selected for type-to-replace) above the doc's **callable names** (Variables / Rules / Tables / Chains, variables showing their resolved value; applying writes `{name}`). Both paths type the **existing** shorthand the engine already promotes — discoverability for the whole composition surface with **no new syntax** (P5-conformant by construction). This is the primary front door that keeps the closed inventory legible at the point of authoring. The **`#` tag picker** (UXP-10) is the same pattern over the document-wide tag index (`collectTags`, usage counts shown): it opens on `#prefix` at the caret (mdInline's sigil rule; never inside a `[[#id|…]]` link token) and applying writes the existing `#tag` — so tags stop drifting (`#todo` vs `#todos`).

#### 7.1a Inline-argument verbs (`/verb:value`) — the narrow-gate rule (UXP-69)
A slash verb may accept an inline argument so the value is typed, not dialog-picked — `/due:tomorrow`, `/start:2026-07-01` set the date with the keyboard and skip the dialog; a bare `/verb` still opens the dialog. This **reuses the existing `/` door** (it is NOT a new sigil and does not grow the §2 syntax inventory — see P5), and it is the preferred way to make a dialog-only verb keyboard-first. **A verb-with-argument is a real keyboard-usability win, but the parser change is shared, so it MUST be gated narrowly or it silently eats other verbs' text.** The contract, from the `/due:value` build (PR #163):

- **Gate the `:value` arm to the specific opted-in verbs, never all verbs.** The slash-query regex that captures `verb:value` widens a parser every command flows through. Only the verbs that declare an argument (the `SLASH_ARG_VERBS` membership — the regex in index.html is authoritative, not this list) may consume a trailing `:value`; for every other verb a colon is **plain text**. Concretely: capture the optional `:value`, but if the word before the colon is not an arg-verb, drop the colon from the query so the command matches on its word and the value stays in the node text. The acceptance test is `/quote:x` → applies *quote*, leaves `:x` in the text (NOT consumed).
- **Pin the text-strip to the trigger position, not to the (possibly trimmed) query length.** After deciding what the query is, the strip that removes `/verb[:value]` from the buffer must delete from where the trigger character actually sits — derive the offset from the raw matched span, not from a query you may have shortened. Mismatch here mangles the surrounding text (a too-far-right offset leaves a fragment like `/q`). 
- **A bare verb (no value, or an empty `verb:`) falls through to the dialog** — the value-less path is unchanged, so the door still works for users who don't know the inline form.
- **Validate the value and report failure (P4).** An invalid argument (`/due:notaword`) MUST flash why ("Not a valid date…"), never silently no-op — the whole reason inline verbs are worth it is that they're a real input channel, and a real channel can't swallow bad input. A valid one sets state and flashes a confirmation.
- **Keep the value-parse a pure core** (e.g. `parseDateSlash(query) → {key, raw} | null`), Node-testable, returning `null` for "not an arg-verb / no value" so the caller branches to the dialog. Pin the gate cases (arg-verb with value, bare verb, non-arg-verb-with-colon) in `tests/test.mjs`.
- **Aliasing:** if two keys share one menu door (the single `due` "Schedule" command also fronts `/start:`), resolve the real key from the parsed query at apply time; the menu match treats the alias as the host verb.

The principle generalizes beyond dates: any verb whose only door was a dialog can gain a `/verb:value` fast path, **provided the arg-arm is gated to that verb and the strip is trigger-pinned.** Reach for it to make dialog-only actions keyboard-complete — that is a P2/P3 improvement — but never by widening the shared matcher unconditionally.

### 7.2 Pill pattern (all live inline objects)
Dice/math/variable/grammar/markov/rolltable are one object with different generators (the "everything is under the grammar engine" reality from `CLAUDE.md`). They share: render = icon + (name) + result + edit affordance · interaction = a single **body-click acts by pill family** and **stays in display mode** — the **P1 body-click carve-out (UXP-63)**: **generators** (dice · grammar · markov · random-pick var · estimate) **re-roll / re-sample** in place; **computed or declarative** pills (math · formula var · display-only var · sequence) **open their editor**; a **deck** (`{shuffle|cycle|once|stopping}`) **advances** one draw. One gesture, outcome by pill type *because the pill type differs* — a deterministic value has nothing to re-roll, a deck advances by design — so **each pill's `title` *and* `aria-label` names its own outcome** ("click to re-roll / re-sample / edit / advance"), never ambiguous to sight or AT. The pencil, where present, always opens the dialog (do not deviate). *(The stronger structural fix — a shared `interactionClass(type)` driving both the cue and the dispatch — stays deferred, weighed against the deliberate-distinct-pills philosophy; see UXP-63.)* · authoring = dialog **and** shorthand where inline-able, with preview before promotion (P2-5/P4-2) · a11y = accurate `aria-label` updated on reroll (P3-6 interim). A new generator plugs in here and MUST NOT define its own interaction or a11y behavior.

### 7.3 Feedback pattern
Per §6 — the four channels, no bespoke feedback UI.

### 7.4 Affordance pattern
Per §4 — the three doors, built in order.

### 7.5 Column menu (consolidated table-column ops)
A base column's operations live in **one** menu — the Column menu — off the **name-pill header cell** (Bases PR 2c). The column name is an editable **name pill**; **clicking the header cell around the pill** opens the Column menu, **dragging it** reorders, and a **right-border grip** resizes (double-click = auto-fit). There is no separate `▾` opener and no hidden gestures (the old double-click-cycles-alignment is gone). The menu follows the §7.1 pattern (`role="menu"`/`menuitem`, `↑↓`/`Home`/`End`/`Enter`/`Esc`, reduced-motion respected) and grows by **section**, not by sprouting header controls: today **Calculate · Alignment · Width · Insert column · Move column · Delete column**; later formatting etc. slot in as further sections. The rule: a new per-column capability is a new menu section, **never** a new hidden header gesture. **Keyboard door:** `Shift+F10` / the Menu key on a focused base cell opens the **cell's context menu** — the Column sections plus, on a data row, the Row sections (Insert above/below · Move up/down · Delete row) — covering both of the cell's axes with no extra chord (replacing the dropped `⌘+M`, which collided with macOS minimize — see §3). The row handle's click menu (`showRowPanel`) stays the pointer path. Direct-manipulation paths (drag-reorder, drag-resize) each ship a **visible cue** and a keyboard-reachable twin in the menu (Move left/right, Width presets), so nothing is drag- or double-click-only (P1/P2/P3).

### 7.6 Dialog footers
Dialog footers order buttons **dismiss/neutral first, the committing action last**; a danger action always occupies the final slot, never the first. The shared `confirmDialog` is the reference implementation; a dialog may not invert this order for emphasis.

---

## 8. Microcopy & terminology

- Use the §1 user-facing vocabulary in every string and `aria-label`.
- Describe **outcomes, not mechanics**: "Collapse point", not "toggle node folding".
- Plain language; the storage-warning copy is the reference register.
- One concept = one word across UI, `?` panel, and docs.
- Menu, button, and dialog labels use **sentence case** (capitalize only the first word and proper nouns); "Markdown" is a proper noun and is always capitalized in user-facing copy.
- Every icon-only control: `aria-label` (accessible name) **plus** a `title` (hover) — and `aria-hidden="true"` on the decorative glyph inside so the name isn't doubled (matches `accessibility.md` Phase 0).

---

## 9. Per-feature conformance matrix

The punch list. ✅ conformant · ⚠️ partial · ❌ non-conformant — with the governing rule.

| Feature | P1 predictable | P2 discoverable | P3 reachable | P4 responsive |
|---|---|---|---|---|
| Outline nav / move / indent | ✅ (collapse on `Ctrl/⌘+./,`; UXP-5) | ✅ (`Tab`/`Shift+Tab` + Alt-arrows keyboard; mouse bullet-drag; **touch: drag the bullet sideways to indent/outdent + reorder — UXP-70**, closing the prior gap where touch had no nesting-depth front door) | ✅ (flat ARIA tree: treeitem + level/posinset/setsize/expanded/selected — UXP-19; collapse-btn + breadcrumb named/keyboard-operable — UXP-14) | ✅ |
| Paragraph block | ✅ (documented prose-mode exception) | ✅ | ✅ (same treeitem/grid structure as all points; the prose-mode Enter carve-out is P1's documented exception, not a P3 gap) | ✅ |
| `/` and `@` menus | ✅ | ✅ (both typed at the caret; on **touch** the `@` insert menu also has a visible door — the `#edit-bar` "Insert a pill" button — UXP-105; block turn-into rides the bullet long-press) | ✅ (listbox + option + activedescendant; arrow/Enter/Esc nav — a11y Ph1 complete) | ✅ |
| Markdown / TODO states | ✅ | ✅ | ✅ (badges emit keyword text, not color alone; done = strikethrough; priority = `[#A]` text — P3-4 satisfied) | ✅ |
| Pills (dice/math/grammar/…) | ✅ | ✅ (all reachable via `@` menu — UXP-11 audit; SHORTCUTS names each type) | ✅ (labels + reroll announcements — UXP-15; pencils named + Enter/Space-operable — UXP-13/14; dialogs focus-trap + restore — UXP-16; pill `tabindex="-1"` + Enter/Space body activation — UXP-19) | ✅ (gr-bad typo marker — UXP-6; live `#ERR` — UXP-34) |
| Variables | ✅ | ✅ (`{` picker + variables panel; UXP-9) | ✅ (pill: tabindex/Enter/Space + per-state aria-labels for ↻/?/— errors; vars panel: role=region + named close-btn + `aria-live="polite"` on content-refresh, signature-guarded so unchanged lists don't re-announce — UXP-38) | ✅ (live ↻/？/— error codes + distinct aria-labels per state; no stale fallback — UXP-34) |
| Inline `{…}` shorthand | ✅ | ✅ (live preview tooltip on `}` close: → dice/math/grammar/var — UXP-7) | ✅ (shorthand promotes to fully a11y pills; gr-bad uses dotted underline + color (not color alone) + AT announce — P3-4 satisfied — UXP-6) | ✅ (gr-bad typo marker + AT announce — UXP-6) |
| Tables (cells) | ✅ (Tab/Shift+Tab/Enter nav; computed cells read-only + Σ-tagged) | ✅ (`@table` inserts; `✏ markdown` button + `.mt-promote` button (touch-fallback visible) reveal the edit/convert path — UXP-22/23) | ✅ (`role="grid"` + native row/columnheader/gridcell mapping; computed cells `aria-readonly` — UXP-19) | ✅ |
| Table columns (ops) | ✅ (one Column panel: Calculate/Alignment/Insert/Move/Delete; no hidden double-click; UXP-21) | ✅ (one sized ▾ door; grip cue for drag; full-height "+") | ✅ (panel role=menu/menuitem keyboard-operable; sized targets + aria-labels; `Shift+F10` cell context menu covers row ops too — UXP-14) | ✅ (refresh reflects every op) |
| Table formulas (`#+TBLFM:`) | ✅ (Calculate aggregates + the Formula dialog for arbitrary `$N=`/`@R$C=` assignments; UXP-3 A+B) | ✅ (Formula menu section; column-name chips ARE the reference picker; hint teaches the grammar; raw `#+TBLFM:` stays the power path) | ✅ (panel role=menu/menuitem; dialog focus-trapped — UXP-16; computed cells `aria-readonly` — UXP-19) | ✅ (live computed preview in the dialog; footer auto-added/removed; reason-coded `#ERR (cycle/bad ref/non-numeric)` — UXP-8) |
| Links (`[[ ]]`) | ✅ | ✅ (`[[` picker live — UXP-4; Copy-link stays the power path) | ✅ (picker is listbox/option + activedescendant; the rendered link pill carries `role=link` + `tabindex="-1"` + `aria-label` and Enter/Space follows it — UXP-53) | ✅ (rename repaints on-screen links immediately) |
| Footnotes / hashtags / emoji | ✅ | ✅ (`#` tag picker — UXP-10; **`:` emoji picker** — UXP-106, so every authoring sigil now opens a filtering menu) | ✅ (hashtag chips fully operable — UXP-39; the fn-ref trigger is `role=button` + `tabindex="-1"` + `aria-label`, with a click/tap + Enter/Space reveal — UXP-55; the emoji picker is a `role=listbox`/`option` caret menu with arrow/Enter/Esc nav — UXP-106; the rendered emoji glyph itself is non-interactive text) | ✅ |
| Search | ✅ | ✅ | ✅ (aria-label + aria-describedby on search box; saved-search chips role=button + Enter/Space/Delete/Esc; ↓ jumps to outline) | ✅ (in-doc empty-state banner when query yields 0 matches — UXP-59; workspace snippet context-aware: windows around needle / names field for prop/is/due hits — UXP-64) |
| Dates / agenda (List · Timeline · Calendar) | ✅ (click/Enter zooms everywhere; the Calendar grid now matches the Schedule date-picker — `role="row"` weeks + Arrows/Home/End **and PageUp/PageDown** month nav — UXP-62) | ✅ (toolbar Agenda button + `/due` Schedule verb + bullet menu; `?` panel **Dates & agenda** section + search legend document `due:`/`start:` — UXP-37) | ✅ (toggles/chips/bars/cells: `role`+`tabindex`+`aria-label` with `keydown` beside `mousedown` — caret invariant; Calendar cells `role=gridcell` in `role="row"` weeks + roving tabindex — UXP-62; **non-colour urgency cue** `! ` on overdue Gantt names + calendar chips, P3-4 — UXP-66) | ✅ (no silent failure; empty-state hints route to `/due`) |
| Autosave / storage | n/a | ✅ | ✅ (`role="alert"` on the warning — UXP-18) | ✅ (reference pattern; file/workspace errors now use `flashError` toast instead of native `alert()` — UXP-58) |

**Syntax conformance (P5) — read this whenever proposing a feature.** The inventory in §2/P5 **is** the current authoring language; today's set is the baseline, not a problem. The rule is **no growth without sign-off**. The standing risk is *new* sprawl — and the roadmap already contains two examples to police: a proposed render-only `{= expr}`/`{NdM}` "second syntax alongside `[[type:key]]`," and possible `B3`-style table refs. Both are **P5-3 violations unless they replace** what they overlap; route them through the `{…}` engine / `@row$col` instead. Tracked as a standing guard in `ux-remediation.md`.

---

## 10. How this plugs in (altitude map)

- **The spine** (vocabulary split, the five principles, keyboard grammar, the invariants) is summarized in `CLAUDE.md`'s "UX discipline" section so it is **always loaded**.
- **This document** is the full standard — added to `CLAUDE.md`'s "read these before building" list beside `ux.md` and `roadmap.md`.
- **The gate** (`ux-definition-of-done.md`) runs on every UI change and is appended to `adding-an-artifact.md` as its final step, so the per-artifact recipe and the universal UI gate are the same checklist.
- **`ux.md` and `accessibility.md` keep their jobs** (strategy and sequencing); this standard cross-references them so discoverability and accessibility are satisfied together, not in disconnected passes.

---

## Appendix — acceptance tests at a glance

| Principle | The feature is done only if… |
|---|---|
| **P1** | a user can predict an unused shortcut from the grammar |
| **P2** | a docs-free user can find and use it through visible UI (Guided) |
| **P3** | it is keyboard-only operable and a screen reader announces name + state + result |
| **P4** | no action leaves "nothing happened, no reason" |
| **P5** | it added zero new top-level syntaxes (or replaced one and updated the inventory) |

*Compiled against the Pointliner source and its existing guidance docs. The engineering invariants are quoted from `CLAUDE.md`/`accessibility.md`; the principles, vocabulary, keyboard grammar, and gate are the UX standard proposed for adoption.*
