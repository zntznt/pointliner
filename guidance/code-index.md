# Code index

**Generated by `tools/symbol-index.py`. Do not edit by hand.**
CI regenerates this and fails on drift, so it cannot rot (#1430).

Every top-level declaration in the app `<script>` of `index.html`, grouped by the section
marker it sits under and by the `DOMAIN:` banner above that (#1431), with the first line
of its own comment as its purpose. `grep 'DOMAIN:' index.html` prints the coarse outline.

No line numbers, deliberately: they drift every edit and names do not
(`guidance/architecture-reference.md`). Grep a name to find it. For jump-to-symbol while
editing, `python3 tools/symbol-index.py --with-lines` prints them to stdout.

**2088 declarations in 154 sections across 17 domains.**

# Document model & caches

## doc-cache registry

- `DOC_CACHES` — Every cross-node cache in the file registers here, so resetDocCaches() AND the invalidation test
- `regDocCache`
- `docCaches`
- `makeDocCache` — makeDocCache(name, compute) — the sanctioned door for a NEW whole-tree cache: the standard

## model

- `root`
- `focusedId`
- `showDone`
- `showNotes`
- `VERBOSITY_TIERS` — The verbosity dial — a 3-POSITION control (guided → standard → lean), cycled by ⌘⇧. or set
- `verbosity`
- `isLean`
- `isGuided`
- `isStandardOrLean`
- `searchQuery`
- `searchQueryLC`
- `searchTerms`
- `searchMatchSet`

## virtualization state

- `IND`
- `computeIndent`
- `IS_TOUCH` — Touch devices have no hover, so hover-revealed affordances need touch fallbacks.
- `bulletLongPressed`
- `_zoomToastShown`
- `EST_ROW`
- `OVERSCAN`
- `flatRows`
- `flatIndex`
- `rowTops`
- `_vlParkedIdx`
- `totalHeight`
- `heightCache`
- `vlist`
- `winStart`
- `dirty`
- `_savePending`
- `_lastSavedAt`
- `fileHandle`
- `fileName`
- `undoStack`
- `redoStack`
- `nodeMap`
- `parentMap`
- `selectedIds`
- `selAnchorId`
- `selFocusId`
- `nodeClipboard`
- `mkRoot`
- `uid`
- `ensureDocId`
- `mkNode`
- `inheritedType` — type that a new Enter-sibling should inherit
- `BLOCK_PREFIX_MAP` — Block types that store their markdown prefix in node.text
- `textBlockPrefix` — #1463: which block prefix the TEXT actually carries, longest first. applyBlockCmd used to read the
- `deriveTypeFromText`
- `rederiveFromText` — Re-derive BOTH derived hints (type AND checked) from node.text, the single source of truth.
- `textForDisplay` — Text shown in rendered (non-edit) view — block prefix stripped. Also strips a
- `LINK_RESOLVE_DEPTH` — Render `[[…]]` link tokens in a title to legible PLAIN TEXT for the no-pill,
- `linkText`
- `mdLinkOut` — #1111: the markdown form a resolved link takes in an exported .md.
- `CAPTION_TAG_RE` — #943: strip hashtag tokens from a title/snippet shown as a link CAPTION or backlink row.
- `stripCaptionTags`
- `displayTitle` — Title for a no-pill display sink: textForDisplay (prefixes/markers stripped) with
- `ART_SNIFF` — A point's text as it READS on screen — markdown, pill, and {…} syntax resolved
- `EMPTY_VARMAP`
- `displayText`
- `aliasesOf` — A point's alternate names — the reserved `aliases` property, comma-split, trimmed,
- `nodeNames` — Every name a point answers to: canonical title first, then aliases (deduped

# Point semantics: to-dos, progress, pills

## TODO states & priorities (Org-style)

- `TODO_STATES`
- `TODO_PRIOS`
- `PRIO_CYCLE`

## Sequences: user-definable state sets

- `DEFAULT_SEQUENCE` — UXP-158: WAITING is now a structural HELD state (heldFrom..doneFrom), not a hardcoded string in
- `SEQ_STATE_RE`
- `parseSequence` — "TODO NEXT | DONE" → { states, doneFrom, heldFrom } or null. UXP-158: an OPTIONAL middle band
- `SEQ_NAME_RE` — seqDeclParts(body) — sniff a typed sequence DECLARATION: {seq Flow: BACKLOG DOING | SHIPPED}.
- `seqDeclParts`
- `ruleDeclParts` — ruleDeclParts(body) — sniff a typed NAMED grammar declaration: {rule tavern: The {adj} {noun}}.
- `queryParts` — queryParts(body) — sniff a typed query pill: {query: is:todo | due:week}. The reserved
- `queryKey`
- `countParts` — countParts(body) — sniff the query family's third verb: {count: is:todo} (#541).
- `rollParts` — {roll: <search-query>} — the generative sniff twin of queryParts: a reserved `roll:` keyword
- `PROP_KEY_RE` — LEAN FLOOR phase 1 — the promoting inline stub for a SIDECAR-backed verb. propDeclParts(body) sniffs
- `propDeclParts`
- `dateDeclParts` — LEAN FLOOR: the date twin of propDeclParts — {date due: VALUE} / {date start: VALUE}, the promoting
- `setProp` — Pure: set (or replace) an arbitrary property key on a node, mirroring setAliasProp/setDateProp. An
- `sequenceLint` — Pure P4 advisory for the @sequence dialog (C5). parseSequence tokenizes states on
- `_seqsCacheVer` — Document-wide declared sequences in document order (token-gated like the other
- `collectSequences`
- `allSequences` — Default first, then declared in document order — built-in keywords keep their
- `normStateKey` — The sequence a keyword belongs to (first match), or null — null also means
- `sequenceForKeyword`
- `boardLanes` — boardLanes(model, groupBy, seqs, lastRow) — the board view's pure lane model
- `nextLaneKw` — LEAN FLOOR phase 3: the next lane keyword for a keyboard board-card move. `states` is the sequence's
- `calBaseItems` — calBaseItems(model, dateBy, lastRow) — the calendar view's pure item model
- `BASE_ROWS_DEFAULT` — baseInlineView(collapsed, baseRows, totalDataRows, isZoomed) — the pure decision for
- `baseInlineView`
- `keywordIsDone` — Done-ness, generalized: the keyword sits at-or-right-of its sequence's done split.
- `keywordIsHeld` — UXP-158: held-ness (blocked), generalized: the keyword sits in the [heldFrom, doneFrom) band. A
- `_statesCacheVer` — Every recognized state keyword (default + declared), uppercase. Cached on
- `knownStates`
- `LEAD_WORD_RE` — Leading #STATUS word candidate (`#TODO`, `#NEXT`, etc.); only a member of
- `parseTodo` — Split a node's text into { keyword, priority, body }. No keyword → keyword '',
- `formatTodo` — Re-serialize a parsed todo to `#keyword [#P] body`, trimming cleanly when the
- `collapseDoubledKeyword` — #947: collapse a leading RUN of ≥2 state keywords to the canonical single-keyword form
- `collapseTaskKeywordCollision` — agent-review: the app's OTHER to-do dialect collision — a TASK-MARKER continuation prefix
- `cycleIn` — Step through an ordered cycle list with wraparound; unknown current → start.
- `cyclePriority`
- `cycleTodoState` — Text-level mutators the DOM calls. Clearing the keyword also drops the priority
- `cycleTodoPriority`
- `todoSortKey` — Sort key for ordering sibling todos: not-done before done, then priority
- `compareTodo`
- `sortChildrenByProp` — Sort a node's children by a property key, ascending. Numbers sort naturally; dates sort
- `setTodoState` — Direct setters for a one-click state/priority picker (vs. the cycle helpers).
- `setTodoPriority`

## Task-checkbox grammar — single source of truth (code-review F4)

- `TASK_BOX` — A GFM checkbox is `[ ]`, `[x]`, or `[X]`. Every task-aware site composes TASK_BOX
- `TASK_BOX_CAP`
- `TASK_DONE_BOX`
- `TASK_RE`
- `TASK_FIRST_RE`
- `TASK_LINE_RE`
- `TASK_DONE_G`
- `TASK_TALLY_G`
- `TASK_STRIP_RE`
- `TASK_STRIP_G`
- `TASK_KEEP_RE`
- `TASK_STRIP_ONE`
- `isTaskFirst`
- `toggleTaskLine` — #437: toggle the to-do marker on the line the caret is in — a plain line gains `- [ ]`, a task
- `taskShortcutAt` — #1471: what the Ctrl/⌘+Shift+X shortcut does to `text` with the caret at `off`, and WHAT TO SAY
- `taskShortcutSay` — What each outcome is called, so both entry paths (editing, and from the cursor) say the same
- `todoDoneFromText` — Node-level done-ness from the text: keyword wins — done iff it sits right of

## progress cookies ([/] and [%])

- `tallyMarkers` — A `[/]` (fraction) or `[%]` (percent) cookie in a point's text renders as a
- `progressCount`
- `formatProgressCookie`

## progress clocks ([o N/M])

- `CLOCK_RE` — A manually-advanced segmented tension clock (Blades/Ironsworn). It joins the [/]
- `CLOCK_RING`
- `parseClock` — Parse one clock body → {done, total, computed}, or null (→ the token stays literal
- `clockGlyph` — The ring glyph for a fill: 0 is always ○ and a full clock always ●; the middle
- `formatClock` — The display / export string: "◕ 3/6". Same string in the pill and in flattenArtifacts.
- `advanceClock` — Advance (dir=+1) or step back (dir=-1) a clock, clamped to [0,total]. Returns the
- `advanceClockInText` — Rewrite the ORDINAL-th valid clock token in `text` by dir (+1/-1). Pure; returns the
- `clockFillFor` — A COMPUTED clock's fill = the parent's done-tally (the same count [/] shows), capped at
- `completingChildIndex` — The child that COMPLETES a computed clock: walking node.children in order, the index of
- `clockCompletionCue` — True if `child` is the point whose done-ness completes `parent`'s (first) computed clock.

## depth scope: the shared "how deep" vocabulary (UXP-159)

- `SCOPE_TOKEN_RE` — One closed set of scope tokens, honored by every child-scoped rollup (property
- `resolveScopeDepth`
- `collectScoped` — Gather descendants of `node` down to `depth` levels (depth 1 = direct children,

## subtree aggregation: sum / avg / count over a child PROPERTY

- `propExprValue` — A math pill may aggregate a named property over the point's DIRECT children —
- `childPropNumber`
- `aggHasSkippedValues` — #809: does an aggregated property in `expr` skip a child's NON-EMPTY, non-numeric value?
- `isAuditableAgg` — #1284 "explain this number": the audit behind a child-rollup pill (the panel's "trust the numbers").
- `auditAgg`
- `hiddenBelowScope` — #1284: the value a scope-excluded point is HIDING, as { count, sum } or null. Walks the point's
- `auditHiddenNote` — #1284: how a hidden-value row reads. Named separately so the copy is pinnable and says the AMOUNT,
- `auditTitle` — A point's plain title for the audit rows: drop pill tokens ([[type:key]]) and any {…} bodies, then a

## meter pills: {meter: value/max} → a segmented bar of a numeric property

- `METER_FILL` — LegendKeeper's Meter Block, for HP / spell slots / stress. A COMPUTED display (like the
- `METER_POOL_STYLES` — The icon-pool styles: a style word after the ratio draws one FA glyph per unit (filled vs
- `parseMeter` — Parse a {meter: …} body → {value, max, style} or null. style is 'bar' (default) or a pool
- `meterPool` — A pool's fill counts, or null if it should NOT be a pool (max over the cap, or a non-integer/
- `resolveMeter` — Resolve a parsed meter's refs against a node (props read via childPropNumber). Returns
- `meterBar` — The bar string: `cells` glyphs, filled = round(value/max * cells) clamped to [0,cells]
- `formatMeter` — The display / export string: bar + the exact count, e.g. "██████░░░░ 8/12". Same string

## action pills: {HP -= damage} → click-to-apply a mutation to a named stat

- `ACTION_RE` — The generative/compute engines READ; an action pill WRITES. Unlike a declaration
- `parseActionPill`
- `actionRhsValue` — The right side of an action, as a number, rolling the GENERATIVE engine when it's there so
- `actionNewValue` — The value an action pill writes: `current OP rhs`, with the right side resolved through
- `formatStatValue` — A mutated stat should read cleanly: a whole result stays whole (14, not 14.0000000),

## depth nudges (#519): point-of-relevance discovery

- `nudgeSumKey` — A numeric property is worth a sum() nudge only when it can actually roll up — i.e.
- `nudgeRollTag` — A tag is worth a {roll: #tag} nudge once its roster is big enough to be a real
- `reduceAgg` — The reducer's identity semantics, shared by the child-prop rollups AND the query
- `aggregateChildren`
- `_aggColReCache` — Substitute every sum|avg|count|min|max(propName) in a math expression with its
- `aggregateVarBaseColumn`

# Units, lint, appearance, dates & journal

## units: a declarable conversion table (SR-6 / #875)

- `BUILTIN_UNITS` — The calendars/sequences substrate pattern applied to "what is a quantity": a built-in
- `parseUnitDecls` — Parse the units-declaration text into a resolved table (the dialog's core). Each non-empty
- `unitTable` — The doc's live table: built-ins + declared units (a doc name wins on a tie — the packVars
- `normalizeUnits`
- `UNIT_DIM_BASE` — The built-in base unit of each built-in dimension (the ratio-1 anchor), so unitsToText can
- `unitsToText` — Reconstruct editable declaration text from a resolved table (the dialog's round-trip, the
- `convertUnits` — Same-dimension multiplicative convert; null on unknown unit / dimension mismatch / non-number.
- `splitArgsTopLevel` — Split on TOP-LEVEL commas only (commas inside nested parens stay). Pure helper for convert().
- `replaceConvert` — Replace every convert(valueExpr, from, to) with its numeric result (innermost-first, so
- `checkQuery` — #1195: extract the search query embedded in a check's count("q") or sum|avg|min|max("q", prop),
- `expandAggExpr`
- `mathPrepassErrs` — #889: did the expandAggExpr pre-pass INTRODUCE a deterministic #ERR into an expression that
- `firstEmptyRollup` — #557: the prop name of the FIRST rollup in `expr` whose scope matched ZERO values, or null. A
- `emptyExtremalProp` — #1449: min/max over a QUOTED SEARCH that matched nothing. expandAggExpr collapses such a reducer
- `depthExtremalProp` — The min/max DEPTH GAP, made actionable rather than closed. `sum(cost, 2)` reaches two levels down;
- `childrenComputePillsNotProp` — #1200: an empty rollup ({= sum(fee)} that matched no `fee` property) has two distinguishable
- `queryReducerLeaf` — #914: a QUOTED query reducer ({= count("q")} / {= sum("q", prop)}) that is subtree-scoped (no
- `wordsScopeLeaf` — #950: a words(subtree)/words(children) count on a childless HEADING can only ever count the
- `countWords` — Pure: an estimate-grade word count of prose — block + inline markdown, artifact tokens,
- `subtreeWords` — Pure: word count over a SCOPE of `node`. The scope is the shared depth vocabulary (UXP-159):

## outline constraints / lint (F2)

- `CHECK_KEY` — A point may carry a reserved `check` PROPERTY whose value is an evalMath boolean
- `ALIAS_KEY` — Reserved property holding a point's alternate names (comma-separated) so the
- `REPEAT_KEY` — Reserved property holding a recurrence phrase for a dated to-do (#462). Same model as

## per-name appearance config (#464) — pure display, keyed by name

- `APPEARANCE_COLORS` — root.appearance = { tags: {tagname → colorName}, props: {propKey → iconName} }. Display only,
- `APPEARANCE_COLOR_ALIAS` — Retired swatch names map to the nearest accent hue on load, so existing tag colors survive the
- `APPEARANCE_ICONS` — Curated FA icon shortlist — ONLY glyphs already in the embedded subset (no CDN / no rebuild).
- `isValidTagName` — A valid hashtag name: `[\w-]+` segments joined by `/` (mirrors the mdInline / collectTags
- `tagColorOf` — The swatch color for a tag, or null. Hierarchical like tag search: a color set on `thread`
- `propIconOf` — The FA icon configured for a property key, or null. Pure.
- `setTagColor` — Set (or, with a falsy value, clear) a tag color / prop icon on the appearance config. Mutates
- `setPropIcon`
- `normalizeAppearance` — Load-time validator: keep only well-shaped {tags, props} of name→known-value. Never throws.
- `CREATED_KEY` — Reserved timestamp properties (#467): `created` is stamped once when a point first gets
- `TIMESTAMP_KEYS`
- `nowStamp` — A local wall-clock ISO datetime to the minute. `d` is injectable for tests.
- `parseStamp` — Parse a stored timestamp to epoch-ms for sorting/compare, or null if absent/unparseable
- `timestampOf` — This point's `created` / `edited` timestamp epoch-ms, or null. (timestampOf(node, key))
- `RECENT_EDIT_MS` — The window `is:recently-edited` matches: the last 48 hours. `recentEditNow` is a seam so
- `_recentEditNowOverride`
- `recentEditNow`
- `checkExprOf` — This point's `check` assertion string (the reserved property's value), or null.
- `repeatOf` — This point's `repeat` phrase (the reserved property's value), or null.
- `nodePropVars` — The point's OWN numeric properties as a { name: number } overlay, so a bare name
- `resolveNodeScope` — The merged { name: number } variable scope visible AT a node — the ONE place the two
- `evalCheck` — Evaluate a point's `check` constraint → 'pass' | 'fail' | 'error' | null.

## custom calendars (#527, Tier 1)

- `activeCalendar` — The epoch-day INTEGER is always the source of truth; a calendar only changes the
- `CAL_GREGORIAN` — Pure core: given a node's ancestor-id chain (root-first … node-last, ids only), which calendar-id
- `resolveCalendarId`
- `namedCalendars` — The extra named calendars keyed by id (never the default, which lives on root.calendar). Each is
- `normalizeCalendarBindings` — Guarantee the {calendars, gamelog} shape on a root and re-validate each named calendar (mirrors
- `calendarForNode` — The calendar a given live node's dates resolve under — the per-scope override on activeCalendar().
- `journalHomeId` — The current Journal home's id WITHOUT creating one (findOrCreateJournalHome mutates). Read-only.
- `CAL_EPOCH_MAX` — Every valid fiction epoch lives inside this window (±~27k years of epoch-days). Outside it,
- `normalizeCalendar` — Validate + normalize a calendar def; returns null (= Gregorian) if unusable. Pure.
- `calYearLength` — Days in one full year of a fixed-table calendar. Pure.
- `epochToCal` — epoch-day integer → { year, month (1-based), day (1-based), weekday (0-based) } for a
- `calToEpoch` — { year, month, day } → epoch-day integer for a fixed-table calendar, or null if the
- `calEraYear` — The display year for a given era (default = the calendar's intrinsic year). Pure.
- `calYearFromEra` — Inverse of calEraYear — a user TYPES a display year ("1005 DR"); the parse layer MUST convert
- `calWeekdayName` — Name resolvers for the LABEL layer (RISK-1). A custom calendar may name none, some, or all of
- `calMonthName`
- `calComponents` — The COMPUTE seam for evalMath's date component fns (year/month/day/weekday/quarter). Pulling a

## the render-label layer (#527 PR-A): every grid/tick/aria label the calendar touches

- `calWeekLen` — The week's column count — THE grid-geometry question, asked by CSS var setters + keyboard nav.
- `calWeekHeads` — Header pairs for a day-of-week row: short = the 2-char visual eyebrow, full = the aria-label.
- `calMonthTitle` — The month-pane title: "July 2026" | "Hammer 1005" (era-display year, matching every other label).
- `calDayLabel` — A full spoken date for aria-labels: "Sunday, July 12, 2026" | "Sul, Hammer 12, 1005". Pure.
- `calDayShort` — The compact tick/hover form: "Jul 12" | "Hammer 12" (fiction month names stay whole — slicing
- `dueWindowDays` — The search-window span for `due:week` / `due:month` (QX-2). "This week / month" means today
- `dueDateToday`

## the Calendar dialog line grammars (#527 PR-C)

- `parseCalMonths` — The dialog authors a calendar as TEXT (one month per line, the seq/qbase/data-pack
- `parseCalWeek` — One line, three forms: "10" (an unnamed 10-day week) | "Sul Mol Dul" (length = the name
- `parseCalEras`
- `calendarToText` — The inverse: an existing calendar back into the dialog's field texts (for editing). Pure.

## the calendar-switch audit (#527 PR-C, adversarial review #5)

- `auditCalendarSwitch` — Dates persist as STRINGS re-parsed per render, so changing root.calendar silently

## journal / daily-notes pure cores

- `todayISO` — todayISO() — pure, but depends on current time. Returns 'YYYY-MM-DD'.
- `journalFileName` — journalFileName(iso) — pure, Node-testable. One file per day: iso + '.opml'.
- `isoParts` — Split a 'Y-M-D' label into its three rungs. A NEGATIVE display year is legal under a fiction
- `findOrCreateChild` — findOrCreateChild(parent, label, mk, fuzzy) — find-or-create one direct child.
- `findOrCreateDatedEntry` — findOrCreateDatedEntry(home, iso, mk) — pure tree operation, no DOM, Node-testable.
- `dateFormsHint` — Parse a due-property value string → epoch-day number, or null on failure.
- `parseDueDate` — Pure for absolute dates; depends on current time only for relative forms.
- `formatDueDate` — Turn an epoch-day into { iso, label, state } for display.
- `formatDateConcrete` — A concrete, unambiguous date label for a CONFIRMATION context (e.g. the capture strip's
- `parseDateSlash` — parseDateSlash(query) — pure, Node-testable. The keyboard bridge for scheduling
- `parsePropArg` — parsePropArg(rest) — pure. The payload half of a /prop one-shot: "key=value" or "key:value" →
- `parsePropSlash` — parsePropSlash(query) — pure. The /prop:key=value (or key:value) one-shot inline bridge. The slash
- `propWriteRoute` — propWriteRoute(key, val) — pure. #1121: which WRITER a /prop:key=value payload belongs to.
- `parsePropLines` — parsePropLines(text) — pure. #948: a multi-line block of `key: value` (or `key = value`) lines,
- `parseBaseSlash` — parseBaseSlash(query) — pure. LEAN FLOOR: /base:RxC (e.g. /base:3x4) → { rows, cols } clamped to a
- `setDateProp` — Set (or, with a falsy val, leave untouched) one date property on a node, replacing
- `stampEdit` — #467: stamp the point's timestamps. `created` is set only if absent (first content);
- `SLASH_ARG_VERBS` — Opted-in /-verbs that take a typed ":value" (the keyboard bridge that skips the
- `parseSlashQuery` — parseSlashQuery(rawQuery, trigger) — pure, Node-testable. Splits a captured slash
- `setCheckProp` — Set (or, with an empty val, clear) the reserved `check` constraint property.
- `setAliasProp` — Set (or clear) the reserved `aliases` property from a comma-separated string.
- `priorityRank` — Walk the tree and collect every point with a parseable "start" and/or "due"
- `collectDueDates`
- `dueItemOrder` — UXP-109: same date first, then higher priority (A < B < … < none) as the tiebreak, so the agenda
- `_addMemo` — #1099: the same gather over the FOLDER's reading set. wsAllDocRoots substitutes the LIVE root for
- `collectDueDatesFolder`
- `agendaScopeMessage` — The line the agenda shows when folder scope is on. Pure so it is pinnable without a browser
- `collectJournalDates` — Source 2 — JOURNAL: entries are dated by TREE POSITION (home 'Journal' > YYYY > MM > DD), not a
- `collectLoreDates` — Source 3 — LORE: a point carrying a `when:` or `date:` property = "this happened". A neutral
- `collectChronicleDates` — Source 4 — CHRONICLE (#652): the in-world game log. Like the journal it is dated by TREE POSITION
- `collectTimelineItems` — The timeline's date-sources merged and tagged for filtering. Tasks keep their urgency fields;
- `collectActions` — UXP-112: the agenda's companion to collectDueDates — UNDATED actionable to-dos (the GTD
- `calendarMonthGrid` — Pure: given any epoch-day, return the 42 epoch-days (6 weeks × 7 columns) of
- `GANTT_MAX_DAYS` — Pure: build the Gantt layout for the agenda Timeline view. Each dated item
- `agendaGantt`
- `addMonths` — Pure: shift an epoch-day by whole months, clamping the day to the target
- `agendaMonthCells` — Pure: map dated items onto a month's 42-cell calendar grid for the agenda
- `agendaWeekCells` — Pure: build the Week view — 7 day cells (Sun→Sat) for the week containing weekEpoch,
- `agendaDayStats` — Pure: for a day's items, split into active-first order (done sinks) and a {todo, done}
- `addWeeks` — Pure: shift a week by `delta` weeks — 7-day steps under Gregorian, week.length-day steps

## recurring tasks (#462)

- `_WD` — A reserved `repeat` property (same model as due/start/check) holds a tiny recurrence
- `_UNIT_ALIAS`
- `parseRepeat`
- `nextOccurrence` — Given a repeat descriptor and the occurrence's OWN date (the old due, fixed-cadence base),
- `_WD_NAME` — Human-readable phrase for a repeat descriptor (dialog preview + toast). Pure.
- `describeRepeat`
- `ordinalSuffix` — 1→st 2→nd 3→rd 4→th … 11-13→th. Pure.
- `urgencyMark` — Pure: a non-colour urgency cue prefixed onto agenda labels (the visual twin of
- `agendaState` — Pure: the urgency state the AGENDA should render for an item, given done-ness.
- `agendaLabel` — Pure: the agenda badge label for an item. A done item's "Nd overdue" is wrong (the
- `continuationPrefix` — start with so the format continues (a new to-do gets its `- [ ]`, a new quote
- `migrateTodoText` — Legacy migration: a `_type="todo"` node from before the markdown-first refactor
- `codeNodeHTML` — Render a whole-node code block (node.type === 'code'). The text is `\`\`\`` +
- `INLINE_CTL` — #1470: the two attributes every focusable control rendered INSIDE a point carries, as ONE string
- `renderContentHTML` — Render a node's display HTML with its dice/markov/grammar data in scope so
- `countVarRefs` — Count display-only `{name}` references to a variable across the document (the
- `typedVarDeclSnapshot` — Snapshot a node's TYPED variable declarations as name→displayValue, for before/after
- `warnVarDeclRipple` — Ripple warning for a typed `{name := expr}` declaration edited in this node (O1a):
- `shadowedDeclKeys` — Identify shadowed declarations POSITIONALLY (Stage B) from the ordered events stream
- `exprNamesVar` — Walk the entire tree in document order, evaluating each [[var:KEY]] token's
- `orphanedVarRefCount`
- `freezeOrphanNote` — #1146: the sentence appended to the freeze's own confirmation. Empty when nothing broke, so
- `collectVars`
- `_varMapAtCacheVer` — Positional variable resolution (Stage B): the map of variables visible AT a given node,
- `varMapAt`
- `collectNodeDecls` — Pull a node's variable DECLARATIONS (name → expr, with frozen pick value) into the
- `_posVarMapCacheVer` — #767: per-pill POSITIONAL variable maps for a point that declares a variable in its OWN text.
- `renderPosVarMaps`
- `VAR_DISTS_KEY` — #952: THE DISTRIBUTION LANE. A variable may hold an uncertain quantity ({cost := 100 to 200}),
- `varDistsOf`
- `attachVarDists`
- `varDistRec` — A distribution record for a name visible in `vars`, or null. Positional by construction: the
- `resolveVarDefs` — Resolve a name→expr `defs` map (+ frozen `pickVals` + first-seen `order`) into a
- `migrateEmphasisText` — Add block prefix to nodes that have a type but no prefix in text (migration)
- `migrateNodePrefixes`
- `buildIndex`
- `deregisterSubtree`
- `nodeById`
- `parentOf`
- `ancestorsOf` — The chain of ancestor NODES above `node`, nearest-first (parent, grandparent, …),
- `pathTo`

# Markdown, generators & tables

## markdown

- `ZWSP` — Zero-width space used by insertLineBreak to anchor the caret on blank lines.
- `editableText` — Read a live contenteditable's text: token spans contribute their raw [[TYPE:KEY]]
- `restoreTypedRun` — Pure caret math for a trigger-menu apply: remove the trigger run (the trigger char
- `stripTriggerRun`
- `editableTextBlocks` — Block-aware serialization for the table "✏ markdown" editor. That editor is a plain
- `safeUrl` — reject URLs that could execute script; allow data:image only for <img>
- `isRemoteUrl` — #1325: a REMOTE image reference (http/https) makes a network request when it renders — it leaks the
- `isImageUrl` — #1327: does this reference clearly point at an image? A data:image, or a URL/path (has a scheme or a
- `imgSrcFromHtml` — #1327: the src (+ alt) of a clipboard/drag "copy image" payload — text/html that LEADS with an <img>.
- `webImagesAllowed` — Whether THIS document has opted in to loading web images (default off — the zero-network promise). A
- `urlHost` — The host of a URL for the consent copy ("load pictures from example.com"); the bare string on a miss.
- `webImageBlockedHtml` — #1325: the stand-in for a not-yet-allowed remote image. Clickable (and keyboard-operable) — it opens the
- `openWebImageConsent` — #1325: the consent prompt. Shown the first time a web image is added, or when a blocked placeholder is
- `maybeOfferWebImageConsent` — #1325: after a remote image is added, offer the opt-in right then (the "first time you add a web image"
- `imageDialogFields` — #1326: the shared fields for both Insert-image dialogs — an address, alt text, and a live thumbnail that
- `escHtml`
- `escAttr`
- `escQ` — escQ is used ONLY inside attribute values. It must be a full attribute escaper, not
- `EMOJI` — Common emoji shortcodes (:name:). Unknown codes are left as typed.
- `filterEmojiCandidates` — UXP-106: the emoji picker's candidate filter. Pure + Node-testable, mirroring
- `mdEmph` — Emphasis / highlight / sub / sup — runs on already-escaped text. Shared by the
- `_inlineDepth` — Depth guard + save/restore wrapper for rendering a target node's HTML inside
- `_inlineDocId`
- `MIRROR_ROW_CAP` — #917: subtree transclusion. A mirror token ALONE on its line renders the target's SUBTREE
- `MIRROR_LINE_RE` — #1142: the one test for "this line IS a mirror" -- a caption-less link, alone on its line. Shared by
- `mirrorSubtreeRows`
- `_mirrorBlockLine` — Set per line at mdInline entry (sync render, same module-global discipline as cookieNode):
- `_mirrorBlockSuppress`
- `renderNodeInline`
- `mirrorBlockHtml` — #917: the block-mirror BODY — the target's own rendered line (the head) plus its subtree
- `renderLinkPill` — inline markdown within a single line (text is NOT yet escaped)
- `renderCrossLinkPill` — Cross-document link pill (CF-2). A [[docId#nodeId|label]] token pointing at a point in
- `IMG_MODE_CLS` — #992 image sizing. The width modes ride the markdown title field — no new syntax (P5).
- `imgSizeParts`
- `mdInline`

## render-list context globals

- `diceRenderList` — Set by renderContentHTML before calling mdToHtml so that mdInline token
- `queryRenderList`
- `markovRenderList`
- `mathRenderList`
- `estRenderList`
- `varRenderList`
- `seqRenderList`
- `grammarRenderList`
- `cookieNode` — The node currently being rendered, so mdInline can compute a [/] / [%]
- `withHostScope` — withHostScope(node, fn) — run fn with cookieNode set to the host point, restoring whatever was
- `globalVarMap` — Collected variable map {name: value} for the current render; updated by
- `globalRuleMap`
- `renderVarMap` — Positional (per-node) variable map for the node currently rendering — the variables
- `renderPosVarMap`
- `_varsCacheVer`
- `_varCycles`
- `_varShadowedKeys`
- `_varActiveExprs`

## dice rolls

- `parseDice` — Parse "XdY" dice notation with +/- modifiers and multiple terms, e.g.
- `_sessionRng` — #1243 (deferred): the document session RNG. When a doc carries a seed, the FROZEN/on-action draws
- `srand`
- `applyDocSeed` — (Re)seed the session RNG from the current doc. Called on every doc load and whenever the seed is
- `setDocSeed` — Set or clear the document session seed and re-seed the RNG. A number activates reproducible draws;
- `rnd`
- `rollParsed` — Roll a parsed term list into {total, parts} — parts keep individual roll chains.
- `diceExprStr` — Canonical expression string, e.g. "4d6-2+5d12"
- `diceKey`
- `parseVersus` — #1243 opposed rolls. Split "A vs B" into its two dice-expression sides at the top level. Exactly
- `versusKind` — A parsed dice term list is a success POOL when any term counts successes, else a SUM.
- `versusSide` — Resolve ONE side of a contest to a total. A dice expression (with any +modifiers) ROLLS; a plain
- `versusSideKind` — The kind of one side WITHOUT rolling — 'roll' | 'fixed' | null — for classify/promote validity checks
- `versusValid` — A contest is valid iff BOTH sides resolve to a number — a roll, a constant, or a variable/formula. So
- `rollVersus` — Roll one contest: resolve each side (a roll collapses to a pip sum or success count; a fixed side is
- `makeDiceRoll` — Build a fresh roll record from raw user input; null if the formula is invalid.
- `diceBreakdownHTML` — Inline HTML for the formula breakdown — handles exploding, keep/drop, Fate, and
- `diceTotalStr` — Displayed total for a dice record — success pools show "N succ", Fate shows a
- `renderDicePill`

## markov chains

- `parseMarkov`
- `weightedPick`
- `walkMarkov` — Walk `steps` transitions from `start`; stops early at a state with no outgoing
- `markovKey`
- `markovParts` — markovParts(body) — sniff a typed inline markov chain: {markov: a→b, b→c, c→a 2}.
- `makeTypedMarkovRoll` — Build an ANONYMOUS, TYPED markov record from a {markov: …} body. Anonymous (no name)
- `makeMarkovRoll`
- `renderMarkovPill`
- `UL_RE`
- `OL_RE`
- `DD_RE` — TASK_RE is defined with the task-checkbox grammar block (search "single source of truth")
- `ATX_RE`
- `HR_RE`
- `FENCE_RE`
- `BQ_RE`
- `SPOILER_RE`
- `renderListItem` — One <li>; task-list items (`- [ ] foo`) render a clickable checkbox. taskNo is
- `mdToHtml` — Per-line block markdown → HTML. Each line is classified independently so a single
- `MD_SNIFF` — One sniff, twenty replaces: every transform below requires one of these — a char-anywhere
- `stripMd`

## tables (model ↔ markdown)

- `mtSplitRow`
- `parseTable`
- `serializeTable`
- `starterTableText` — Build a starter pipe-table markdown string: `rows` total rows (row 1 is the
- `planBaseConvert` — Pure decision behind the NON-DESTRUCTIVE /base convert (the data-loss fix). An
- `planTablePromote` — Pure decision behind "Convert to base" on a static table (§6 promote). Given the

## table formulas (Org-mode spreadsheet conventions)

- `TBLFM_PART_R`
- `TBLFM_PART_C`
- `TBLFM_REF` — A reference is `@…` (with optional `$…`) or a bare `$…`; never the empty string.
- `orgResolveComp` — Resolve one @row / $col component to a 0-indexed coordinate.
- `parseOrgRef` — Parse a single field reference token ('@2$3', '$3', '@-1') into a 0-indexed
- `parseTblfm` — Parse a TBLFM string into [{target, expr}]; target ∈
- `MATH_CONST_NAMES` — Bare-word evalMath atoms that are NOT document-variable references (they need no
- `mathErrorReason` — Classify *why* evalMath(expr, vars) returned null / non-finite, for the
- `MATH_VARIADIC_NAMES` — #1169: the argument counts a known math function accepts. FN1/FN2/FN3 are keyed BY arity and one
- `fnArities`
- `fnArityProblem` — #1169: a KNOWN function called with the wrong number of values. evalMath's parser already detects
- `arityPhrase` — #1169: name the function and a shape that works, in the #1159 house style ("name the fix, not
- `mathReasonPhrase` — A human phrase for a mathErrorReason code, so the math dialog, the check dialog,
- `cellNum` — What a base cell reads as a number when NO column role is set (#1469) — one rule, two
- `colIsNumeric` — Does EVERY non-empty cell in this column read as a number? The gate Sort gates its
- `computeTable` — Apply TBLFM formulas to a table model, returning a new rows grid with every
- `TBLFM_LINE_RE` — Split a table node's text into the grid portion and its trailing `#+TBLFM:` recipe.
- `extractTblfm`
- `stripTblfm`
- `remapColIndex` — Remap a stored 0-based column index across a structural column op, so metadata that
- `reindexTblfmCols` — Reindex the absolute 1-based $N column refs in a #+TBLFM recipe across a column op.
- `remapNodeColumns` — Keep a base's column-associated metadata aligned when a column is inserted, deleted, or
- `looksLikeCellFormula` — Detect a *spreadsheet-style* formula a user typed straight into a base cell —

## static markdown tables (render-anywhere, read-only)

- `tableDelimCells` — GFM table delimiter row detector — the false-positive guard. Returns the cell
- `renderStaticTable` — Render a detected pipe-table block (grid lines + an optional trailing #+TBLFM:

# OPML, commands & navigation

## OPML

- `headEl` — Doc-level config rides the <head> as underscore-prefixed custom ELEMENTS
- `headJSONArray`
- `toOpml`
- `pruneNoexport` — A root-shaped COPY with every `noexport` subtree removed, for the one serializer whose output
- `exportExclusionImpact` — What a shared copy loses to those exclusions, so the person exporting learns it BEFORE they hand
- `EX_SNIFF` — Encode newlines/tabs/CRs as numeric character references — XML attribute-value
- `ex`
- `MAX_OPML_DEPTH` — WAVE-3 HARD-11: max ingestion nesting. Well beyond any real document (deep outlines are a handful
- `treeDepthExceeds` — ITERATIVE depth check (a stack, so it can't itself overflow on the deep tree it's testing) — the
- `arrAttr` — Parse an underscore-prefixed sidecar attribute as an ARRAY of records, defaulting to
- `fromOpml`

## commands (unified)

- `BLOCK_CMDS` — Point-level commands — change WHAT THIS POINT IS. Triggered by "/".
- `DEFAULT_STATE_ICONS` — State entries for the / menu, generated per sequence (default + declared) so a
- `_stateCmdsCacheVer`
- `stateCmds`
- `slashActiveIdx` — The / pool: static block commands + the per-sequence state entries.
- `blockCmdsPool`
- `ROLL_TEMPLATES` — Inline inserts — drop a thing AT THE CARET. Triggered by "@".
- `PATTERN_RECIPES` — #1267: ready-to-use {…} recipes for the Builder's "Patterns" section. Each is a COMPLETE, already-
- `INSERT_CMDS`
- `FORMAT_CMDS` — Inline formatting — decorate a SELECTION. Lives on the selection toolbar.
- `FORMAT_SHORTCUTS` — Ctrl/⌘ + letter → the FORMAT_CMDS id it wraps the selection with. The standard editor

## icons: Font Awesome (subset, embedded) with a unicode fallback

- `FA_OK` — The FA webfont is inlined in the <head> as a base64 woff2 subset, so it is
- `FA_GLYPHS`
- `paintIcon`
- `setIcon` — `spec` is either a unicode string (fallback only) or {fa, fb}
- `refreshIcons`
- `mkCmdItem` — build a unified cmd-item element

## zoom / breadcrumb

- `commitActiveEdit` — Commit any in-progress edit (flush text + refold artifacts via exitEdit's blur
- `zoomInto`
- `zoomTo`
- `ZOOM_TITLE_MIN` — Fit the zoomed point's title inside its max height (--zoom-title-max). Shrink the font
- `fitZoomTitle`
- `followLinkTarget` — Follow a node link to (did, tid): zoom same-doc, switch+zoom cross-doc. The single
- `followNodeLink`
- `copyNodeLink` — Copy a ready-to-paste link token for a node onto the clipboard. Always uses the
- `toastGate` — Minimal transient confirmation toast (no persistent chrome). Self-styled so it
- `_hintTimer`
- `_errorHoldUntil`
- `_pendingHint`
- `bottomStackHeight` — UXP-116: lift the toast above any open bottom-docked panel (footnotes/backlinks/variables)
- `usableBottom` — The y a popup must not cross: the viewport bottom, less the 8px margin, less whatever the docked
- `flashBottom`
- `flashHint`
- `hintDwell` — #655: how long a hint stays up, by message length. Floor 1400ms (a short "Saved" confirmation),
- `flashError` — Error toast: same #flash-hint element as flashHint, but red-tinted and longer-lived so a
- `TOUCH_HINT_KEY` — One-time touch onboarding (D4): on touch there's no hover, so the bullet's press-hold
- `maybeShowTouchHint`
- `ptsLabel` — Confirmation-toast helpers (P4-3, UXP-12): every structural/destructive action
- `countPoints`
- `fallbackCopy`
- `zoomOut`
- `renderBreadcrumb`
- `crumbLabel` — A base has no prose title (its text is the table markdown), so show a clean label
- `mkCrumb`
- `mkSep`

# Render & bases

## render

- `render`
- `_lastRenderFocusedId`

## virtual list

- `pushRows` — pos/size are the node's 1-based position among its VISIBLE siblings and that
- `pushSearchRows` — `anc`/`states`/`needTags` are threaded exactly as computeMatchSet threads them, and for the same
- `flatten`
- `measureTops`
- `rowAtOffset` — largest index i with rowTops[i] <= y (clamped to valid range)
- `computeWindow`
- `renderWindow`
- `reconcileHeights` — measure rendered rows; if real heights differ from estimates, fix offsets/spacers
- `reconcileRaf`
- `scheduleReconcile`
- `forceIncludeId` — scroll the page so a given row sits comfortably in view, then re-window
- `_vlPreservingFocus` — true only WHILE renderWindow rebuilds around a focused editor: the removal-blur must not
- `ensureRowVisible`
- `winRaf`
- `appendGhostRow`
- `isVisible`
- `countHiddenDone` — Pure (mobile-neophyte review, MOBILE-2): count the checked to-dos anywhere in `tree` that
- `flashHiddenIfDone` — A checked to-do is filtered out of the outline while show-done is off, so the row
- `rollForwardRepeat` — Recurring tasks (#462): when a dated to-do carrying a `repeat` property flips to done,
- `reopenTaskText` — Flip a task's done marker back to open in node.text: uncheck every `[x]` task box and
- `reopenDoneKeyword` — If the first line leads with a DONE keyword, rewrite it to its sequence's first active
- `createChildModel`
- `olNum` — Returns the ol number for this node within parent

## interactive table widget

- `mtSel`
- `mtDrag`
- `mtReorder`
- `mtColResize`
- `mtGlobalInit`
- `mtNavSel`
- `mtPointerFocus`
- `MIN_COL_W` — Column width is a base view-property (node.colW: [px|null]) — markdown pipe-tables
- `clampColW`
- `COL_W_STEP` — LEAN FLOOR phase 3: the keyed column-resize step — the keyboard twin of the fine resize DRAG (the
- `stepColW`
- `mtHost`
- `mtInlineSetup` — Pre-set the render globals for a base node so every subsequent mtInlineRaw() call skips
- `mtInlineTeardown`
- `mtInlineRaw` — Render one cell's raw text through mdInline, assuming mtInlineSetup() was called
- `mtInline` — Standalone cell render — sets up & tears down globals (the pre-batch form).
- `mtSelectCellContents` — Select a cell's whole contents (type-to-overwrite) and remember the cell so the
- `mtSpliceCell` — Replace the [start,end) span of one cell's raw text, then repaint that cell.
- `mtMoveItem`
- `cardDropAfter` — Which side of a card the pointer is on. A grid reads left to right, so the horizontal midline is
- `reorderIndex` — Where an item lands when dropped on a target. The subtlety this exists to get right: removing the
- `qbaseColRoles` — Parse a base's stored text into its {aligns, rows} grid model (sans the #+TBLFM
- `inferColRolesFromModel` — #922: infer a display role for each column of an AUTHORED base from its data, so Board
- `_mtInferRoleCacheVer` — Memoized wrapper over the live node (authored bases only). Keyed like mtModelRead — a
- `inferredColRoles`
- `mtColRoles` — The one accessor every colRole READ site consults: authored bases keep the hand-set
- `mtCellHtml`
- `mtModelText`

## variable base (rows project as document variables)

- `varBaseName` — Pure: one addressable name segment from user cell/header text. Lowercase, runs of
- `isVarBase` — Pure over the node: project a variable base's rows into variable defs.
- `varBaseDefs`
- `_varBaseDefsCacheVer` — Memo: the parse + classify pass must not run once per varMapAt call (collectVars
- `varBaseDefsMemo`
- `varBasePreview` — Pure: what WOULD this base project under a candidate name? The dialog's live preview

## query base (QP-2 Phase A)

- `_qbaseCache` — A base carrying node.qbase = { expr, cols:[{name, field}] } sources its rows from
- `_queryPillCache` — Query-PILL row cache (#451 item 4): renderQueryPill runs a full-document queryRows on
- `queryRowsMemo`
- `QBASE_ROW_CAP`
- `parseQBaseSort` — SV-2: parse the query editor's sort field against the column list. Blank -> null
- `isQueryBase`
- `qbaseColList` — The rendered-column list: entries with a usable field, exactly as queryTableRows
- `qbaseFieldWritable` — Phase C: a column WRITES THROUGH when it projects a plain property key — the exact
- `qbaseModel`
- `mtModel`
- `_mtModelReadCacheVer` — Read-only twin of mtModel for PAINT paths (widget build, cell patch, focusin): they
- `_mtEditSession`
- `_mtCellEditStart`
- `mtModelRead`
- `mtCommit` — Serialize the grid back to node.text, preserving the trailing #+TBLFM: recipe (Org
- `mtRecompute` — Re-run the table's formulas, baking computed display values into the target cells of
- `mtCommitEpilogue` — Repaint every computed (non-focused) cell from the current model without rebuilding
- `mtPatchCells`
- `BASE_VIEW_KINDS` — #1383: put focus back on the SAME control after a base rebuild. refreshTable replaces the whole
- `BASE_CHROME_CLASSES`
- `BASE_CARD_CLASSES`
- `baseRefocusSelector`
- `baseFocusDescriptor` — The DOM half: read the descriptor off the element that has focus. Escapes `nid` here so the core
- `refreshTable`
- `repaintNodeContent` — Repaint a node's display after an artifact record changed. Base nodes hold a
- `handleCellPill` — Display-mode pill click inside a base cell: pencil opens the editor, body

## single-assignment TBLFM editing (UXP-3 Part B)

- `tblfmGetAssign` — Both operate on the canonical lhs string ('$3' column · '@2$3' field), exact
- `tblfmSetAssign` — Set/replace the assignment for lhs; an empty expr removes it. Returns the new
- `mtBuildAggFormula`
- `mtHasFooter` — True when the TBLFM contains at least one @>$N= footer-row formula.
- `mtColAggKind` — Return the current aggregate kind ('sum'|'avg'|'count'|'min'|'max'|'none')
- `aggKindLabel` — Display label for an aggregate kind — what the Calculate footer shows beside the
- `mtApplyAggregate` — Apply an aggregate function to colIdx (0-based). Adds/removes the footer row
- `mtSetFormula` — Set (or clear, when expr is empty) one TBLFM assignment and recompute (UXP-3
- `openFormulaDialog` — The formula dialog (UXP-3 Part B): authors one column/cell formula with the

## column model ops (reuse the same model logic the header gestures used; now

- `effectiveAlign` — exposed as explicit, keyboard-reachable panel actions) ───────────────────
- `mtSetAlign`
- `baseStructMessage` — #1400: what a structural base op says when it lands. Pure, so the six sentences are pinned as
- `COL_W_PRESETS` — #1415: the width presets, ONE home. The Column menu builds its rows from this and the message
- `colWidthLabel`
- `mtColLabel` — #1400: the header name a message should use for a column, or '' when it has none.
- `mtColValueCount` — How many non-empty DATA cells a column or row carries -- said out loud before a delete, so a
- `mtRowValueCount`
- `mtInsertCol`
- `COL_ROLE_CYCLE` — Set/clear a column's display role (FR-1). Lazily sized like colW; an all-null
- `cycleColRole`
- `mtSetColRole`
- `mtMoveCol`
- `mtDeleteCol`
- `mtSetColWidth` — Set (or clear, with w=null) a column's pinned width and re-render the grid.
- `mtSetColFmt` — Set (or clear, with f=null) a column's number format (ε). A parseNumFmt record
- `mtApplyColWidthLive` — Live width during a drag: paint every cell in the column without a rebuild

## row model ops (mirror the column ops; surfaced in the Row menu)

- `mtLastDataRow` — All are footer-aware: the Calculate footer (when present) is the data-only last row,
- `mtMoveRow`
- `mtInsertRow`
- `mtDeleteRow`
- `mtPanelReturn`
- `hideColPanel`
- `_mtPanelOpenGuard` — One-shot dismissal guard (#811). Menus opened from a MOUSEDOWN-activated control
- `mtGuardPanelDismiss`
- `mtMenuBuilder` — Shared builders for the column/row dropdown menus (§7.1 menu pattern: role=menu/
- `mtOpenMenu`
- `showRowPanel` — The consolidated Row menu: Insert · Move · Delete — the lean replacement for the
- `showColPanel` — The consolidated Column panel: Calculate · Alignment · Width · Insert · Move · Delete.
- `attachBaseHeaderTouchMenu` — Touch door to the Column menu (#811): PRESS AND HOLD a column header. The mouse

## whole-base operations (surfaced on the base bullet's menu)

- `baseFrozenMarkdown` — Portable copy serializers (pure, text-in/text-out → unit-testable). "Frozen" bakes
- `baseRecipeMarkdown`
- `copyTextToClipboard` — Generic clipboard write + confirmation toast (navigator.clipboard with the same
- `mtClearSelection`
- `mtSelRect`
- `mtApplySelection`
- `mtFocusCell` — Keyboard cell navigation (UXP-2). Focus a cell by logical (r,c) and select its
- `mtSetRovingCell` — Roving tabindex for the base grid (#443, the ARIA grid pattern the calendar already

## base view system (BV-1)

- `mtViewSwitcherHtml` — node.view = { kind: 'board', groupBy: colIdx } (absent = table view, today's
- `wireViewButtons` — Wire the view-switcher buttons (mousedown + preventDefault per the caret invariant, Enter/Space
- `mtRefreshViewSwitcher` — #939: repaint the view-switcher strip IN PLACE so Board/Calendar enable/disable follows the
- `statusColWarning` — #1114: WHICH half is missing, for the Board refusal. One message used to cover three different
- `boardBlockReason`
- `mtSetView`
- `mtBaseChromeHtml` — The strip(s) above a base's grid or board: the query strip (query bases) and the
- `sortReading` — #1469: which reading a Sort will apply to one column — the user's role when they set one,
- `SORT_READING_COPY` — What that reading is CALLED, in the user's words. `as` names it in the menu section; `asc`/
- `sortReadingCopy`
- `capFirst`
- `mtSortOrder` — SV-1: sort a base's data rows by one column, role-aware — numeric compare for a
- `mtSortRows`
- `mtSortBase` — The one-shot sort door (saved-views-proposal SV-1): a DATA operation — the new order
- `mtWireQBaseEdit` — QP-2 Phase C: write-through cells. A query base's plain-property cells edit the SOURCE

## FR-1 per-role cell editors

- `_cellPopEl` — While a Date- or Status-role cell is focused for editing, a popover under the cell
- `hideCellPop`
- `showCellEditorPop`
- `scheduleCellPopHide` — The deferred hide shared by both cell focusout paths: only a genuine focus-leave
- `mtSetCellValue` — Write ONE cell's value through the correct commit path — the keyboard door's writer
- `mtWireBaseChrome` — Wire the shared base chrome (query strip + view switcher + the query base's
- `mtAddRowsCapSection` — The rows-cap section: All / 5 / 10 / 20, applied to node.baseRows (null = all).
- `showBaseRowsMenu` — The rows-cap menu (the chrome button's pointer path). Reuses the shared
- `showBaseSettingsMenu` — The unified whole-base settings menu (bases round 3): view + outline rows cap in
- `mtColumnLabel` — Pure: the user-facing label for a base column — the header cell's text with
- `showBaseColumnsMenu` — The bullet-menu door to per-column ops (#811): list the base's columns; picking
- `mtCollapsedHost` — Move one card (data row) to another lane: write the keyword into the groupBy
- `bvMoveCard`
- `showCardMenu`
- `buildBoardWidget` — The board view (BV-1): lanes are the owning sequence's states in declared order,
- `buildCardsWidget` — The cards view (BV-2): a reading view. Every data row is a card in a responsive
- `mtWireCardOps` — #955: the card-level structure ops for the gallery — reorder, add, delete. Every one is a DOOR
- `showGalleryCardMenu` — The card's own menu. Driving showed why this is not showColPanel: handed a rowIdx it opens with
- `mtMoveRowTo` — Move a row to an absolute index. mtMoveRow only steps by ±1 (it is the menu's Up/Down), and a
- `_calMonthByNode` — The calendar view (BV-3): the FR-1 date column places each row on a month grid.
- `buildCalendarWidget`
- `buildTableWidget`
- `mtWireCells` — #955: the cell edit/nav layer, lifted verbatim out of buildTableWidget so a NON-TABLE view can
- `mtInitGlobal` — global gesture + keyboard handlers for table cell selection (attached once)
- `_descCache` — Total descendant count for a node's collapse-count badge. Memoized on _varsVer (#451
- `countDescendants`
- `renderRow`
- `appendNoteIndicator` — The hidden-note indicator: a tiny trailing glyph inside the display-mode

# Notes, properties & dialogs

## per-point notes

- `noteEditId` — A secondary plain-text block under a point (`node.note`): muted display,
- `buildNoteRow`
- `NOTE_ICON` — The note glyph everywhere it appears (gutter mark, hidden-note indicator):
- `PROPS_ICON`
- `buildNoteEl` — The note editor element itself (no row wrapper) — shared by the outline row
- `commitNote`
- `openNoteEditor` — The bullet-menu door: open (creating if absent) the note editor for a point.

## properties

- `hasVisibleProps` — #494: does this node produce ANY visible props chip? buildPropsArea skips the
- `buildPropsArea` — Build just the .props-area chip list (shared by outline row and zoom view).
- `_checkVerdicts` — Last-seen check verdict per node id — so a re-eval that FLIPS the verdict (a child
- `buildCheckChip` — The constraint chip: evaluates the point's `check` and renders a pass/fail/error
- `buildTimestampLine` — #467: a muted "Created … · Edited …" line for the zoom detail view, or null if the point
- `formatStampDisplay` — A stored ISO stamp → a readable "Jul 10, 2026, 2:32 PM"-ish label (locale-formatted).
- `buildPropsRow` — Full outline row: invisible gutter clones + the props-area.
- `openPropsDialog` — Properties editor dialog — opens from bullet menu or chip click.

## the Custom calendar dialog (#527 PR-C)

- `CAL_PRESETS` — Two generic, original example calendars (P2: a preset is the field-format tutorial).
- `buildCalendarFromFields` — Assemble a calendar object from the dialog's five field strings, or null when any field
- `applyCalendarChange` — THE one path for every root.calendar change (#527 review: gating the audit on the Activate
- `openCalendarAuditConfirm` — The proportional warning (P4: never a silent reinterpretation). Counts, the first few
- `showAdvanceMenu` — The campaign-clock advance menu (#527): "time passes" in the units a table actually uses.
- `openCalendarDialog`

## the Custom units dialog (SR-6 / #875)

- `UNIT_PRESETS` — File → Custom units. Text-first authoring on the same openInsertDialog scaffold as the
- `openUnitsDialog`
- `applyUnitsChange` — THE one path for every root.units change: parse, snapshot for undo, store the resolved table

## inline date-picker calendar

- `CAL_MONTHS` — A reusable month calendar bound to a date text input. The caret stays in the
- `CAL_DOW`
- `CAL_DOW_FULL`
- `DATE_KEYS` — Property keys that hold a date — these get the inline calendar in the props dialog.
- `buildDatePicker`
- `attachDateCalendar` — Wire an inline calendar to a date text input. The calendar lives in `host` and

## schedule dialog

- `DUE_ICON` — A two-field dialog for setting / clearing the "start" and "due" properties — a
- `openDueDateDialog`

## outline constraint / lint dialog (F2)

- `CHECK_ICON`
- `openCheckDialog` — Edit a point's `check` constraint with a live preview that runs evalCheck against

## aliases dialog

- `ALIAS_ICON`
- `openAliasDialog` — Edit a point's alternate names (the reserved `aliases` property, comma-separated).

## templates

- `TEMPLATE_ICON` — Save a point's subtree as a named, reusable template (doc-level config on
- `openSaveTemplateDialog` — Save-as-template door (bullet menu): name dialog defaulting to the point's text.
- `isSavedTemplateName`
- `openAddToPackDialog` — #518: "Add to data pack" — capture a subtree into a shareable pack (the authoring lever). It
- `openTemplatePicker` — /template door: a picker dialog listing saved templates; click stamps one here.

## data-pack manager (#487)

- `PACK_ICON` — The front door for declarative data packs (the engine — root.plugins + mergePackRules +
- `_packEditId`
- `_packDraft`
- `_packFocusId`
- `_packDrafts` — UXP-244: unsaved editor input, keyed by pack id, so dismissing the editor no longer destroys what
- `_packReadFields` — Set by buildPackEditView to read its live fields; null while the list view is up. Both exits
- `PACK_NEW_DRAFT_KEY` — Which slot a pack's draft lives in. A SAVED pack keys by its own id. An uncommitted new pack
- `packDraftKey`
- `stashPackDraft`
- `packUnderEdit` — Resolve the pack under edit — the in-memory draft (a fresh New pack) or one on root.plugins.
- `openDataPackManager`
- `buildPackListView`
- `buildPackEditView`
- `parsePackVarLines` — Parse the edit view's vars textarea ("name = expr" per line) → { vars:[{name,expr}], bad:[lines] }.
- `packVarsText` — UXP-244: a pack's stored var records rendered back to their authoring text — the inverse of
- `packDraftFrom` — UXP-244: is there anything in the editor worth keeping when the user dismisses it? Pure.
- `packFieldsFor` — UXP-244: what the edit view SHOWS — a kept draft when there is one, else the saved pack. Pure, so
- `rollPackPickVars` — Freeze every classified pick var to a rolled value at COMMIT time (#585). Given the parse
- `exportDataPacks` — Export root.plugins as a downloaded JSON file (the { plugins:[…] } wrapper parsePackImport reads).
- `importDataPacks` — Import data packs from a chosen JSON file. parsePackImport is the trust boundary (valid packs

## appearance config dialog (#464)

- `APPEARANCE_ICON` — One "Appearance" dialog (File menu) to give a tag an on-brand color and a property key an
- `_apprFocus`
- `openAppearanceDialog`
- `openWorkspaceSwitcher` — The document switcher (brief #5b): an io-card list of the connected folder's .opml docs.
- `openSeedDialog` — Broken-links report (backlog #4): a maintenance view of every dangling link — same-doc AND, when a
- `showBrokenLinksReport` — folder is connected, cross-doc — the first consumer of the pure collectBrokenLinks. Mirrors the
- `deleteFootnote` — Phase B: remove a footnote from the doc store. Undoable (pushUndo). Deleting a REFERENCED footnote
- `showFootnotesReport` — Phase B: the Footnotes manager. A File-menu report (the Broken-links family), built on the shared
- `openCitePicker` — Phase C: the "cite an existing footnote" picker — a P2 front door for reuse from the @ menu. Lists
- `stampTemplate` — Stamp a fresh copy of a saved template as a new point. Replaces the invoking

# Refile, capture & strips

## refile

- `pickerTitle` — The one display label for a point inside any picker / navigator: a base shows a
- `treeRows` — Pure model behind the point-tree navigator (the reusable tree powering the refile
- `tagTreeRows` — Pure model behind the TAG BROWSER (the tag-tree front door). Turns collectTags' flat dotted
- `resolveRefileTarget` — Move moveId's subtree under targetId (becomes targetId's last child). targetId ===
- `refileNodeTo`
- `TP_INDENT` — Render a treeRows() result into `listEl` (role="tree"). The reusable DOM half of the
- `renderTreeRows`
- `buildTreePicker` — Modal point-TREE picker (supersedes the old flat list): a search box over the
- `selectionRoots` — UXP-133: the top-most selected subtrees — drop any selected node that has a selected ANCESTOR,
- `refileNodesTo` — UXP-133: move several subtrees to one target in a single undo/render/toast. Skips a target that
- `openBulkRefileDialog` — UXP-133: the bulk refile picker — the same tree navigator as single refile, excluding all
- `openRefileDialog` — The refile picker: a modal tree navigator. "Top level" is the leading option
- `openTagBrowser` — The TAG BROWSER (File menu → Browse tags): a front door onto the hashtag namespace, which is

## capture / quick inbox

- `MAX_INBOXES` — Fast-add a point into a designated inbox from anywhere, without navigating there.
- `captureDraft`
- `captureCount`
- `captureSlot`
- `captureDue`
- `inboxAt` — Slot N's node (or null). Prunes a stale id (point deleted) lazily on read.
- `resolveInbox`
- `inboxSlotOf`
- `setInboxSlot` — Set slot N to a point id (extending the list with empty slots if needed, capped at 10).
- `removeInboxSlot`
- `reorderInboxList` — Reorder the inbox list so a chip's SLOT NUMBER (which capture key it answers to) changes: pull the id
- `moveInboxSlot`
- `captureOpen` — Capture lives in a TOOLBAR STRIP (like the agenda), not a modal — you can insert without
- `captureManage`
- `openCaptureDialog` — Open (or re-target) the strip for `slot` (default: first non-empty slot, else 1). If a slot
- `captureReturnFocus`
- `closeCapture`
- `toggleCapture`
- `normalizeCaptureSlot` — Resolve which slot to target: an explicit slot if given; if that slot is empty, try to
- `captureCurrentPointId` — The point a slot-shortcut adopts when its slot is empty: the point being edited, else the
- `captureTargetSlot` — While the dialog is open, a slot shortcut MOVES the selector (no reopen): retarget the
- `renderCaptureStrip` — Build the strip: one compact row [▾ dest] [input] [Capture]. The destination ▾ toggles a
- `autoGrowCapInput`
- `capInputKeydown`
- `buildCaptureManager` — The in-strip destination manager (second line): a chip per configured inbox to target it,
- `captureAddInbox` — Add-an-inbox: the tree picker (a modal is fine for this rare, one-off pick of WHICH point
- `doCapture`
- `appendTextToInbox` — Append one markdown-aware point (like doCapture) to the resolved inbox, or to the top
- `buildSharePointText` — Combine a shared body / title / url into one point's plain text (#465). The body leads; a
- `handleUrlAppend` — #465: URL-based capture. `?append=<text>` (and the PWA share_target, which routes shared

## journal strip: capture's twin for today's entry (DL §4: docked strips share one

- `journalOpen` — grammar). The date chip is segmented: year / month / day each navigate to that
- `toggleJournalStrip`
- `openJournalStrip`
- `closeJournalStrip`
- `renderJournalStrip`
- `openJournalLevel` — Zoom to the year (depth 1), month (2) or day (3) node of today's journal path,
- `doJournalAdd`
- `appendJournalPoint`
- `findOrCreateNamedHome` — Shared "dated log" home resolver (product-identity §8a substrate): a user-designated target
- `findOrCreateJournalHome`
- `resolveRollLogHome` — The roll-log home (#918). Same substrate as the journal: an explicit target or a top-level
- `ROLL_LOG_HOME` — UXP-245: the roll-log home IF one exists — this never creates it. resolveRollLogHome does the
- `ROLL_LOG_HOME_LEGACY`
- `rollLogHome`
- `rollLogToggleMessage` — UXP-245: what toggling roll logging says. The old copy always promised "your Rolls log" — a
- `rollLogFirstEntryMessage` — UXP-245: the ONE confirmation a session gives that a roll was recorded, fired on the first logged
- `rollLogEntry` — Pure: one log line, `HH:MM · <source> → <result>`. The source (the pill's expr / def / name) is
- `logRoll` — #918: append ONE roll's result to the roll-log home when logging is on. This is the single
- `_rollLogged`
- `_rollLogAnnounced`
- `grammarLogSource` — The roll-log source label for a grammar pill: a NAMED grammar/table logs its callable name; an

## chronicle strip (#652): the journal's in-world twin. The journal logs the REAL day

- `chronicleOpen` — (todayISO, observed); the chronicle logs the day the GM SETS — a movable in-world cursor,
- `chronicleHome` — The bound in-world-log home node, or null when nothing is bound (the strip has no target then).
- `chronicleCal` — The chronicle's calendar object (from the named store), or null (Gregorian) as a safe fallback.
- `chronicleCursor` — The current in-world cursor (epoch-day integer). Seeds from the calendar's `current` (its "today
- `syncChronicleButton` — Show/hide the toolbar Chronicle button: it only exists when an in-world log is bound (nothing to
- `toggleChronicleStrip`
- `openChronicleStrip`
- `closeChronicleStrip`
- `renderChronicleStrip`
- `nudgeChronicleCursor` — Move the in-world cursor by whole game-days. The cursor is an epoch-day integer, so ±1 is
- `openChronicleDate` — Jump the cursor: zoom into the cursor date's entry (creating the path), so the GM can read/write
- `doChronicleAdd`
- `openJournalEntry`
- `cycleJournalMode` — Toggle where journal entries go: 'append' (a Journal home point in this document)

# Search & caret

## search

- `SEARCH_IS_VALUES` — The search-operator vocabulary — SINGLE-SOURCED here so the parser and the picker's
- `SEARCH_IS_RE`
- `SEARCH_FIELDS`
- `SEARCH_DATE_VALUES`
- `SEARCH_PRIORITY_VALUES`
- `SIDECAR_KEYS` — Parse a query string into terms: [{neg, kind:'text'|'tag'|'is', value}].
- `HAS_SIDECAR_FIELD`
- `parseSearchQuery`
- `SEARCH_IS_NEAR` — The words people reach for that are NOT is: values, each mapped to the one that is. Curated,
- `SEARCH_DATE_WORDS` — dateStillTyping(value) — is this date value a PREFIX of something valid, i.e. is the user still
- `dateStillTyping`
- `searchTermProblems` — searchTermProblems(terms, opts) — why an unreadable filter matches nothing, one entry per
- `termMatchesNode`
- `_searchBlobVer` — doc-cache: searchBlob (per-node; see DOC_CACHES) — the text-term haystack,
- `searchBlob`
- `queryMatchesNode` — AND of all terms; a negated term must NOT match. Empty term list matches
- `QUERY_ROW_CAP` — queryRows(expr, rootNode, hostId, cap) — the shared query-to-rows core: run a search
- `resolveSearchExpr` — Resolve any embedded {…} in a search expr so a query ARGUMENT can be dynamic — {count: {status}},
- `queryRows`
- `queryReduce` — {roll: <search-query>} — pick ONE random point matching the query, returned as its title
- `queryCountIn` — count("query") — the original cardinality form, now a thin delegate to the general reducer.
- `_qrfMemo` — 4c (cross-document-direction.md): the FOLDER-scoped reducer behind {= sum("query", prop,
- `queryReduceFolder`
- `_qpfMemo` — 4c: folder-scoped query ROWS — queryRows run over every doc in the reading set (own live,
- `queryRowsFolder`
- `stripQueryTags` — #558: the {roll: query} payoff should read as fiction, not as its own database key.
- `rollLabel` — pickFromQuery(expr, rootNode, hostId, ctxRoot) — pick one matching point title.
- `pickFromQuery`
- `queryHits` — #899: the POOL behind a roll, split out of pickFromQuery so the draw is testable without
- `pickOneHit`
- `queryHitsFolder` — #899: the FOLDER-scoped pool behind {roll folder: <query>} — the generative member of the 4c
- `pickFromQueryFolder`
- `queryTableRows` — queryTableRows(expr, cols, rootNode, hostId, cap) — the query-base row engine
- `searchHighlightNeedles` — The strings worth <mark>ing for a parsed query: positive text terms plus
- `nodeMatchesSearch`

## saved searches

- `toggleSavedSearch` — Toggle a query in a saved list: present (trim-exact) → removed, absent →
- `isSavedSearch`

## templates (pure cores)

- `upsertTemplate` — A template is { name, node } — a named subtree snapshot stored doc-level on
- `removeTemplate`
- `findTemplate`
- `mergedTemplates` — #583: the list the /template picker and stamp paths read — the union of PACK templates and
- `computeMatchSet` — Single post-order pass: record every node whose subtree (incl. self) matches,
- `highlightContent`

## content events

- `activeContentId`

## chrome excursions: caret restore

- `lastEdit` — Going to the search bar, the file menu, or the help panel mid-edit must be
- `chromeReturn`
- `armChromeReturn`
- `restoreChromeReturn`
- `rerollInEdit` — Re-roll a pill while its point is in EDIT mode (#450): capture the caret, run the reroll,
- `PILL_CLASS_TYPE` — #1116: which artifact type a rendered pill element is, so a DOM element can name the token it
- `pillTokenFor`
- `PILL_MENU_TIP` — #1116: the second half of the pill's tooltip in Guided. AP punctuation, no em dash.
- `pillTitleWithMenu` — #1373: join a pill's own tooltip to the menu tip WITHOUT doubling the sentence break. The tip opens
- `PILL_TITLE_SEL` — The pill-tooltip policy for a DOM subtree. Standard and Lean strip the "Click to re-roll/edit"
- `applyPillTitlePolicy`
- `REROLL_PILLS` — The generative re-rollable pills, data-driving the four identical mousedown branches (#450).
- `attachContentEvents`
- `attachSwipeIndent` — Swipe-to-indent for touch (single point). Bound on the row content; active only in
- `isQuotedLiteral` — The ONE quote-literal test the three lockstep functions share (#521/#522). A body is a
- `classifyBraceBody`
- `templateAttempt` — #916: a GLUE TEMPLATE body — nested balanced {…} groups optionally glued to bare literal
- `templateParts`
- `braceAttemptAnnounce` — A short, human reason a {body} that CLASSIFIES 'invalid' will not become a pill — the tooltip
- `braceAttemptReason`
- `braceWouldPromote` — Would a {body} promote to an artifact pill on exit? (Thin doc-context wrapper —
- `braceTypeLabel` — What pill type will a valid {body} become? Returns [pillType, detail|null].
- `highlightGrammarText` — Escape a plain-text segment for edit mode, wrapping each top-level {…} that
- `EDIT_ANCHOR` — A sentinel placed right after each ATOMIC pill span (contenteditable=false) in
- `anchorEditInlines`
- `editModeHTML` — Build the innerHTML for a node in edit mode: plain text segments are HTML-escaped
- `enterEdit`
- `_suppressPruneOnFlush` — When applyInlineReplace flushes the active edit mid-insert (its `ed.blur()`), the
- `_pendingFullRender` — Set when a STRUCTURAL change happens mid-edit (e.g. link-and-create adds a new
- `exitEdit`
- `isComputedNode` — #801 (agent-review, UXP-194): repaint visible points whose DISPLAY depends on
- `repaintComputedDependents`
- `caretFromPoint`

# Input & interaction surfaces

## paste

- `handlePaste`
- `tableBlockEnd` — Shared GFM table-block detector (the ONE detector — reused by findFirstTableRange,
- `splitPastedPoints` — Split pasted text into point-strings, grouping each GFM table block into ONE entry so
- `parseMLlines`

## keydown

- `hideInlineMenus` — Close every caret-anchored picker + the brace preview in one call. The menus re-derive
- `INLINE_MENU_NAV` — The single-column inline picker menus, data-driving their identical ↓/↑/Enter/Tab/Escape
- `onKeyDown`

## multi-select

- `clearSelection`

## #1490: the row cursor, said out loud and reachable

- `rowCursorSay` — Escape out of an edit lands on <body> by design — the ladder's last rung IS "blur", and focus
- `moveRowCursor` — Put the row cursor on a point and say so. One writer, so the arrows, the Escape rung and any
- `updateSelVisuals`
- `_nsbSaid`
- `updateNodeSelBar`
- `toggleSelect` — Cmd/Ctrl-click: add or remove ONE point from the selection (out-of-order, non-
- `rangeSelectTo`
- `flatRowStep`
- `getTopLevelSelected`
- `deepCloneNodeNewIds` — An internal [[#id|...]] link (a mirror/embed, or a same-doc jump) points at a node id, which the clone
- `remapClonedLinks`
- `_cloneNodeNewIds`
- `serializeNodesToText`
- `copySelection`
- `exportSelectionToMarkdown` — Export the selected points (top-level only, so a selected child isn't duplicated under
- `deleteSelected`
- `indentSelected`
- `outdentSelected`
- `pasteFromNodeClipboard`

## selection toolbar

- `selTb`
- `buildSelTb`

## touch edit bar

- `editBar` — While a single point is being edited on a touch device, show a strip of structural
- `editBarTargetId` — The point the bar currently acts on. `activeContentId` is the live edit target; we cache
- `updateEditBar`
- `positionEditBar` — Lift the bar to sit just above the on-screen keyboard. visualViewport shrinks when the
- `editBarMove` — A structural button: run the move, keep the point focused, refresh the bar's state.
- `ebBtn` — mousedown/pointerdown + preventDefault: act WITHOUT blurring the contenteditable (the caret

## touch quick bar (#566): the one-handed play loop

- `quickBar` — A bottom-anchored bar of display-mode actions on touch: capture, new point, new
- `quickBarHeight` — element lookup, not the const: flashBottom/syncBlPanelBottom may run during boot,
- `updateQuickBar`
- `quickNewPoint` — The bar's + is the ghost row's twin ("New point" means the same thing everywhere, P1):
- `rollPalette` — #520: the roll-template palette. A tap on a chip creates a new point, types the {…} template
- `qbRoll`
- `buildRollPalette`
- `openRollPalette`
- `closeRollPalette`
- `rollPaletteOpen`
- `pickRollTemplate`
- `recipeToNodes` — #1267: turn a PATTERN_RECIPES entry into model node(s). A line recipe is one point; a block recipe
- `insertRecipe` — #1267: drop a recipe as a working line/block. If the current point is empty it fills IN PLACE (no
- `selectedNodes` — Bulk edit a multi-selection the same way the bullet popup edits one point: the
- `applyTodoCycleToNodes` — LEAN FLOOR: apply a to-do text-mutator (cycleTodoState / cycleTodoPriority) to every node in a list,
- `bulkCycleTodo` — The keyboard bulk verb: cycle every SELECTED point's to-do state (or priority) with no picker/modal —
- `addTagToText` — #1285: bulk-tag-by-search — the migration on-ramp (Jules: "a bulk-tag-by-search is the difference
- `bulkAddTag`
- `openBulkTagDialog`
- `showSelTypeMenu` — A compact "turn into" menu for the selection — the same type-switcher row the
- `updateSelToolbar`
- `hideSelTb`
- `applyInlineFormat`

## bullet popup

- `bpop`
- `bpopTimer`
- `bpopShowTimer`
- `bpopExpanded` — A hover-opened menu shows only the primary actions until the user clicks "More
- `bpopReturnFocus` — The node id to restore focus to when the popup gives focus back.
- `bpopRestoreAfterAction` — #1411: ONE restore, and it is deliberately NOT inside hideBpop -- an outside-click dismissal must
- `bpopRestoreFocus`

## column panel one-time init

- `_mtColPanel`
- `freezePillToText` — Build the inline-artifact (pill) action rows for a point's Shift+F10 menu (C4) — the
- `collectPillActions` — where the pill has a separate editor, an edit row. A short suffix (expr/name/result)
- `manualClocksOf` — The {done,total} of every MANUAL clock in text, in document order (computed [o /M] skipped

## distribution peek (the 2026-08 panel: "show me the odds of this pill")

- `DIST_PANEL_ICON` — A read-only panel that runs a generator pill N=1000 times and shows the distribution: a histogram for
- `distPillDef`
- `dateSeries` — #1297: generate a run of dated to-do points at a fixed interval; the batch series the recurring-task
- `insertDateSeriesPoints` — Insert the schedule: a parent point over one dated to-do per date. Mirrors insertDistAsPoints'
- `openDateSeriesDialog` — The dialog: what repeats, from when, how often, until when. Dates parse through parseDueDate (so
- `insertDistAsPoints`
- `openDistributionPanel`
- `AUDIT_ICON`
- `openPillAudit` — #1284 "explain this number": the read-only audit for a child-rollup math pill (panel: "trust the
- `toggleSpoilersOf` — Toggle every spoiler block rendered under a node — the keyboard twin of clicking one. A
- `showBulletPopup`
- `scheduleBpopHide`
- `hideBpop`
- `_bpopSwallowClick` — #1211: set by a mousedown inside #bpop (whose items act + hideBpop on the down-press). The trailing
- `_bpopSwallowTimer`
- `showTodoPicker` — Compact state + priority picker for a todo node. Reuses the #bpop element (so

# Command menus & dialog shell

## slash commands

- `slashMenu`
- `slashState`
- `isSlashMenuOpen` — "open" = slashState exists, NOT the visible .on class — so in lean mode (where the panel never
- `checkSlash` — "/" opens the point-level menu, "@" opens the inline-insert menu.
- `slashFooterHTML` — Footer content for the / and @ menus: a one-line description plus, when the
- `renderSlashMenu`
- `positionSlashMenu`
- `leanSlashTip` — LEAN MODE: the / @ menu renders nothing, but a fully-blind menu is a trap (panel review, 5/6 lenses:
- `renderLeanSlashTip`
- `placeAtCaret` — shared caret-anchored placement for a fixed-position element (menu + lean tip use it)
- `slashMove`
- `_slashApplyContent`
- `slashApply`

## shared grid size picker (used by BOTH /base and @table)

- `sizePickerState` — Reuses the slash-menu popup element (no separate dialog): picking /base or
- `SIZE_PICKER_MAX`
- `isSizePickerOpen`
- `openSizePicker`
- `renderSizePicker`
- `sizePickerMove`
- `sizePickerConfirm`
- `closeSizePicker`
- `focusNewBase` — Land focus in a freshly-created base's HEADER cell (#441; #922 header half). focusNode()
- `createBaseAt` — Create a base (interactive grid) from /base — NON-DESTRUCTIVELY. This fixes the
- `promoteStaticTable` — "Convert to base" on a static table (§6 promote). Splits the point around the table
- `baseToStaticText` — IA-1 (product-identity §3b, freedom of form): the inverse of "Convert table to base".
- `convertBaseToText`
- `findFirstTableRange` — Find the FIRST static pipe-table block in a point's text, returning its [l0, l1)
- `announceDebut` — Insert an inline artifact at the current caret (used by the "@" menu).
- `insertInlineArtifact`

## "@" insert menu inside base cells

- `CELL_INSERT_EXCLUDE` — Same INSERT_CMDS pool minus table (no table-in-base) and footnote (footnotes
- `checkCellSlash`
- `cellSlashApply`
- `insertCellArtifact` — Cell flavor of insertInlineArtifact: records go on the node's sidecars as
- `rangeOffset` — Logical character offset of a (container, off) DOM point within el's text.
- `applyInlineInsertion` — Insert raw markdown at a caret offset.
- `applyInlineReplace` — Replace the text span [start,end) of a node with `str`, then drop back into

## inline-insert dialog

- `ioBack` — A small in-app modal that replaces native prompt() for links/images. Styled
- `ioCard`
- `ioCancel`
- `ioReturnFocus`
- `insertAtCaret` — Insert text at an input/textarea caret (used by the syntax-helper chips).
- `caretEnd` — Syntax-helper chips shown beneath the dice / markov fields. These are built
- `diceAddDie` — Add a die: if the formula already ends with this exact die, bump its count
- `diceAddMod` — Adjust a flat modifier: bump a trailing ±N in place (collapsing to nothing at
- `diceAddExplode`
- `diceAddKeep`
- `DICE_CHIPS`
- `insertAndSelect` — Insert text and select a placeholder word inside it, so the user can type
- `buildChipRow` — Build the shared .io-chip button row. Each chip either inserts a string snippet at the caret
- `MARKOV_CHIPS`
- `dialogHelp` — A `?` help button for a dialog header (#466): deep-links to the Concept guide entry

## container-agnostic dialog root

- `_dialogRoot` — When _dialogRoot is set, openInsertDialog renders into it instead of ioCard
- `_dialogCancel`
- `_dialogGuideCmd`
- `_restoreState`
- `_dialogFormEl`
- `_builderFormDrafts`
- `_builderFormCmd`
- `_insertDrafts` — UXP-246: unsaved insert-dialog input, keyed by dialog kind. The rule the pack editor, the capture
- `insertDraftFrom` — UXP-246: should a dismissed insert dialog leave a draft, and what is in it? Pure.
- `insertDraftMatches` — UXP-246: may this draft be offered back to a dialog opening with these seeds? Pure.
- `wireDialogDraft` — UXP-246: give a HAND-ROLLED dialog the same draft survival the shared builder now has, without

## the shared dialog shell (UXP-247)

- `dialogReturnTarget` — The ONE place a dialog's shell lives. Every part of this was copy-pasted across 22 dialogs, and
- `openDialogShell`
- `openInsertDialog`
- `closeIo`
- `closeBuilderWindow` — Close the builder window after a nested dialog submits. Clears builder state,

## #844: guard rails for the shared #io-card

- `ioCardBusy` — Every dialog shares this ONE card and every opener starts by wiping it, so two
- `_bgDialogChain`
- `withBgDialog`
- `_ioPending` — The one live promise-backed dialog: { owner, alive(), dismiss() }. dismiss() is that
- `registerIoPending`
- `openConfirmDialog` — Lightweight destructive-action confirmation. Returns a Promise that resolves
- `openDiceDialog` — Shared dialog for creating / editing a dice formula. Calls back with a fresh
- `openMarkovDialog` — Shared dialog for creating / editing a Markov chain. Calls back with a fresh

# Generation engines & indexes

## roll tables (legacy → grammar migration)

- `parseRolltable`
- `rolltableDefToRules` — Convert a legacy roll-table definition to a single grammar rule line:
- `migrateRolltables` — Per-node legacy migration (called from migrateNodePrefixes, which recurses):

## generative grammar engine

- `DICE_SNIFF`
- `DICE_ATTEMPT` — A die STARTED but missing its number of sides: {2d}, {2d+3}, {10d}. The `d` must be followed by
- `splitTopLevel` — Split `str` on top-level `sep`, ignoring separators nested inside {...}.
- `matchBrace` — Index of the `}` matching the `{` at `open`; -1 if unmatched.
- `codeSpanRanges` — Inline backtick code spans are the escape hatch: `{2d6}` should stay LITERAL, not promote to a pill.
- `inCodeSpan`
- `parseAlt` — One alternative "template 2" → {template, weight}; trailing integer = weight.
- `parseRules` — Parse a multi-line definition into {rules:{name:[{template,weight}]}, order:[]}.
- `parseRulesLoose` — #582: the TOLERANT twin of parseRules, for DATA PACKS ONLY. The strict parseRules
- `pickWeightedAlt` — Weighted random choice among [{...,weight}] entries.
- `GRAMMAR_OUTPUT_CAP` — Replace every {...} in a template via resolveBrace, recursively.
- `expandTemplate`
- `condParts` — Parse a conditional brace body `COND : THEN [| ELSE]` (Ink-style conditional
- `splitCondTop` — splitCondTop(str, sep) — splitTopLevel plus QUOTE awareness, for the conditional only. #1363.
- `condAttempt` — condAttempt(body) — TRUE when a body reads as an attempted TEST that condParts refused.
- `condAttemptReason` — condAttemptReason(body) — the P4 half of condAttempt: name the fix, not just the miss (the house
- `condEmptyMessage` — condEmptyMessage(cond, vars) — what a conditional whose TEST could not be read should say.
- `strCondVerdict` — String-equality conditional arm (#540). A conditional's test is normally an evalMath

## text modifiers (A1)

- `IRREGULAR_PLURALS` — Common-irregulars dictionaries for the word-shaping heuristics. Curated and CLOSED:
- `IRREGULAR_PAST`
- `irregularLookup`
- `F_TO_VES` — English pluralizer: common irregulars first (the recorded A1 deferral, closed via the
- `pluralize`
- `pastTense` — English past tense: common irregulars first (same pass), then the regular heuristic:
- `possessive` — Possessive (AP style; documented heuristic like pluralize): a word ending in s takes a
- `presentParticiple` — Regular present participle (documented heuristic, same honesty as pastTense: irregular
- `ordinal` — English ordinal of an integer-valued string; non-integer input returned unchanged.
- `ARTICLE_AN` — #772: a/an by PRONUNCIATION, not the leading vowel LETTER. Silent-h words sound vowel-
- `ARTICLE_A`
- `indefiniteArticle`
- `MODIFIERS` — One canonical string→string fn per modifier (null-proto so `m in MODIFIERS` is safe).
- `modParts` — Detect a modified reference: a bare base identifier + ≥1 modifier suffix, EVERY
- `applyMods` — Fold the modifier functions over a string, left-to-right.
- `bracedModParts` — {{a|b}.cap} — a modifier chain on an INLINE generator, not a named rule/var. Where modParts shapes
- `bracedDiceParts` — {{a}d{b}} / {2d{sides}} / {{n}d6} — a die whose COUNT and/or FACES come from a {…} (a variable or a

## hierarchical / property items (A6)

- `fieldParts` — `{base.field}` reads a *field* of a picked item: with `weapon: sword | axe` and
- `fieldModParts` — Field-then-modifier chain (#545, the A6 phase-2 half that shipped): {w.damage.cap} — a
- `dottedVarParts` — Detect a LONG dotted reference (≥3 identifier segments, e.g. a prefixed variable-base
- `resolveDottedVar` — Resolve a long dotted reference against the vars map: the LONGEST dotted prefix (≥2
- `resolveField` — Resolve a base.field read to its expanded string, or null when undefined (A6). The

## stateful sequences (decks / cycles)

- `SEQ_MODES` — A {mode: a | b | c} brace whose leading `mode` keyword gives it MEMORY: a
- `seqParts` — Parse a {mode: a | b | c} body → {mode, items, count} or null. The FIRST top-level `:`
- `shuffledIndices` — A fresh shuffled [0..n-1] (Fisher–Yates; routes through Math.random so tests can seed it). `rng`
- `nextSeqIndex` — Advance a sequence record's state and return the index of the item to emit, or
- `advanceSeq` — Advance a sequence and return its emitted text — the chosen item expanded
- `resolveBrace` — Resolve one {...} body. Content-sniffed: a conditional `cond: a|b` (a comparison
- `expandRule` — Expand a named rule, guarding against cycles (↻) and runaway depth (…). A rule

## input-dependency snapshot (#827 item 5)

- `recordVarReads` — A grammar pill freezes its expansion by design (a document must not rewrite
- `depsChanged` — Has any recorded input changed since the freeze? `deps` is the roll-time
- `runGrammar` — Run a grammar definition, merged with document-wide rules. Local rules win.
- `expandText` — Expand a plain string's {…} against document-wide rules + variables. Used by
- `rollPickSource` — Expand a random-pick variable's source ONCE against the document namespace.
- `rollPickRecord` — #1243: roll a pick var's source into its stored shape. A contest source ("A vs B") rolls both
- `_rulesCacheVer` — Document-wide rule map: walk the whole tree and merge into one namespace

## declarative data packs (plugins)

- `validPluginPack` — Shape guard: a pack is applied only if it's a plain object carrying a string id.
- `pluginPackActive` — A pack is ACTIVE unless it carries `enabled: false` (#487 manager toggle). Absent =
- `mergePackRules` — Merge every valid pack's grammar rules into `target` (a null-proto rule map), in
- `packVarDefs` — Flatten every valid pack's variables to a resolvable [{name, ...}] list, in pack order.
- `packTemplateDefs` — #583: a pack may also carry reusable subtree TEMPLATES — the natural vehicle for pre-made

## data-pack manager model (#487) — pure list ops over root.plugins

- `packLabel` — A short display label for a pack (a non-blank name, else id, else "(unnamed)").
- `newPluginPack` — A fresh empty pack with a unique id. `gen` injectable for tests.
- `updatePluginPack` — Return a NEW plugins array with pack `id` replaced by `patch` (shallow-merged), others
- `removePluginPack` — Remove pack `id`. Pure — returns a new array.
- `togglePluginPack` — Flip a pack's enabled state (absent → was active → becomes false). Pure.
- `upsertPackTemplate` — #518: append (or replace-by-name) a captured subtree template on pack `id`. The node is stored
- `removePackTemplate` — #518: remove a template by name from pack `id`. Pure — returns a new array.
- `parsePackImport` — Parse an imported packs payload (a JSON string or a parsed value) to a clean array of
- `collectRules`
- `_callablesCacheVer` — Document-wide callable name index: variables (with resolved values), grammar
- `collectCallables`
- `filterBraceCandidates` — Filter a collectCallables() list by prefix (case-insensitive, anchored at start).
- `BRACE_FORMS` — The generative FORMS the { picker offers alongside document names — the whole
- `BRACE_FORM_ROWS` — Filter BRACE_FORMS by prefix (label or any key, anchored). Empty prefix = all.
- `filterBraceForms`
- `defaultBraceChoice` — The DEFAULT (highlighted) candidate for the forms+names picker: row 0 — unless the

## hashtag index

- `tagScanText` — Document-wide tag list for the # autocomplete (UXP-10): every #word the hashtag
- `_tagReCache` — Does `text` carry `value` as a hashtag? Lifted verbatim out of termMatchesNode's tag branch so
- `tagHit`
- `_stateReKey`
- `stripStateTags`
- `extendAncTagText` — Extend an accumulated ancestor-tag string by ONE level — the walkers' per-descent step.
- `ancestorTagText` — The tag text an ancestor chain contributes: each ancestor's scan text, states stripped.
- `_tagsCacheVer` — Pure over rootNode (tests); no-arg = the live document, cached on _varsVer.
- `collectTags`
- `collectPropKeys` — doc-cache: collectPropKeys (see DOC_CACHES) — the living makeDocCache tenant: the factory owns
- `_collectPropKeysCache`
- `_dupTitleCacheVer` — Ids of every point whose display title collides (case-insensitively) with at least one
- `duplicateTitleIds`
- `resolveTypedLinks` — Resolve bare typed `[[Title]]` spans (no `#`) against the live document. The `[[` picker
- `filterTagCandidates` — Filter a collectTags() list by prefix (case-insensitive, anchored at start).

## node links (same-document)

- `LINK_RE` — A link is a [[#TARGETID|label]] token in node.text pointing at another node by id.
- `_linksCacheVer`
- `collectLinks`

## relationship graph (#516: the browsable recognition surface)

- `graphNodeLabel` — A clean label for a graph node: the point's identity, in the words it READS as.
- `graphModel` — Build the undirected graph from the link index: one node per point that
- `GRAPH_UNLINKED_CAP` — Cap on unlinked edges the graph draws per open (graphUnlinkedEdges). Measured (synthetic docs,
- `graphUnlinkedEdges` — The graph's UNLINKED half (product-identity.md §2c, "the scratchpad test"): a graph that only
- `mergeUnlinkedEdges` — Merge graphUnlinkedEdges' output into a same-doc graphModel result. A node that has ONLY
- `qid` — #898 (cross-document-direction.md §4b / §6 step 5): the NEIGHBORHOOD graph — the point you are on
- `unqid`
- `GRAPH_NEARBY_CAP` — The cap. 150 matches GRAPH_UNLINKED_CAP, and the reasoning is the same one: it is the LAYOUT that
- `GRAPH_NEARBY_HOPS`
- `nearbyAdjacency` — Undirected adjacency over the whole link web. Undirected on purpose: "near this idea" includes
- `nearbyGraphModel` — Breadth-first from the start point out to `hops`, capped at `cap` nodes. BFS order is the point
- `docGraphModel` — 4b (cross-document-direction.md): the DOC-LEVEL folder graph — documents as nodes, one
- `graphLayout` — Deterministic force-directed layout (Fruchterman-Reingold, seeded). PURE: the same
- `GRAPH_TAP_MIN` — UXP-243: the tap floor, and the separation the layout aims for so the floor actually fits.
- `GRAPH_MIN_SEP`
- `nearestDistances` — id -> distance to the closest OTHER node. O(n²), the same order as the layout's own repulsion
- `relaxSeparation` — Push apart any pair closer than minDist, a few bounded rounds, re-clamped into the box each time.
- `tapRadius` — The radius a node's invisible hit circle can take without stealing its neighbour's tap: the floor,

## UXP-249: the tap-floor guard

- `tapFloorCandidates` — UXP-248 fixed nine controls under the 24px floor and pinned those nine BY NAME. Nothing stopped
- `clampPositions` — Fit a position map inside [margin, dim-margin], PRESERVING the layout's shape: one scale for

## timeline (#516: recognition-by-time)

- `timelineModel` — A chronological narrative of every dated point across the WHOLE date range,
- `BROKEN_NAME_MAX` — Broken-links report (backlog #4). Pure: enumerate every link whose target is missing, same-doc AND
- `brokenNodeMessage`
- `collectBrokenLinks`
- `timelineOpen` — The graph's chronological twin: a full-viewport overlay listing every dated point
- `openTimeline`
- `closeTimeline`
- `toggleTimeline`
- `timelineSources` — #647: which of the three date-sources the timeline shows (session-only, like agendaMonth). All on
- `TL_SRC_LABELS`
- `renderTimeline`

## cross-document link index (CF-1: the workspace-wide link/backlink foundation)

- `WLINK_RE` — The same-doc link path above (LINK_RE / collectLinks) is per-document and stays exactly
- `parseLinkToken` — Pure: parse a SINGLE [[...]] token (anchored) -> { docId|null, nodeId, label }, else null.
- `tokenUnderCaret` — Pure: find the link token at or spanning `offset` in `text` (edit-mode caret offset).
- `dupDocIdGroups` — Pure: build the whole-workspace link index from already-parsed docs.
- `buildWorkspaceIndex`
- `workspaceBacklinks` — CF-4: other-doc points that link TO targetDocId#targetNodeId, read from the CF-1 index's

## §5 spine helpers (cross-document-direction.md)

- `wsDocRoot` — wsDocRoot: THE own-doc-liveness chokepoint (§5.3). Every cross-doc read resolves a docId
- `wsAllDocRoots` — 4c: the folder READING SET — [docId, root] pairs covering every indexed doc, with the
- `findNodeInRoot` — Find a node by id inside an arbitrary parsed tree — nodeById only knows the live doc's
- `_wsVarsMemo` — Resolved variable map of ANOTHER doc, memoized on the index generation (§5.1). collectVars
- `wsDocVars`
- `backlinkSnippet` — 4d: the context a backlink row shows under its title — the source LINE around the first link
- `searchSnippet` — WS-1: workspace-wide search. Points across the folder (excluding the current doc, which
- `searchWorkspace`
- `UNLINKED_MIN_LEN` — Minimum title length for unlinked-reference matching (1-2 char titles would match too broadly).
- `matchableNodeNames` — Pure: list points that mention any of targetId's names (canonical title OR an
- `unlinkedNameRegex` — The shared name-matcher behind BOTH the same-doc and cross-doc unlinked-reference scans: build a
- `collectUnlinkedRefs`
- `collectCrossUnlinkedRefs` — Cross-document unlinked references (backlog #3, view-only): the same name-scan as collectUnlinkedRefs
- `linkifyMention` — Pure: replace the first outside-token occurrence of title in text with [[#targetId|]].
- `grammarKey`
- `makeGrammarRoll` — Build a fresh grammar record from a definition; null if malformed.
- `makeSeqGen` — Build a fresh stateful-sequence record (a grammar record carrying mode/items +

## repeat (Nx)

- `repeatParts` — {Nx: template} — emit `template` N times, re-expanded each time, joined by
- `renderSeqGenPill` — A stateful-sequence pill (deck / cycle / once / stopping). Reuses the grammar
- `isYesNoOracle` — UXP-145: is this grammar a pure Yes/No oracle? A CONTENT-SNIFF, not a stored flag — every
- `renderGrammarPill`
- `GRAMMAR_CHIPS`
- `openGrammarDialog` — `tableFlavor` is the "Roll table" door: same dialog, same grammar record —
- `SEQ_CHIPS`
- `openDeckDialog` — Front door for the deck/cycle pill (the @ "Deck" door). One field: the

## yes/no oracle (a friendly door over weighted alternation + A5)

- `ORACLE_BANDS` — The most-reused solo-gen primitive: ask a question, get Yes/No at tunable odds.
- `oracleSwingSide` — UXP-111/124: the "with complication" (swing) oracle. A six-way answer so the most common solo
- `oracleSwingBody`
- `ORACLE_SWING_BANDS`
- `ORACLE_CHIPS`
- `ORACLE_SWING_CHIPS`
- `oracleParts` — oracleParts(body) — sniff the oracle's typed twin: {oracle: likely} / {oracle: likely + swing}
- `openOracleDialog`
- `rerollGrammar`
- `editGrammar`
- `pruneGrammar`

# Math, variables & authoring aids

## typed shorthand → pill promotion

- `parseVarDecl` — Typed variable declaration: {name := expr} → { name, expr } (or null). The `:=`
- `varDeclIsPick` — Classify a typed declaration's RHS: a PICK (frozen text value) or a FORMULA (live math).
- `_VAR_PROBE`
- `varDeclKind` — #952: a declaration's RHS is one of THREE kinds now, not two. The estimate case has to be named,
- `varDistDraw` — The draw behind a distribution variable. Correlation is not a mechanism built here — it falls out
- `varDistHeadline` — "mean (p5 – p95)" for a distribution variable, or null when it will not sample. The SAME
- `promoteBraceBody` — Promotion is one of the two places a {roll:} resolves (the other is rerollGrammar), and it is
- `promoteBraceBodyIn`
- `_promotedLit` — Scan all top-level {…} in node.text and promote each valid one. Outer braces
- `promoteInlineShorthand`
- `nameSetSignature` — #1357: the set of NAMES in a { name: value } map, as a comparable string. Only the names
- `pendingBraceMessage` — #1357: how many points were waiting on the value this edit just supplied. P4 — the promotion
- `offSearchMessage` — #1375: the point you just edited no longer matches the search it is listed under. Said, not acted
- `retryPendingBraces` — #1357: promotion is ONE-SHOT. A `{…}` whose names were not resolvable when its point was
- `promoteCellShorthand` — Promote {…} shorthand inside ONE base cell string. The promoteInlineShorthand walk,
- `promoteLoadedShorthand` — Promote {…} shorthand across a whole loaded document. The edit-commit path
- `artifactToShorthand` — Inline-able artifact ⇄ grammar source. Returns the {…} text an artifact
- `unfoldedPrefixLen` — Length `prefix` would have after unfoldArtifacts rewrites its inline-able tokens
- `foldedOffsetFor` — The inverse: translate an offset captured against the UNFOLDED edit buffer into
- `_unfoldData` — While a node is in edit mode its text transiently holds {…} grammar source in
- `artifactTokenRe` — Entering edit mode: rewrite each inline-able token in node.text to its {…} source
- `unfoldTokensIn` — #1467: the STRING half of unfolding -- tokens back to the shorthand the user typed -- with no
- `unfoldArtifacts`
- `applyRefold` — Restore each untouched unfolded {sh} in `text` back to its token (pure helper).
- `refoldArtifacts` — Leaving edit mode: fold untouched {…} back to their original tokens (frozen state
- `foldedTextForSave` — Canonical (folded) text of a node that may currently be unfolded for editing —
- `withFoldedActive` — Run `fn` with the actively-edited node's text temporarily folded, then restore
- `caretAfterGrSrc` — Place the caret immediately *after* the .gr-src span whose content ends at
- `grSrcSpanClean` — A .gr-src span's text must be exactly one balanced {…} — highlightGrammarText
- `normalizeGrSrcSpans` — Self-healing pass for live edits around .gr-src spans (UXP-28): if any span's
- `_highlightGen` — Live grammar styling while editing: when the caret lands just after a `}` that
- `checkInlineHighlight`

## math expressions

- `DAY_MS` — evalMath's lookup tables are hoisted to module scope so they're allocated once
- `MATH_CONSTS` — All lookup tables are null-proto: ident() probes them with `in`, and on a plain
- `ymdIntrinsic` — #544 date helpers: INTRINSIC (arithmetic-safe) y/m/d ↔ epoch and days-in-month, for the active
- `ymdToEpoch`
- `daysInMonthOf`
- `FN1`
- `intGcd` — #544: integer GCD (Euclid), inputs rounded + made positive. gcd(n,0)=|n|, gcd(0,0)=0. Shared by
- `FN2`
- `FN3`

## math body completion (the { picker, stage 2 — inside a {= …} body)

- `MATH_DATE_FNS` — The vocabulary the { picker offers once a body commits to math: FN1/FN2/FN3 +
- `mathFnGroup`
- `tokenLeftOfCaret` — The identifier fragment ending at `caret` within a math body — the token being
- `mathFragmentAt`
- `MATH_FN_DESC` — One-line glosses for every math completion (shown in Guided under the signature hint), so a
- `mathGuideFor` — Which concept-guide entry a math completion's ? opens, by group. Dates go to the dates-in-math
- `mathCompletions`

## body completion — Phase 2 (search operators, oracle bands, meter props)

- `searchTokenAt` — The whitespace-delimited token ending at the caret in a query string (a search term
- `SEARCH_IS_DESC` — One-line glosses for the is: structural filters (shown in Guided). The other operator families
- `searchCompletions` — Complete a single search token against the doc's vocabulary. ctx = {tags, vars, states}
- `ORACLE_BAND_DESC` — {oracle: <band>} — complete the closed likelihood set. Empty prefix → all five (small,
- `oracleCompletions`
- `meterTokenAt` — {meter: prop/prop} — the identifier token at the caret, and completion over the point's
- `meterCompletions`
- `evalMath`
- `formatEpochDays`
- `isDateExpr`
- `MOON_GLYPHS` — #649: a bare moonphase(…) pill displays its 0..1 result as a moon glyph (like asdate → an ISO
- `moonGlyph`
- `isMoonExpr` — Glyph-format only a BARE moonphase call (the whole expression), so composing it for the raw number
- `isConvertExpr` — #983: a BARE convert (the whole expression) rounds for display. Same rule as isMoonExpr above and
- `formatConvertResult` — 4 significant figures, not N decimal places. Two decimals reads fine at 289.68 and destroys
- `formatMathDisplay`
- `formatMathResult`
- `groupThousands` — #946: DISPLAY-only number formatting — thousands separators by default, plus an optional
- `formatNumDisplay`
- `parseNumFmt` — Pure: build a lean fmt record from the math dialog's three text fields (or null when all blank),
- `formatVarValue` — A resolved variable value is a number (formula var) or a string (random pick).
- `mathKey`

## a roll inside math: {= 1d6 + 2}

- `MATH_DICE_RE` — evalMath speaks only numbers, so a dice term is invisible to it. FREEZE the roll (engine-1 feeds
- `mathHasDice`
- `rollDiceTerm` — One roll of a single dice term → its total, or null if it doesn't parse.
- `freezeDiceInExpr` — Substitute every dice term with its parenthesized rolled total; non-dice text (props, vars, sum(),
- `makeMathResult`

## uncertainty fields (B2)

- `EST_N` — A distribution can't ride evalMath (which ALWAYS returns a NUMBER — a load-bearing
- `SPARK_RAMP`
- `EST_Z95`
- `rngFromSeed` — mulberry32 — a tiny seedable PRNG so sampling is deterministic per pill (NOT
- `estNewSeed`
- `estGaussian` — A standard-normal draw (Box–Muller: two uniforms → one gaussian).
- `meanOfSamples`
- `sampleLogNormalCI` — `lo to hi` → a lognormal whose 5th/95th percentiles are lo/hi (positive bounds);
- `tokenizeUncertain` — Tokenize an uncertain expression, or null on an unknown char / malformed number.
- `parseUncertain` — Parse an uncertain expression into an AST, or null if malformed. Precedence,
- `estChildPropExpr` — Read a child point's uncertain property value (the raw expression string), or
- `_distSampleStack` — Sample an AST node → a length-n array. Distributions zip element-wise under
- `sampleUncAst`
- `sampleUncertain` — THE public sampler: an uncertain expression → a length-n sample array, or null
- `quantileSorted` — #1101: the quantile lived unexposed inside distSummary, which computed p5/p50/p95 and threw the
- `distQuantile`
- `distChanceOver` — The share of the distribution strictly above / below a threshold, as a PERCENTAGE (0..100) —
- `distChanceUnder`

## distribution readout (the 2026-08 panel: "run this pill N times, show the odds")

- `DIST_BAR_MAX` — Extends the estimate lane's Monte-Carlo idea to the categorical/discrete generators (dice pool, deck,
- `distReadout`
- `distChanceAtLeast` — Inclusive tail probabilities as a PERCENTAGE (0..100), for the peek's "chance >= N" / "<= N" field.
- `distChanceAtMost`

## #1298: combinatorial probability + Monte-Carlo, as COMPOSABLE evalMath scalars

- `lgamma` — Natural log of Γ(x) via the Lanczos approximation (g=7). Overflow-safe: hypergeom over a 60-card
- `lnChoose` — ln C(n, k) in log space; -Infinity (→ exp = 0, an impossible term) when k is out of [0, n].
- `hypergeomAtLeast` — P(X >= atleast) as a PERCENTAGE (0..100) for X ~ Hypergeometric(pop, successes, draws): the odds of
- `wilsonBound` — #1311: a Wilson score confidence-interval bound for an empirical proportion wins/games, as a
- `simulateChance` — The share of a Monte-Carlo outcome array satisfying `value <op> t`, as a PERCENTAGE — the reducer
- `SIM_MAX`
- `hashSeedStr` — FNV-1a of a string → uint32, to seed a simulate() from its own call text so the answer is STABLE
- `sampleGenerator` — Sample a generator N times → an outcome array (numbers for dice, strings otherwise). Uses the same
- `distSummary` — {mean, p5, p50, p95} of a sample array (non-finite values dropped), or null.
- `sparkline` — A pure-string unicode histogram of a sample array (the export-safe inline viz).
- `estNumFmt` — Shared number formatter (concise, finite-safe) so the pill, preview and export
- `distHeadline` — The `mean (p5 – p95)` headline, in ONE place. Eight call sites used to build this string by hand,
- `formatDist` — "mean (p5 – p95) ▁▂▄▆█…" — the frozen headline display. Pure + export-safe.
- `usesDistVar` — Sniff a {…} body that should promote to an estimate pill: an uncertain
- `estParts`
- `estKey`
- `makeEstRoll` — Build an estimate record from an uncertain expression, or null if it won't parse.
- `renderEstPill` — Display-mode estimate pill. Re-samples from {expr, seed} against the render node
- `EST_CHIPS`
- `openEstimateDialog`
- `rerollEst` — Re-sample an estimate in place (click handler): a fresh seed, new draws.
- `editEst` — Open the editor for an existing estimate; on submit, replace its expression.
- `pruneEst` — Drop estimate records whose [[est:KEY]] token no longer appears in the text.
- `renderMathPill` — #1181: `opts.preview` renders the pill for a DIALOG PREVIEW rather than for the document. A preview
- `varKey`
- `renderVarPill`
- `pruneVars`

## sequence pill + dialog (declaring a state set; parallels @var)

- `seqKey`
- `pruneSeqs`
- `pruneQueries`
- `seqDefString` — Re-serialize a sequence record's states back to the "A B | C" definition form
- `renderSeqPill`
- `renderQueryPill` — renderQueryPill(key, q) — a live embedded search: runs q.expr over the current document
- `openSeqDialog` — Declare/edit dialog on the shared openInsertDialog harness. Validation and the
- `QUERY_CHIPS` — The search-operator chips shared by the query and query-base dialogs: TEACH chips that insert
- `openQueryDialog`
- `parseQBaseCols` — parseQBaseCols(text) — one column per line: 'Name: field' or just 'field' (the
- `openQBaseDialog`
- `editQBase`
- `VARBASE_PREVIEW_CAP` — Variable base: turn a base's rows into document variables (bases-direction §7b). The
- `varBasePreviewHtml`
- `openVarBaseDialog`
- `qbaseCreateAt` — Create a query base at a point, mirroring createBaseAt's non-destructive rule:
- `editQuery`
- `editSeq`
- `MATH_CHIPS` — Math chips: constants + operators that are tedious to type, plus the most
- `REDUCER_CHIPS` — #1281 residual: the rollup reducers as click chips, so a total is built without typing a function
- `DATE_CHIPS` — #1323: date computation had no visible door — computing a date ("30 days from today", "days until the
- `propKeyChips` — #1281 residual: the document's own property keys as chips, so a key is PICKED, never typed — the last
- `openMathDialog`
- `openColFmtDialog` — ε: the base-column twin of the math pill's number format. The same three fields
- `rerollMath` — Edit an existing math expression in-place; re-evaluates and updates the pill.
- `editMath`
- `pruneMath`

## variables

- `VAR_NAME_RE` — Validate an identifier: a-z, 0-9, underscore, must start with a letter/underscore.
- `VAR_CHIPS` — Chips that insert common values/expressions into the variable dialog.
- `openVarDialog`
- `varEditLabel` — #1403: ONE name for one door. The bullet-menu row, the dialog it opens and the pill's own pencil
- `editVar`
- `rerollPickVar` — Re-roll a random-pick variable from its declaration pill: re-expand the stored
- `rerollDistVar` — #952: re-seed a DISTRIBUTION variable in place. The gesture is the existing one (body-click
- `announce` — Write a short message into the polite live region so screen readers hear
- `overlayCountMessage` — UXP-241: which of an overlay's two rendered strings is the honest thing to announce. The graph
- `announceOverlayCount` — Read the RENDERED strings back rather than recomputing the count, so the announcement can never
- `roveIndex` — UXP-240: where a roving-tabindex group's focus goes for a navigation key. This file already grew
- `roveTo` — Swap the group's single tab stop onto `to` and focus it. The other five groups each wrote this
- `hideSlashMenu`

## link picker ([[ trigger)

- `lpMenu` — Always on (UXP-4: a built capability with no front door is non-conformant; the
- `lpState`
- `isLpMenuOpen`
- `hideLinkMenu`
- `linkCandidates` — Link-target candidates: every point whose title OR an alias contains the query
- `workspaceCandidates` — CF-3: other-doc points from the workspace index (CF-1) matching the query, for the [[
- `linkCreateOption` — The link picker's "+ New point" create option (link-and-create): the trimmed
- `checkLinkTrigger`
- `renderLinkMenu`
- `lpMove`
- `lpApply`
- `positionLinkMenu`

## { brace autocomplete (forms + callable names)

- `braceMenu`
- `braceState`
- `_pickerMousedownActive` — True while a pointer interaction that STARTED on a caret-anchored picker menu ({ / # / :)
- `_pickerGuardTimer`
- `armPickerGuard`
- `disarmPickerGuard`
- `isBraceMenuOpen`
- `hideBraceMenu`
- `openBraceHelp` — The picker's `?` help mark: commit the draft DELIBERATELY, then open the concept guide at

## {…} shorthand live-preview tooltip (UXP-7)

- `bracePreview` — A non-interactive tooltip that appears below the closing } of a valid {body}
- `hideBracePreview`
- `showBracePreview`
- `searchCtx` — The doc vocabulary the search completion draws on (cached collectors, cheap).
- `_bodyVocab` — Per-SESSION vocabulary cache for body completion. The input handler runs markDirty()
- `bodyVocab`
- `completionScopeVars` — The variable scope a {= …} / conditional body will actually resolve against: positional
- `bodyCompletion` — Dispatch a {…} body to its stage-2 completion. Returns {prefix, fragStart, matches}
- `checkBraceTrigger`
- `BRACE_BROWSE_ROW` — The one row in the brace menu that does not insert text: it opens the full Pill picker. Offered
- `BM_GROUP_LABELS`
- `_braceRenderKey`
- `renderBraceMenu`
- `positionCaretMenu` — Position a caret-anchored picker (the { and # menus share this) at the logical
- `braceMove`
- `renderLeanBraceTip` — Lean: the { grammar picker shows the SAME one-line tip as the / and @ commands (item 8, modes
- `applyBraceFormText` — Pure core of braceApply's forms/names splice: replace `{`+prefix with the inserted
- `consumeTokenEnd` — Forward-consume the tail of the token the caret sits in, using the SAME boundary rule
- `braceApply`

## # hashtag autocomplete (tag picker)

- `tagMenu`
- `tagState`
- `isTagMenuOpen`
- `hideTagMenu`
- `emojiMenu` — UXP-106: the emoji picker — the fifth caret-anchored §7.1 picker, mirroring the tag picker
- `emojiState`
- `isEmojiMenuOpen`
- `hideEmojiMenu`
- `checkTagTrigger`
- `renderTagMenu`
- `tagMove`
- `tagApply`

## emoji picker (UXP-106) — mirrors the tag picker above

- `checkEmojiTrigger`
- `renderEmojiMenu`
- `emojiMove`
- `emojiApply`
- `checkMdBlockPrefix`
- `applyBlockCmd` — Point-level command: change the node's type (or insert a table). Inline
- `getCaretOffset` — Returns the logical character offset of the caret within el.
- `setCaretByOffset` — Set the caret to a logical character offset within el (inverse of getCaretOffset).
- `domPointForLogical` — Find the DOM {node, off} for a logical character offset, counting atomic pill
- `selectLogicalRange` — Select the logical [start,end) span in a contenteditable (type-to-replace). Used

# Tree editing & history

## node type / checked

- `toggleTaskInNode` — Flip the Nth task-list checkbox (- [ ] / - [x]) in a node's text, then refresh
- `firstTaskChecked` — Pure: is the point's FIRST task box checked? null if it doesn't open with a task box.
- `setFirstTaskChecked` — Set the point's first task box to `checked` (idempotent — a no-op if already there).
- `propagateCheckboxToSelection` — Bulk checkbox toggle across a selection: when the clicked checkbox belongs to a point
- `_pendingVarBaseRender` — Re-roll a dice token in place (click handler), keeping its formula.
- `scheduleVarBaseRender`
- `repaintAfterRoll` — Repaint after a roll/edit changed a pill's frozen value. A PROJECTING variable base's
- `rerollDice`
- `editDice` — Open the editor for an existing dice token; on submit, replace its formula
- `pruneArtifacts` — Drop every sidecar record whose inline token no longer appears in node.text — the
- `pruneDice` — Drop dice records whose [[dice:KEY]] token no longer appears in the text.
- `rerollMarkov` — Re-walk a markov chain in place (click handler), keeping its definition.
- `editMarkov` — Open the editor for an existing chain; on submit, replace its def + re-walk.
- `pruneMarkov` — Drop markov records whose [[markov:KEY]] token no longer appears in the text.
- `rerenderNode`

## tree mutations

- `splitForSibling` — Pure core (UXP-60): split `text` at `offset` for an Enter-keystroke sibling.
- `inFence` — Pure (issue #405): would a split at `offset` break a fenced code block of `text`?
- `mergeUpText` — Pure core (UXP-68): merge `body` (the prefix-stripped trailing point) onto the end of
- `mergeBodyText` — #818 (agent-review, UXP-199): the body a merge carries up — the marker/keyword prefix
- `ARTIFACT_SIDECARS` — Token-keyed sidecars (the 8 with prune functions). Cloned whole onto a split's new
- `cloneArtifactSidecars`
- `mergeArtifactSidecars` — Merge counterpart (UXP-68): append a merged-away point's sidecar records onto the
- `insertSiblingAfter`
- `mergeUpInto` — UXP-68: Backspace at offset 0 of a non-empty point merges it UP into the previous
- `caretOffsetIfEditing` — #814 (agent-review, UXP-207): a structural op on the point being edited preserves the
- `indentNode`
- `outdentNode`
- `deleteNode`
- `duplicateNode` — Duplicate a point and its whole subtree, inserting the copy as the next sibling. Fresh
- `lastVis`
- `moveNode`
- `setCollapsed`
- `toggleCollapse`
- `setFolded` — Paragraph first-line collapse ("fold"). A separate state from `collapsed` (which hides
- `toggleFold`
- `collapseToLevel` — Collapse the whole (current viewport) outline so only its first `n` levels show.
- `expandAll` — Expand every node in the current viewport (show all levels).
- `focusNode` — #803 (agent-review, UXP-200): focus lands SYNCHRONOUSLY when the row already exists in
- `_focusNodeGo`
- `focusNodeAtOffset` — Like focusNode but lands the caret at a logical character `offset` (unfolded

## arrow-key cross-node navigation

- `lineH`
- `insertLineBreak` — Shift+Enter: add a newline inside the same point (multi-line content).
- `caretScrollDelta` — How far to scroll so the caret is visible, in pixels to ADD to the scroll position; 0 when it is
- `nearestPointToClick` — #1210: a click on the empty canvas — below the last point, or in the wide margins of a short
- `caretRectIn` — The caret's rect, or the element's as a fallback — a collapsed range yields no rects on an empty
- `keepCaretInView` — Bring the caret back into view, but only if it has left it. Called when the user WORKS (types or
- `CARET_KEEP_MARGIN`
- `CARET_KEEP_KEYS` — The keys that mean "I am working at the caret" without producing input. Escape and chords are
- `isAtFirstLine`
- `isAtLastLine`
- `navigateToPrev`
- `navigateToNext`

## drag and drop

- `dragId`
- `dragIds`
- `dragDropInfo`
- `bulletDragMoved`
- `dropLine`
- `isDescOf`
- `clearDropVisuals`
- `clearAllDrag`
- `attachBulletMenuGestures` — #1343: the bullet's "usual operations" — click zooms into the point (a paragraph folds instead), Enter/Space
- `attachDragHandlers`
- `attachBulletTouchGestures` — Touch gestures on a bullet (HTML5 drag never fires on touch, so this is the touch
- `performDrop`
- `performMultiDrop`

## dirty

- `resetDocCaches` — Invalidate every registered document cache. This bump is the single invalidation point shared
- `diskTargetOpen` — UXP-103: the persistence signals must all mean the SAME thing — "unsaved to a named disk
- `unsavedToDisk`
- `updateDirtyDot`
- `markDirty`
- `_varPanelTimer` — markDirty fires per keystroke (prose and base cells alike); rebuilding the whole
- `scheduleVarPanelUpdate`

## undo / redo

- `UNDO_MAX_ENTRIES` — Snapshots are stored as JSON strings, not live object graphs: a parsed
- `UNDO_MAX_BYTES`
- `snapshot`
- `trimStack`
- `SNAPSHOT_PRESERVE` — #1420: the fields an undo must NOT move. A snapshot is the whole root, so every document-level
- `restoreSnapshot`
- `recordTextEdit` — Text edits are recorded as tiny {prev,next} entries instead of full-tree
- `flushActiveTextEdit` — Commit an in-progress text edit before a structural op snapshots the tree,
- `pushUndo` — structural ops: flush any in-progress text edit, then snapshot the whole tree
- `applyEntry` — apply one history entry; `dir` is 'prev' for undo, 'next' for redo
- `undo`
- `redo`
- `markClean`
- `displayName` — Pure: the user-facing name for a stored filename , drop the .opml extension and
- `toFileName` — Pure: turn a user-typed/bare name back into a storage filename (sanitize + ensure
- `updateTitle`
- `renderFileName` — Refresh the menu-header filename display (bare name, no extension).

# Persistence, workspace & import/export

## file I/O

- `hasFSA`
- `fsaOk` — #1112: hasFSA is PRESENCE. The method exists in profiles that refuse to open the picker,
- `OPML_TYPE`
- `PICKER_REFUSAL_MS` — #1112: did the picker get REFUSED, or did the user cancel it? The two are indistinguishable by
- `pickerOutcome`
- `runPicker` — Run a picker and classify its failure. Returns { ok, value, outcome } -- outcome is null on
- `pickerRefusedMessage` — The message a refused picker earns. Names the browser as the cause and the way out, rather than
- `adoptDoc` — The single runtime chokepoint for making `newRoot` the live document. Every
- `newFile`
- `commitFileName` — Rename the current outline from the menu header. On a Chromium file handle the
- `openFile`
- `saveFile`
- `saveAsFile`
- `dlOpml`
- `writeH`

## workspace folder (connect · persist · reconnect)

- `hasWorkspace` — Phase 1, step 3 (guidance/roadmap.md): a durable on-disk backing folder — the
- `workspaceDir`
- `pendingDir`
- `docViewState` — Per-doc view state (idea 5): switching between folder docs used to drop you at root, fully
- `captureDocView`
- `workspaceFile`
- `workspaceIndex`
- `_wsIndexGen`
- `_wsWriting`
- `_wsWarned`
- `_wsKnownModified`
- `_wsKnownSize`
- `_wsSelfWriting`
- `_wsReconciling`
- `_wsForceWrite`
- `_wsPromptSnooze`
- `WS_PROMPT_SNOOZE_MS`
- `WORKSPACE_KEY`
- `OPEN_TABS_KEY`
- `LASTFILE_KEY`
- `saveLastFileHandle` — UXP-127: persist the last single-file handle so a returning session can reopen the same disk
- `clearLastFileHandle`

## document tabs (over the workspace switcher)

- `openTabs` — A tab strip that surfaces the existing workspace switcher: tabs are the documents
- `tabAdd` — Add `name` to the tab list if absent (no duplicates — a file is one tab). Returns a
- `tabClose` — Close `name`. Returns { tabs, nextActive } where nextActive is the filename to switch
- `tabCycle` — The filename `dir` steps away from `active` in the tab order, wrapping. dir = +1 next,
- `workspaceAffordance` — Pure: the File-menu affordance state machine. Precedence — the capability gate wins
- `firstLineTitle` — Pure: the document's title — its first point's display text, first line, markdown
- `workspaceFileName` — Pure: the .opml filename to create in the connected folder. Mirrors saveAsFile's
- `uniqueWorkspaceName` — Pure: a collision-safe filename within the folder (brief #5b). `existing` is the set of
- `docIsBlank` — Pure (#839): is the document effectively blank — nothing a user would miss if it were
- `connectWriteDecision` — Pure (#839): what should connect do about the filename it derived for the current doc?
- `workspaceDocList` — Pure: shape a raw directory listing into the document list — keep only '.opml' files

## sync-safety pure cores (folder-backed write reconciliation)

- `reconcileAction` — Folder sync (Dropbox/iCloud/Drive on the workspace folder) can change a doc's
- `bootPrefersFile` — #746 pure decision: at boot, is the on-disk file the authoritative copy (vs the local
- `_wsTmpSeq` — Pure: the temp filename for an atomic write. We write here, then move() over the
- `tmpWriteName`

## IndexedDB handle store

- `idbOpen` — A FileSystemDirectoryHandle is structured-cloneable but NOT JSON-serializable, so it
- `idbGet`
- `idbSet`
- `idbDel`
- `applyFileModeLabels` — Reflect the current workspace state into the File-menu rows: a connect/reconnect/
- `renderWorkspaceAffordance`
- `persistOpenTabs` — Persist the open-tab list so it survives a reload (alongside WORKSPACE_KEY). Best-effort.
- `restoreOpenTabs` — Restore openTabs from IndexedDB after a workspace (re)connects: drop any file that no
- `renderDocTabs` — Render the document-tabs strip. Shown ONLY with a connected workspace (gated on
- `openDocTab` — Open a document into a tab. switchWorkspaceDoc does the swap AND registers the tab +
- `closeDocTab` — Close a tab from the strip. If it's the active doc, switch to the neighbor first
- `cycleDocTab` — Cycle to the next/prev open tab (Ctrl/Cmd+Shift+] / [). No-op with <2 tabs.
- `gotoDocTab` — Jump straight to the Nth open tab, 1-based (Alt/Option+1..9). No-op if there is no such
- `connectWorkspace` — Connect (user gesture): pick a folder, write the doc into it once via the existing OPML
- `connectAdoptExisting` — #839: the connect-by-opening path — the current doc is blank and the chosen folder
- `reconnectWorkspace` — Reconnect (user gesture): a stored handle was rehydrated but lacks permission; re-request
- `reopenWorkspaceDoc` — Reopen the workspace doc from disk, honoring newer-wins, and rebind for auto-write.

## workspace link index (CF-1: scan the folder into the cross-doc index)

- `unmarkedFilesNote` — Scan every .opml file in the connected folder into one in-memory cross-document
- `scanReparseList` — §5.2 (cross-document-direction.md): which files a rescan must actually re-read + re-parse.
- `_wsScanCache` — §5.2 incremental rescan. Still visits every file (so additions/deletions are always caught —
- `_wsScanDir`
- `_wsLastScanReparsed`
- `scanWorkspace`
- `refreshWorkspaceIndex` — Rebuild the index from the connected folder. Call (do NOT await — background) after any
- `_wsOwnIdxAt` — §5.4: refresh-on-save — after a successful folder write of the CURRENT doc, fold its live
- `refreshOwnDocInIndex`

## document switcher (brief #5b: the multi-doc workspace)

- `listWorkspaceNames` — List the connected folder's .opml documents (shaped + sorted by the pure core).
- `DISCARD_CONFIRM`
- `switchWorkspaceDoc` — Switch to a different doc already in the folder. Dirty-guarded like Open.
- `newWorkspaceDoc` — New document inside the folder (collision-safe). Reroutes File → New when a folder is
- `createWorkspaceDocNamed` — The actual folder-doc creation, given a chosen (possibly blank) name. Split out of
- `createWorkspaceNote` — CF-5: create a new note (.opml) in the folder titled `title`, WITHOUT switching to it —
- `saveToWorkspaceDoc` — File the CURRENT (detached) document into the folder under a collision-safe name and
- `deleteWorkspaceDoc` — Delete a doc from the folder (confirmed). If it was the current doc, fall through to the
- `teardownWorkspace` — Tear down all workspace state and stop auto-writing. Does NOT delete any file or
- `disconnectWorkspace` — Disconnect: forget the connection and stop auto-writing. Does not delete the file or folder.
- `degradeWorkspace` — Single degrade point: clear the folder-backed file, flip the affordance to Reconnect,
- `anchorWorkspaceFile` — sync-safety: record the current workspace file's on-disk lastModified as our
- `safeWriteOpml` — sync-safety: atomic OPML write. Write to a hidden temp sibling, then move() it over
- `flushWorkspaceFile` — Continuous auto-write to the folder-backed file (brief #4). Called (not awaited) on
- `handleExternalChange` — sync-safety: the workspace file changed on disk under us (another device via folder
- `openReconcileDialog` — sync-safety: the 3-way reconcile prompt. Modeled on openConfirmDialog (same io-card
- `openDupDocIdDialog` — #745: let the user resolve a duplicate-docId collision — one button per file to KEEP the shared
- `_dupReconcileRunning` — #745: scan the folder for files sharing a docId and, per collision group, prompt the user to pick
- `reconcileDuplicateDocIds`
- `_wsObserver` — sync-safety: detect an external change to the OPEN file and reconcile BEFORE the next
- `checkExternalChange`
- `startWorkspaceWatch`
- `stopWorkspaceWatch`

## #847: same-origin multi-tab presence (lightweight; deliberately NOT live sync)

- `PEER_TTL_MS` — Two Pointliner tabs holding the SAME document degrade each other: each tab's flush
- `PEER_HEARTBEAT_MS`
- `TAB_ID`
- `_plPeers`
- `_plChannel`
- `_plNoticed`
- `updatePeers` — Pure: fold one presence message into the peers map (a new map; a malformed or unknown
- `peerHoldsDoc` — Pure: is any peer holding `docId` fresh enough to count as present?
- `peerHasSameDoc`
- `plAnnounce`
- `maybePeerNotice` — The soft notice, once per doc per session, whenever same-doc presence is learned (P4).

## plain-text / markdown export

- `frozenTokenText` — UXP-137: the frozen plain-text form of ONE artifact token — the value it currently shows,
- `flattenArtifacts`
- `flattenSpoilers` — A one-way export can't conceal, so a spoiler line ">! text" exports as "(spoiler) text"
- `stripInlineMd` — Strip the inline markdown emphasis markers for a clean plain-text rendering.
- `toMarkdown` — Markdown export: a nested bullet list mirroring the outline, 2 spaces per level.
- `markdownToPoints` — #1265 v1: parse Markdown into an array of top-level point nodes. THE INVERSE of toMarkdown. Headings
- `resolveWikilinks` — #1265 PR 2: resolve Obsidian-style [[wikilinks]] against a name->id map (names lowercased). A plain

## #1296: spreadsheet paste → points with tagged number properties

- `importCellValue` — A cell's value: strip surrounding quotes, and reduce a currency/thousands/percent-formatted number
- `importKeyName` — A header cell → a property key the engine can reference (evalMath idents are [a-z0-9_], and
- `importSplitRow` — Split one delimited line. Tab-separated is a plain split (Excel/Sheets clipboard is TSV and never
- `sniffDelimited` — Does this text look like a delimited table? Returns the delimiter ('\t' or ',') or null. Strict on
- `tableToPoints` — Parse a delimited table into ONE heading subtree: row 1 is the header (its columns name the props),

## #1310: CSV export, the OUT-seam mirror of the spreadsheet paste (#1296)

- `csvCell` — Panel re-run: Yuki's accountant and Diane want a grid, not the read-only HTML page. A section of
- `rowsToCsv`
- `sectionToCsvRows` — A point's child rows → [headerRow, ...dataRows], or null when there is no table to export (fewer than
- `markdownVaultToPoints` — #1265 PR 2: import a set of Markdown files (a "vault") into ONE document. Each file becomes a

## #1265: BibTeX (.bib) → citations in the document footnote store

- `LATEX_ACCENT` — Symbol and letter accents, as COMBINING marks: `\"o` → o + U+0308, NFC-normalized to `ö` at the end
- `bibTranslateLatex` — Translate the LaTeX a .bib file uses to SPELL a name into the characters it means. Split out from
- `bibFieldText` — A field value as readable text: accents translated, escaped punctuation unescaped, any REMAINING
- `bibDropsLatex` — Did cleaning this value have to DROP a command, rather than translate one? Drives the "simplified"
- `bibAuthorNames` — `author`/`editor` → one name per entry. Splits on ` and ` at brace depth 0, so a braced corporate
- `bibYear` — The publication year: `year` when it is one, else the first 4-digit run in BibLaTeX's `date`.
- `bibCitation` — One entry → one neutral citation line. Deliberately style-free (no APA/MLA/Chicago switch): the
- `sniffBibtex` — Does this text look like a BibTeX database? True only when the first non-blank, non-comment content
- `parseBibtex` — Parse a BibTeX/BibLaTeX database into entries. A brace-aware SCANNER, not a regex: values nest braces
- `parseBibEntryBody` — One entry's inside: `key, field = value, field = value`. Split out from parseBibtex so the field
- `bibToPoints` — A .bib file → ONE heading subtree, each entry a point whose [^key] footnote holds its citation.

## #1282: read-only shareable web page — the OUT seam (mirror of Markdown import, the IN seam)

- `checkVerdictHtml` — Panel #3/#4, 3-4 of 7, a dealbreaker for two. The existing HTML export packs the whole app and
- `nodeToReadonlyHtml`
- `readonlyDocTitle` — A page title: the first heading point's cleaned text, else a neutral fallback (there is no doc-name
- `RO_EXPORT_CSS`
- `docToReadonlyHtml`
- `toPlainText` — Plain-text export: tab-indented hierarchy, markdown emphasis stripped.
- `downloadText` — `nameOverride` (without extension) lets a partial export (a point / selection) name
- `exportedNote` — P4: every export reports success, like the self-contained HTML export already does.
- `exportMarkdown`
- `exportPlainText`
- `exportReadonlyHtml` — #1282: the read-only shareable page. Frozen like the .md/.txt exports (values, not the live app), so
- `exportSectionCsv` — #1310: export a section's rows as a .csv an accountant or spreadsheet opens (the OUT mirror of paste).
- `exportNodesToMarkdown` — Export a point (or a set of selected points) to Markdown. Reuses toMarkdown by wrapping

## self-contained HTML export (C1)

- `embedOpmlIntoHtml` — One file that IS the document AND the app: a copy of this whole page with the
- `extractEmbeddedOpml`
- `exportSelfContainedHtml`

# Panels & recognition surfaces

## search wiring

- `searchDebounce`
- `applySearch` — Apply a search query and re-render. Heavy keystroke-driven searches go
- `renderSearchProblems` — The #sh-invalid island: the search box's rendering of searchTermProblems. Returns the problems

## saved searches wiring

- `syncSearchSave` — The star toggles the current query in root.savedSearches (the bookmark model:
- `renderSavedSearches`
- `applySavedChip`
- `forgetSavedSearch`
- `renderWorkspaceSearchResults` — WS-2: cross-doc search results in the focus-shown search panel. The in-doc filter (render()
- `openWorkspaceSearchHit` — Jump to a workspace search hit: switch to its doc (dirty-guarded), drop the filter for a
- `shSavedList`
- `externalDropContent` — #1329: drag an image from a web page onto the outline → a new point that references it. A LOCAL image
- `dragCarriesExternal`
- `handleExternalDrop`
- `searchHashtag` — Apply a tag chip's filter — shared by the click handler and the Enter/Space
- `_clockLongPressed` — Touch has no Shift, so step-back on a manual clock is a LONG-PRESS (tap still advances) —
- `toggleSpoiler` — Reveal is transient DOM state only (never written to node.text), so a spoiler
- `refocusAfterRepaint` — Advance the clicked clock by dir (+1/-1). The clock's ORDINAL is its position among
- `advanceClockAt`
- `advanceClockOrdinal` — Advance the ORDINAL-th manual clock in node.text by dir. Shared by the pointer/keyboard
- `clockAtOrdinal` — The {done,total} of the ORDINAL-th valid clock in text (manual or computed alike), or null.
- `resolveStatTarget` — Where a stat NAME lives, so an action pill can write its new value back: a numeric property
- `runActionPill` — Apply an action pill: read its {target OP rhs} from data-act-body, compute the new value in the
- `openPropChip` — A property chip opens its editor: a date chip (due/start) → the Schedule
- `revealCheckOffenders` — #1195: reveal the points a failing check flags. For a subtree-scoped check, zoom to the check's own

## footnotes

- `getFnRefs`
- `fnId` — A footnote id: a stable opaque token (the pill-id style), the KEY inside a [^id] marker and the
- `newFootnote` — Create a footnote in the doc store and return its stable id (the key to put in a [^id] marker).
- `migrateNodeFootnotes` — Lift a node's legacy per-node footnotes into the doc-level store, remapping [^key] -> [^id] in the
- `migrateFootnotesToStore` — Walk `subtree` (INCLUDING its root) migrating every node's legacy footnotes into `store`. Used at
- `migrateDocFootnotes` — Doc-level load hook: lift every point's legacy per-node footnotes into the root store. Skips the
- `footnoteOrder` — doc-cache: footnoteOrder — document-order footnote numbering. Walk points in render order; each
- `footnoteReport` — Phase B: the Footnotes manager model (pure). Every store entry with its document-order number (null
- `fnIsWritten` — UXP-237: is this footnote actually written? The ONE definition, shared by the render cue
- `stripUnwrittenFnRefs` — UXP-237: an unwritten footnote's MARKER must not survive into an exported file. Both text exports
- `renumberFnMarkers` — Phase A: exports display the document-order NUMBER, not the raw store id, so an exported file reads
- `countUnwrittenFnRefs` — How many markers an export will drop, so the toast can say so rather than removing them silently.
- `countExportLinks` — #1111: how many links the PLAIN-TEXT export will dissolve into prose, so the toast can say so.
- `countMirrorRefs` — #1142: how many MIRRORS a text export will flatten to a single title line, so the toast can say so.
- `syncFnEntries` — Phase A: ensure the doc store has an entry for every [^id] marker this node carries (a blank
- `getFnKeyAtPoint` — detect [^id] pattern under a mouse point (for raw-edit-mode hover)
- `hiFnItem` — highlight fn-panel item for a given key (no cursor movement)
- `fnNodeId`
- `scheduleFnHide`
- `panelOpenSay` — #1473: what a docked panel says when it appears. Pure, and deliberately NOT new copy: it speaks
- `_panelSaid` — Say that a panel appeared, ONCE per appearance. Only for panels that appear WITHOUT taking
- `panelAccName` — The name the accessibility tree reads for a panel. ONE source, so what is announced and what
- `announcePanelOpen`
- `forgetPanelSaid`

## #1465 C2: the docked-panel ring (F6)

- `DOCKED_PANELS` — The same three panels as the comment above: the ones that appear WITHOUT taking focus. That is
- `DOCKED_PANEL_IDS`
- `panelRingStep` — Pure. `openIds` are the docked panels currently open, in page order; `hereId` is the panel that
- `panelSubjectId` — Which point a panel is ABOUT — where "back to the outline" lands when there is no armed caret
- `dockedPanelOpen` — The strips slide in with `.on`; the in-flow section is simply present or absent.
- `openDockedPanels`
- `focusedDockedPanel`
- `PANEL_FOCUS_SEL` — The row F6 lands on: the first focusable inside the panel's ROW container, so the landing is a
- `panelEntryEl`
- `leaveDockedPanel` — Back out of a docked panel to the outline: the caret exactly where it stood — the same restore
- `stepDockedPanels`
- `showFnPanel`
- `hideFnPanel`
- `activateFnRef` — UXP-55: reveal + lock the footnote panel and highlight the matching entry, scrolling it into

## backlinks panel

- `blNodeId`
- `scheduleBlHide`
- `syncBodyPadBottom` — Adjust outline padding-bottom so the open footer panels push the outline upward
- `syncBlPanelBottom`
- `syncHelpBtnBottom` — Keep the floating help button (#sc-toggle) and its panel above whatever bottom panels
- `blGather` — Gather a point's inbound references: same-doc backlinks + unlinked mentions, plus the cross-doc
- `blSectionModel` — #953: the inbound-refs answer is now drawn in TWO places — the bottom strip (caret-driven, the
- `_zblVer` — #953: blGather is not cheap — it walks the whole tree in collectUnlinkedRefs and every folder
- `blGatherWarm` — A memo READ that never computes: the in-flow section uses it to decide whether the full gather
- `blGatherCached`
- `wireCorkKeys` — Keyboard on the board. One Tab stop for the whole grid (the roving pattern this app uses in seven
- `buildCorkToggle` — The Outline | Cards switch, in the zoom view. The `.ag-toggle` chip skin, the same one the graph
- `buildCorkboard` — The board. Each card holds a REAL `.node-content[data-id]` handed to the point editor, so undo,
- `CORK_SNIPPET` — #955 the corkboard: a zoomed point's children as index cards you can rearrange and write on.
- `corkboardCards`
- `cardMoveIndex` — Where a keyboard move lands. `Alt` is the movement modifier everywhere in this app, and a grid
- `buildZoomBacklinks` — #953: the note's own inbound refs, in flow under its children. Same rows, same model, same
- `_zblFill` — Stage 2. Cancels any in-flight fill (a second render supersedes the first), and re-checks
- `scheduleZoomBlFill`
- `blShowWith`
- `showBlPanel`
- `hideBlPanel`
- `renderBlPanel` — The bottom strip: the original caller, now a thin wrapper over the shared renderer.
- `renderBlRows` — #953: the shared row renderer. This was renderBlPanel's body, with three things parameterised so
- `updateBlPanel`
- `syncFnPanelBottom` — Keep the panel docked above the software keyboard on mobile. Crucially the
- `updateFnPanel`
- `renderFnPanel`
- `convertToFootnote`
- `returnToFnRef`
- `fnPanelEl` — hovering the panel itself keeps it alive in transient mode
- `blPanelEl` — backlinks panel

## variables overview panel

- `varPanelEl`
- `isVarPanelOpen`
- `openVarPanel`
- `closeVarPanel`
- `toggleVarPanel`
- `_vpExpandedGroups` — Variable-base panel groups: a base's ~rows×cols projections collapse under one header so
- `varKindLabel` — #1353 Phase 3: the word for a variable's KIND in the Variables panel, or '' when it needs none.
- `vpRow`
- `updateVarPanelContent`

## agenda strip

- `agendaOpen`
- `agendaView` — The extra view shown as a full-width bar below the always-present List. One at a time
- `agendaBars`
- `agendaShowRunning`
- `agendaShowDone`
- `agendaOverdueOnly`
- `agendaSort`
- `agendaFolderScope`
- `agendaFolderScopeAvailable` — Folder scope needs an open workspace (File System Access, Chromium-only). Kept as a named pair so
- `agendaFolderUnavailableMessage`
- `agendaMonth`
- `agendaWeek`
- `ganttNamesW`
- `ganttResize`
- `agToggleKey` — #1385: the identity a chip keeps across a renderAgenda() rebuild. The LABEL is the default and is
- `mkAgToggle` — #1385: `key` is POSITIONAL and optional, so the six call sites that are happy with their label keep
- `agRefocusToggle` — Only on the keyboard path: on the pointer path focus was deliberately never moved here, and pulling
- `mkAgNavBtn` — A calendar/week nav arrow button (‹ › ‹‹ ››). onFire is wired by the caller (mousedown
- `mkAgRow`
- `agMini`
- `agItemAria` — Spoken label for a dated point, shared by the Gantt bars and calendar items:
- `agDocLabel` — The display name of the document a row belongs to, or null for this one. Reads the index's
- `wireAgNav` — Wire an agenda element to navigate (zoom) to its point. Clicking ZOOMS, never
- `mkAgChip`
- `renderAgenda`
- `_refocusGanttResize`
- `renderAgendaList`
- `GANTT_NAMES_MIN` — Gantt timeline: fixed names column on the left; scrollable chart on the right.
- `clampGanttNamesW`
- `renderAgendaGantt`
- `ganttGlobalInit` — One-time document-level move/up for the Timeline names-column resize (mirrors
- `ganttInitGlobal`
- `_agExpandedDay` — Which day is currently expanded (epoch-day) in the Week/Month view — clicking an
- `createDatedPointOnInbox` — Shared cell filler for Week columns and Month cells. Fills `box` with a "x · y" count
- `fillAgendaDay`
- `renderAgendaWeek` — Week view — the planner spread: 7 day columns (Sun→Sat) for the shown week, plus a
- `renderAgendaCalendar`
- `openAgenda`
- `closeAgenda`
- `toggleAgenda`

## link graph (#516: the browsable recognition surface)

- `SVG_NS` — A full-viewport overlay drawing the document's internal links as a node-link web,
- `graphOpen`
- `graphScope`
- `graphShowUnlinked`
- `_graphUnlinkedCache` — Cap on how many unlinked edges the graph draws per open — see the GRAPH_UNLINKED_CAP comment
- `graphAnchorId` — #898: the point the Nearby scope is rooted at, and the adjacency map it walks. The anchor is
- `_graphNearbyAdj`
- `graphAnchorNow` — Resolve the point a Nearby graph should centre on. captureCurrentPointId is the established
- `openGraph`
- `closeGraph`
- `toggleGraph`
- `renderGraph`

# Chrome, boot, builder & autosave

## boot

- `fileMenu` — file menu
- `logoBtn`
- `fmActiveCat` — #603: the menu is a single scrollable page (all categories stacked), with a category nav that
- `fmSetActiveNav`
- `fmRestoreAll`
- `fmShowCategory`
- `fmApplySearch`
- `fmScrollSpy` — Scroll-spy: as the single page scrolls, mark the nav for the section currently at the top (skip
- `relTimeShort` — #1268: a compact relative time for the save chip ("just now", "3 min ago", "2 hr ago"). Pure.
- `saveStatusLabel` — #1268: map the save state to the honest chip label. Pure + testable. The order encodes the truth:
- `SS_GLYPH`
- `SS_TITLE` — #1286: lead with the OWNED FILE, not "the browser." The nervous single-file user (Walter) read
- `saveStatusState` — #1268: read the live save state for the chip. Impure (reads globals); the pure mapping is above.
- `updateSaveStatus`
- `updateFmStatus`
- `fmtKey`
- `openFileMenu`
- `closeFileMenu`
- `fmItems` — keyboard path added ALONGSIDE the pointer path. Per ux-discipline §7.1 the file
- `fmNavBtns`
- `saveStatusEl` — #1268: the save-status chip is the door to the safety story — click it to open the File menu (export,
- `syncRollLogLabel` — #918: the roll-log on/off toggle. Sets state IN PLACE (no closeFileMenu) so it reads like the
- `toggleRollLog`
- `syncWebImagesToggle` — #1328: the per-document web-image control — the revocation half of the #1325 opt-in. The label names the
- `VERBOSITY_NAMES` — Guidance level is a 3-choice CARD (modes batch, item 4): picking a tier sets it IN PLACE and
- `syncVerbosityCard`
- `syncVerbosityStrip`
- `setVerbosity`

## Toolbar-feature chooser (modes batch, item 5)

- `TOOLBAR_FEATURES` — Let the user pick which OPTIONAL feature buttons appear in the top-right cluster. Core controls
- `TOOLBAR_PREF_KEY`
- `TOOLBAR_ORDER_KEY`
- `hiddenToolbar`
- `saveToolbarPrefs`
- `saveToolbarOrder`
- `applyToolbarPrefs`
- `scrollCueClasses`
- `syncScrollCue`
- `syncScrollCues`
- `wireScrollStrip`
- `buildToolbarChooser`

## theme + accent

- `forcedTheme`
- `themeStyle`
- `accentStyle`
- `ACCENT_PRESETS`
- `accentId`
- `hexToRgb`
- `applyAccentCSS`
- `setFaviconHref` — Assert OUR favicon over any other (incl. a root-site /favicon.ico). Mutating an
- `updateFavicon` — Repaint the circle-minus favicon in the given colour (inline, offline). Sets an SVG
- `buildAccentSwatches`
- `themeColorMetas` — PWA title bar: the media-scoped theme-color metas are the palette's third home
- `applyTheme`
- `fullWidth`
- `applyWidth`
- `syncDoneBadge` — MOBILE-2: the persistent "N done hidden" cue lives ON the Done button — the cue and the
- `syncNotesBtn`
- `IS_MAC` — The verbosity dial: cycle guided → standard → lean → guided, + persist + flash. Reachable via the
- `MOD`
- `VERBOSITY_FLASH` — #616: build the verbosity toasts with the platform MOD, not a hardcoded Mac ⌘ that non-Mac
- `syncVerbosityClass`
- `toggleVerbosity`
- `TB_GUIDE_MAP` — Right-click a toolbar button to open its concept guide entry.
- `tbtnTooltip` — Tier-gated toolbar tooltips: immediate (Guided), delayed (~500ms Standard), absent (Lean).
- `_ttpTimer`
- `showTbtnTooltip`
- `hideTbtnTooltip`

## shortcuts / syntax reference

- `GUIDE` — Grouped, data-driven so it stays in sync as features are added. IS_MAC/MOD are defined
- `guideBodyHtml` — A plain-text guide body becomes HTML: escape first, then two authoring conveniences
- `SC_SECTIONS` — The five keyboard sections rendered as ONE scrollable page (Navigate, Edit, Insert,
- `shortcutsAllBody`
- `shortcutsSectionText` — The searchable text for one Shortcuts page: its keys + labels (+ the words a user
- `scToggle` — The `?` chrome button opens the one help surface — the Concept guide, on its Shortcuts
- `highlightGuideMatch` — Mark every occurrence of `q` (already lowercased) in the guide entry's text, like document
- `guideOpen` — The guide is the one help surface. `?` (and the help chords) toggle it: open on
- `toggleGuide`
- `openGuide`

## builder window

- `builderState` — Guided-only: a unified command browser that absorbs the /, @ and { inline menus.
- `builderFilterCmds` — #1106: the builder's search is the COMMAND LINE in Guided (the default tier) — `/` matches every
- `builderBestIdx` — #1106: which row Enter actually fires. The list keeps POOL order so renderNav's section headers
- `builderCmdPool`
- `builderGuideEntry` — Find the GUIDE entry that covers a command. BLOCK_CMDS/INSERT_CMDS ids are
- `PREVIEW_SAMPLES` — Curated, self-contained sample expressions for the builder pane's live preview — one runnable
- `builderPreviewSample` — DOM-free: run a previewable command's sample through the same pure cores the pill uses and return a
- `renderBuilderPane` — Render the guide entry detail for a command into the right pane.
- `renderBuilderPaneInto`

## #1240 guided authoring: "Add another like this" — the pure shape core

- `ROW_TAG_RE` — The panel's headline: reading a starter is delightful; authoring one means typing the brace language.
- `ROW_LEAD_STATE_RE`
- `ROW_CHECKBOX_RE`
- `ROW_ISODATE_RE`
- `rowLeadState`
- `rowContentTags`
- `rowIsTodo`
- `inferPropType`
- `SHAPE_SKIP_KEYS` — Keys that are never a USER COLUMN, so they can neither become one nor make a row look like data.
- `dataPropKeys`
- `inferRowShape` — inferRowShape(siblings, states?) → { sharedTags:[...], props:[{key,type}], todo } | null. A tag/prop is
- `buildRow` — buildRow(shape, values, textValue) → the bullet text a filled form produces, in the authored order the
- `_descCount` — #1240 phase 2: the STRUCTURED path. When a list's rows are PAGES (they have their own children — a
- `richestStructuredSibling`
- `blankScaffoldText`
- `blankScaffoldNode`
- `DECK_MODES` — #1240 phase 3: "Add a card" to a {shuffle:}/deck pill. A deck lives in node.grammar as a record
- `findDeckRecord`
- `deckCardText`
- `rowNoun` — #1240 the DOM half (phase 1): a focused "Add another" dialog. Opened for a shaped list (a heading whose
- `createPromotedRow`
- `stampScaffold` — #1240 phase 2 DOM half: stamp a blank scaffold of a structured sibling under its heading. Clones the

## #1438 / UXP-329: the keyboard route to "add a row like these"

- `addRowTarget` — DOM-free. The "+ Add" door is mouse-only: every affordance is tabindex="-1" with no keydown
- `openAddRowForm`
- `affordanceDirtyIds` — #1240 phase 1b: the VISIBLE front door. On a heading whose children share a shape, append a "+ Add"
- `refreshRowChrome` — The DOM half. Everything buildRow puts on a point OUTSIDE `.node-content` goes stale after a commit,

## #1240 phase 4: the doors of the point you are LOOKING AT

- `viewDoorHost` — DOM-free. Every "+ Add" / "+ Total" / "+ Check" / "+ Variance" door hangs off its parent's
- `renderViewDoors` — The DOM half: give the view parent's doors a home at the END of the list they belong to. It
- `refreshViewDoors` — Keep the view bar honest after an edit that changes the shape (a new property makes a column
- `maybeAddRowAffordance`
- `addDeckCard` — #1240 phase 3 DOM half: append a card to a deck record + re-render (round-trips to the shuffle shorthand).
- `openAddCardForm`
- `maybeAddCardAffordance` — The "+ Card" door on a line that carries a deck pill — same affordance class + verbosity gating as "+ Add".

## builder embedded forms

- `BUILDER_FORMS`
- `showBuilderForm`
- `builderFormApply`

## builder nested dialog helpers

- `builderRestoreTrigger`
- `builderSpliceText` — #1396: the text a builder apply must splice, and the offset in ITS coordinate space.
- `builderSpliceOffset`
- `showNestedDialog`
- `syncBuilderBtn` — Keep the toolbar Command-browser button's pressed state in sync with the modal.
- `launchBuilder` — The builder's visible front door (UXP-178): open it WITHOUT a typed /@{ trigger, from the
- `openBuilder`
- `closeBuilder` — The one closeBuilder — shared by openBuilder (via ioCancel + its handlers) and the top-level
- `builderReturnTypedText` — Put the palette's search text back into the point it was typed from (#1108).

## autosave

- `AUTOSAVE_KEY`
- `AUTOSAVE_PREV_KEY` — UXP-147: ONE rolling pre-overwrite snapshot, so the in-session "Ctrl+Z to undo" promise survives a
- `SNAPSHOT_EVERY_MS`
- `PREV_KEEP_DOCS` — The prev-version store is a PER-DOC map { [docId]: { prev, at } } (was a single global slot, which
- `PREV_BYTE_CAP`
- `parsePrevStore` — Parse the prev-version store into a { [docId]: {prev, at} } map. Migrates the LEGACY single-slot
- `readPrevVersions`
- `withPrevStoreLock` — #846: the prev-version store is ONE localStorage JSON map maintained by read-modify-
- `capturePayloadOf` — Preserve an autosave payload as a restore point under the given docId, immediately
- `stashPayloadAsPrev`
- `rekeyPayloadDocId` — Rewrite an autosave payload's embedded docId. The reconnect stash keys an identity-
- `evictPrevVersions` — `strict` drops the keep-at-least-the-newest exemption: the quota-budgeted stash (see
- `_lastSnapshotAt`
- `autosaveTimer`
- `autosaveDisabled`

## OPFS durable autosave (the working-copy store on non-folder-backed browsers)

- `hasOPFS` — The localStorage crash-buffer caps near 5MB and silently disables past it. OPFS
- `opfsOk` — #1113: hasOPFS is PRESENCE. `getDirectory` exists in a private window and on file://,
- `_lastLsQuota`
- `OPFS_AUTOSAVE_NAME`
- `_opfsWriting`
- `_opfsReconciled` — Gate: until the boot reconcile has read OPFS, autosave is held entirely, or the
- `opfsHandle`
- `writeOpfsAutosave` — Coalesced async write of the autosave payload to OPFS. Never throws to callers.
- `readOpfsAutosave` — Read the OPFS autosave: returns { text, lastModified } or null. Never throws.
- `STORAGE_SOFT_LIMIT` — Most browsers cap localStorage near 5MB. Warn before we hit the wall so the
- `LS_TOTAL_BUDGET` — The working assumption the code plans around: most browsers cap localStorage near 5M
- `storageWarnState`
- `storageWarn`
- `storageWarnMsg`
- `storageWarnBtn`
- `_warnAction` — The action button is mode-aware: normally "Save to file", the reconnect banner (folder needs a
- `storageWarnExamples` — #565: a second action shown ONLY on the first-run examples banner, beside "Start a blank document".
- `showReconnectBanner` — A folder was connected in a previous session and its handle is still stored, but the
- `pendingFile` — UXP-127: a single file was open in a previous session and its handle is stored, but the browser
- `showReopenFileBanner`
- `showExamplesBanner` — UXP-126: the first-run Examples banner. Reuses the soft #storage-warn surface with a "Start a
- `hideExamplesBanner`
- `_blankInviteDismissed` — The blank-canvas door (beginner program, PR B): past first-run, an empty document was a dead end,
- `_blankInviteReady`
- `blankInviteDecision` — Pure show-decision (DOM-free, pinned): every gate in one testable place.
- `showBlankInviteBanner`
- `hideBlankInviteBanner`
- `syncBlankInvite` — Re-evaluated on every render (the single repaint point), so the banner appears when a doc goes
- `reopenPendingFile`
- `showSnapshotBanner` — Shared-snapshot banner (UXP-102). A self-contained-HTML recipient's edits autosave but are
- `showStorageWarn`
- `hideStorageWarn`
- `showWorkspaceWarn` — Lost-workspace-access banner (brief #4). Reuses the dismissible #storage-warn alert
- `scheduleAutosave`
- `writeAutosavePayloadNow` — The debounce body, extracted so the pagehide flush below can run it synchronously.
- `flushPendingAutosave` — #802 (agent-review, UXP-195): text typed in the debounce window must survive a tab
- `_backupNudged` — UXP-119: on the weakest durability tier (no File System Access, so no folder auto-write and
- `BACKUP_NUDGE_MIN`
- `maybeBackupNudge`

## depth nudges (#519): the firing side

- `NUDGE_STORE_KEY` — Generalizes the storage-quota nudge (the sanctioned proactive-hint precedent, ux-
- `nudgeSeen`
- `markNudgeSeen`
- `fireNudge` — The single door. Returns true if it fired (for tests / callers that want to stop probing).
- `maybeNudgeSum` — Trigger 1: a numeric property just added to a point that has a sibling/child sharing it.
- `maybeNudgeRoll` — Trigger 2: a #tag roster grew big enough to be a random-draw pool. Called after a
- `maybeNudgeFirstPill` — Trigger 3: the first time a user's OWN typing turns {…} into a live pill, welcome it once.
- `fieldNudgeKey` — Trigger 4: a point whose text ENDS in a bare `word: number` (e.g. "Design mockups cost: 120") is a
- `maybeNudgeField`
- `isLongDecimal` — #1333 (driven panel #7): a division renders at 8 significant figures by default (91.666667), which a teacher
- `maybeNudgeDecimals`
- `maybeAddFieldAffordance` — #1334 (driven panel #7): typing a bare `key: number` (hp: 15, cost: 120) is a beginner reaching for a
- `maybeNudgeTypedLink` — A hand-typed `[[Name]]` that resolveTypedLinks could NOT turn into a link (no point by that
- `_autosaveDeferred`
- `storageAdvice` — The localStorage crash-buffer write. With OPFS as the durable store, hitting the
- `writeLocalAutosave`
- `lastAutosaveSavedAt` — Returns the savedAt epoch-ms from the localStorage autosave, or 0 if absent/unreadable.
- `lastAutosaveFileModified` — #746: the workspace file's lastModified as we recorded it (_wsKnownModified) at the moment
- `loadedFromEmbed` — C1: a self-contained export inlines its outline in #pl-embedded-doc. When that
- `embedRestoreFailed`
- `applyAutosaveData` — Apply a parsed autosave payload (root + view prefs) to the live app. Shared by the
- `earlierVersionForCurrentDoc` — UXP-147/165: is there a rolling pre-overwrite snapshot to restore FOR THE CURRENT DOCUMENT? The
- `hasEarlierVersion`
- `restoreEarlierVersion` — UXP-147: swap the live doc for the pre-overwrite snapshot, behind the same confirm New/Open use so
- `_showingExamples` — UXP-126 first-run Examples state. DECLARED here, ABOVE the boot restore, because the restore
- `_adoptingExamples`
- `_restoredFromAutosave`
- `RESTORE_FAIL_MSG` — #1136: WHY a boot restore failed, and what the user can do about it. Pure + Node-testable.
- `restoreFailMessage`
- `autosaveRestoreFailed`
- `FIRST_RUN_EXAMPLES` — UXP-126: first-run Examples. On a genuinely-fresh boot (no restored autosave, not a snapshot,
- `STARTERS` — #565: "Start from an example" gallery. The turnkey solo-RPG content already exists in
- `startBlankOutline` — (_showingExamples / _adoptingExamples are declared above the boot restore — see the
- `showExamplesDoc` — Load the Welcome tour AS the whole document. This was the forced first-run path until #1192;
- `maybeShowFirstRun`
- `appendOpmlSubtrees` — #565/#518: append OPML subtree(s) additively — a non-destructive append-and-zoom that never
- `insertStarterSubtree` — #565: the starter gallery's additive insert — one `# heading` root subtree, and no toast on a
- `IMPORT_ICON` — #518: the "Import points" door. Bring the points from another Pointliner .opml file (or pasted
- `openImportDialog`
- `STARTER_ICON` — #565: the "Start from an example" gallery. A picker over the embedded STARTERS (each inserts
- `WELCOME_QUICK_PICKS` — #1192/#1214: the six starters the Welcome chooser leads with. Everyday domains a stranger
- `_welcomeOpen` — var (hoisted, #854's rule): closeIo and adoptDoc are declared thousands of lines ABOVE this and
- `openStarterGallery`
- `dismissWelcome` — #1192 (§3c): dismissing the Welcome chooser is a real exit, not a deferral. Clear the pristine
